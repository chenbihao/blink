diff --git a/runtime/llama.cpp/CMakeLists.txt b/runtime/llama.cpp/CMakeLists.txt
--- a/runtime/llama.cpp/CMakeLists.txt
+++ b/runtime/llama.cpp/CMakeLists.txt
@@ -17,4 +17,7 @@ if(MSVC)
     add_compile_definitions(NOMINMAX _USE_MATH_DEFINES)   # min/max macros + M_PI on MSVC
 endif()
-set(BUILD_SHARED_LIBS OFF CACHE BOOL "" FORCE)   # static deps -> self-contained binaries
+# Blink loads ggml backends from the selected artifact directory.  Keep the
+# core and backend libraries dynamic so base/Vulkan/CUDA artifacts remain
+# composable and do not import optional GPU DLLs from the CPU package.
+set(BUILD_SHARED_LIBS ON CACHE BOOL "" FORCE)
 set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
