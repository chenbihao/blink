[package]
name = "xtask"
version = "0.0.0"
edition = "2024"
publish = false
description = "Blink 构建编排工具（编译插件 / 打包入口 / release 资源校验）"

[[bin]]
name = "xtask"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
serde_json = "1"
