[package]
name = "blink-plugin-ip"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true  # 继承 workspace 级别的 lint（如禁止 UTF-8 字节切片）

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = "3.0"
