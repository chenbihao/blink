{
  "$comment": "PP-OCRv6 模型元数据。包依赖锁已迁移到 locked-requirements.txt（唯一锁源，由 Rust include_str! 嵌入）。",
  "$schema": "model-metadata/v1",
  "models": {
    "ocr_version": "PP-OCRv6",
    "production_ready": [
      "tiny"
    ],
    "tiny": {
      "det_model_name": "PP-OCRv6_tiny_det",
      "rec_model_name": "PP-OCRv6_tiny_rec",
      "params": "1.5M",
      "languages": 49,
      "$note": "唯一通过 spike 资格门的候选。spike 实测：冷启动约 4s 未过 2s 门，峰值工作集约 1136MB 接近 1.2GB 门。以 spike 真实结果为准，不写约 1.5s / 400MB。"
    }
  },
  "supply_chain_risks": [
    "PaddlePaddle PyPI wheel 的传递依赖版本可能随时间变化（numpy/pyarrow 等），已通过 locked-requirements.txt 显式锁定全部包+hash 缓解",
    "PaddleOCR 模型从 paddle-model-ecology.bj.bcebos.com 下载，上游不提供稳定 checksum",
    "SHA-256 对应 cp312/win_amd64 或 py3-none-any wheel；非 Windows 或非 cp312 环境需重新计算 hash"
  ]
}
