{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the settings window (核心 IPC + DevTools 快捷键)。settings 窗独立于 main,不共享 default capability——避免 main 专属的窗口操作权限(拖拽/调尺寸)污染。",
  "windows": ["settings"],
  "permissions": ["core:default", "core:webview:allow-internal-toggle-devtools"]
}
