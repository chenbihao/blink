{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "secondary-windows",
  "description": "次级窗口（voice-overlay / chord-screenshot / chord-pin / context-menu-popup）的 capability。这些窗口需要 core:default 来接收 Tauri 事件（blink://voice-level / voice-partial 等）和调用 invoke。dialog:allow-save 用于截图保存为文件。不授予窗口操作权限（拖拽/调尺寸），避免与 main 窗口混淆。",
  "windows": ["voice-overlay", "chord-screenshot", "chord-pin", "contextmenu-popup"],
  "permissions": ["core:default", "dialog:allow-save"]
}
