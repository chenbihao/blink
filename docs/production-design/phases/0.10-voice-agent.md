# Blink 0.10 — 语音输入(STT + 语音打字)

> **更新时间**: 20260715
>
> **范围**:STT 接入 + hold-to-talk 语音输入两个目标(G1 主窗口 / G2 输入法上屏)
>
> **主题**:在 0.9 文本闭环稳定后,换「嘴」替键盘打字。**架构不变,只加感知层**——语音是新的输入入口,产出的是文字;文字进哪条路由、要不要执行,全套用 0.9 的路由 / 执行 / 信任边界。
>
> **前置**:0.9 完成(Provider 多档 + 主窗口文本闭环 + 统一 tool 架构 + Capability 协议层)。
>
> **原则**:语音只产出文字,不直接执行动作(意图分叉交给 0.9 路由);本地 STT 不捆绑、按需下载;声纹数据上云强告知;P0 主链路零回归。
>
> **STT 架构变更（20260715）**:旧方案使用 sherpa-onnx（C++ ONNX 子进程），因 GitHub 下载不稳定、HuggingFace 模型国内不可达、stdout 解析脆弱等问题已废弃。新方案直接使用 **FunASR Python 工具箱** 的 `funasr-server`（OpenAI 兼容 API），复用现有 CloudSttEngine HTTP 逻辑，FunASR 自动管理模型下载/VAD/标点。
>
> **Python 环境自管理（20260715）**:Blink 通过 **uv** 自管理独立 Python 3.12 环境（`%APPDATA%\blink\python\venv\`），用户无需手动安装 Python 或 pip 包。uv 自动下载 Python + 安装 funasr，避免用户系统 Python 版本过高（如 3.14）导致 C 扩展编译失败。详见 §四 STT 选型。

---

## 〇、实现进度总览

> 以下标记当前代码库的实际完成状态。✅ = 已实现, 🔧 = 部分实现, 🔲 = 未开始。

| 模块 | 状态 | 说明 |
|---|---|---|
| **Hold 事件** (§3) | ✅ | `HotkeyEvent::Hold`/`HoldRelease` 已实现,hook 线程 timer |
| **麦克风采集** (§10.1) | ✅ | **cpal / WASAPI** 实现已替换 waveIn(详见下方) |
| **AudioCapture trait** (§10.1) | ✅ | `mod.rs` trait + `AudioChunk` + 工厂函数 |
| **SttEngine trait** (§10.2) | ✅ | `domain/stt/mod.rs` + `mock.rs` + `cloud.rs` + `local.rs` |
| **VoiceService** (§10.3) | ✅ | `app/voice.rs` hold→录音→STT→注入/fill-query 管线 |
| **文本注入** (§5) | ✅ | `infra/platform/inject/` Clipboard + Ctrl+V |
| **G2 mini overlay** | ✅ | `platform::window::show/hide_voice_overlay` |
| **STT 配置** (§6) | ✅ | `stt_config.rs` + `get/set_stt_config` 命令 |
| **设置页 UI** (§6.3) | ✅ | `frontend/js/settings/tabs/voice.js` |
| **音频测试** | ✅ | `start/stop_audio_test` 命令 + 音量条 UI |
| **本地 STT** (§4.2) | ✅ | `local.rs` FunASR server HTTP 转录 + uv 自管理 Python 环境 |
| **云端 STT** (§4.1) | ✅ | `cloud.rs` OpenAI 兼容 API |
| **STT 诊断** | ✅ | `diagnose_stt` 命令(引擎+模型+子进程全链路检查) |
| **模型管理** (§6.2) | ✅ | FunASR 自动管理模型下载，配置选择 + 统一日志 |
| **CPU/CUDA 切换** | ✅ | 推理设备选择 + CUDA 版 PyTorch 安装支持 |
| **TSF Composition** (0.10.3) | 🔲 | 原地流式注入 |
| **空间管理** | ✅ | Python 环境/模型缓存空间占用展示 + 清理功能 |
| **统一日志窗口** | ✅ | FunASR 运行日志 + 安装进度 + 诊断输出共用一个日志窗口 |
| **真流式** (0.10.3) | 🔲 | Paraformer-streaming 边说边出字 |

### 麦克风采集:waveIn → cpal 替换记录

> **时间**: 0.10 开发期间
>
> **问题**: 原 `windows.rs` 使用 winmm `waveIn` API(手写 FFI),在 Windows 10/11 麦克风隐私设置下
> `waveInOpen` 仍返回成功但采集数据**全是零**(静默拦截)。代码中积累了大量诊断逻辑
> (hex dump / WAV dump / volatile 读取 hack / WAVEHDR 地址固定 workaround)仍无法可靠读数据。
>
> **方案**: 引入 [`cpal`](https://github.com/RustAudio/cpal) 0.18(RustAudio 官方维护),Windows 上走 **WASAPI**。
> 麦克风隐私权限不足时返回明确错误而非静默给零数据。回调驱动替代 5ms 轮询,
> 无 unsafe FFI,代码量 ~450 行 → ~280 行(含重采样/downmix 纯逻辑测试)。
>
> **影响面**: `AudioCapture` trait 不变;`AudioDevice.id` 从 `u32`(waveIn 索引)改为 `String`(设备名);
> `SttConfig.audio_device_id` 同步改为 `Option<String>`。改动收敛在 `platform/audio/` 内部 + 配置类型。
>
> **默认设备透明化**:某些虚拟声卡驱动(如 VR/远程桌面的 Virtual Audio)会劫持 WASAPI
> `GetDefaultAudioEndpoint`,导致 cpal 返回的默认设备与 mmsys.cpl 面板显示的不一致。
> `AudioDevice` 新增 `is_default` 字段标注 cpal 认定的默认设备,设置页设备下拉框的「系统默认」
> 选项旁会显示实际默认设备名(如「系统默认(Virtual Desktop Audio)」),让用户一眼看出
> 默认设备是否被虚拟驱动劫持,从而决定是否手动选择设备。
> 同时用 `build_input_stream_raw` 统一回调类型,消除了原 match-on-format 的 6 个重复闭包分支。

---

## 一、为什么 0.10 才上语音(解耦的智慧)

语音是 Blink「丝滑」定位的完美放大器——把 P0「按键就能输入」推到「连打字都省了」:Alt+Space → 说 → 结果。

但语音也是工程复杂度最高的模态(STT 流式 / VAD / 降噪 / 中文模型 / 本地体积 / 声纹隐私)。把它和 0.9 的「AI 路由闭环」解耦,先在纯文本里把「路由 → 执行」跑顺,语音只是换个输入入口——**0.9 的 Provider / 路由 / Action / 信任边界全套复用,0.10 只加感知层**。

如果 0.9 就发现「AI 路由调本地能力」有坑,能在投语音前止损;反之 0.9 稳了,0.10 的不确定性收敛到 STT 单点。

---

## 二、两个目标:用嘴替键盘打字

0.10 的北极星不是某个炫技场景,而是**让用户可以用嘴代替键盘打字**——在两个 surface 上做好这件事:

### 目标一(G1):唤起后主窗口语音输入

> 用户 Alt+Space tap 唤起主窗口 → 再 Alt+Space hold → 按住即录、边说边转文字 → 松开即停 → 文字填进 blink 主窗口 `#query` → 自动触发搜索

```
Alt+Space tap → 主窗口出现
Alt+Space hold → 录音开始 → 边说边转文字(流式 partial 上屏)
松开 → STT 最终结果 → 填进 #query(queryEl.value = text + dispatch input 事件)
  → 0.9 主窗口搜索链路自动接管(防抖 → search_apps → 渲染结果)
```

**为什么这条几乎零新基建**:0.8.5 的 `blink://chord-fill-query` 已经验证了「后端 emit 文本 → 前端填 `#query` 并触发搜索」的链路(剪贴板 chord 用的就是它)。0.10 只需把「文本来源」从剪贴板换成 STT。

**意图分叉交给 0.9 路由**:用户说「找那个用来 balabala 的文件」→ STT 转成这句文字 → 进 `#query` → 0.9 路由判断是命令(命中 `search_files` Capability / Action)→ 搜文件。

### 目标二(G2):语音输入法出字上屏

> 用户在前台任意应用(编辑器、聊天框、文档)→ Alt+Space hold(不先 tap)→ 按住即录、边说边转文字 → 松开即停 → 文字打到前台应用光标处

```
前台应用 ← (光标在此)
Alt+Space hold → 录音开始 → mini overlay 显示流式 partial 文字
松开 → STT 最终结果 → 文本注入前台应用光标处(mini overlay 消失)
```

**这是 0.10 真正的硬骨头**:G2 必须新建一条文本注入通道。详见 §五。

### 两个目标的工程重心对比

| | G1 主窗口语音输入 | G2 语音输入法上屏 |
|---|---|---|
| 触发方式 | Alt+Space tap → 再 hold | Alt+Space hold(无先验 tap) |
| 文字去向 | blink 自己的 `#query` 输入框 | 外部前台应用光标处 |
| 流式展示 | 直接更新 #query | mini overlay 显示 partial |
| 需要新建 | STT 识别 + Hold 事件 | STT 识别 **+ 文本注入通道** + Hold 事件 + mini overlay |
| 复用现有 | `chord-fill-query` 链路 ✅ | 无,全新建 |
| 工程量 | 小 | 中→大(注入通道是难点) |

---

## 三、语音入口:Alt+Space 长按唤起(hold-to-talk)

### 3.1 设计方案

**核心思路**:Alt+Space 已有 tap/hold 状态机(LL hook 层)。tap → 主窗口(现有);**hold → 语音录音**(新增)。

| 操作 | 触发 | 效果 | 目标 |
|---|---|---|---|
| **Alt+Space tap**(短按) | keyup 时长 ≤ 阈值(~300ms) | 主窗口出现(现有,不变) | — |
| **Alt+Space hold**(长按,无先验 tap) | 按住超阈值时触发 Hold | 开始录音 → 边说边转 → 松开停止 → 注入前台应用 | **G2** |
| **Alt+Space tap → 再 hold** | 先 tap(出窗)→ 再按住 Alt+Space | 开始录音 → 边说边转 → 松开停止 → 填 #query | **G1** |

**G1/G2 区分逻辑**:主窗口是否可见。
- hold 时主窗口**不可见** → G2(文字注入前台应用)
- hold 时主窗口**可见**(先 tap 出窗了)→ G1(文字填 #query)

### 3.2 技术实现:Hold 事件

当前 LL hook 状态机的 tap/hold 判定:keyup 时若 `elapsed > threshold` → 当前是空操作(hold 不触发任何事件)。

**改动**:在 keydown 后起一个 `SetTimer`,超阈值时发 `HotkeyEvent::Hold`。keyup 时:
- 若 timer 已 fire(Hold 已发) → 发 `HotkeyEvent::HoldRelease`
- 若 timer 未 fire(tap) → 取消 timer,发 `Tap`(现有逻辑不变)

```
HotkeyEvent 新增变体:
  Tap(Instant)         — 短按,现有
  Hold(Instant)        — 长按开始(开始录音)
  HoldRelease(Instant) — 长按结束(停止录音→STT→注入)
```

**关键保证**:tap 路径(keyup ≤ threshold)的 `send_event(Tap)` 在 keyup 时立即触发,不受 timer 影响。timer 仅在超阈值后才 fire。

### 3.3 与现有 Chord 模式的兼容

现有 Chord:tap 后持续按住 Alt → `chord-visible` → 按 A/C/Q。Voice hold 用的是 Space(hotkey 主键),不进 chord 字母表,**零冲突**。

### 3.4 录音 UX 细节

- **按住即录**:Hold 事件 → 开始麦克风采集 + STT 流式识别
- **边说边转**:STT partial 结果实时上屏(G1: 更新 #query;G2: 更新 mini overlay)
- **松开即停**:HoldRelease → 停止采集 → STT 最终结果 → 注入
- **取消录音**:Hold 期间按 ESC → 取消,不识别不注入
- **G2 mini overlay**:hold 期间显示一个小型悬浮窗,展示流式 partial 文字 + 录音波形/计时

---

## 四、STT 选型(FunASR Python 工具箱)

### 4.1 接入形态

- **云端 STT**:走 OpenAI 兼容 API（`/v1/audio/transcriptions`），已内置 7 家供应商。
- **本地 STT**:使用 **FunASR Python 工具箱** 的 `funasr-server`（OpenAI 兼容 API）。
  - Blink 通过 **uv** 自管理 Python 3.12 + funasr 安装（用户零手动操作）
  - `funasr-server --model sensevoice --port 8000 --device cpu` 启动
  - Rust 侧通过 HTTP POST `localhost:8000/v1/audio/transcriptions` 转录
  - **复用 CloudSttEngine 的 HTTP 逻辑**，零额外推理代码
- **流式场景**:hold-to-talk 下用户松开才触发最终识别。0.10 先做非流式(hold → 松开 → 一次性识别),真流式(边说边出字)作为增强。

### 4.1.1 Python 环境自管理（uv）

> **设计方针**：Blink 对依赖的 Python 环境采用「自管理」策略——通过 uv 创建独立 venv，用户无需手动安装任何 Python 包。

**为什么用 uv**：
1. **零 Python 依赖**：uv 是单个 Rust 二进制，能自动下载和管理 Python 版本
2. **版本锁定**：避免用户系统 Python 版本过高（如 3.14）导致 C 扩展（editdistance 等）编译失败
3. **隔离环境**：Blink 的 Python 环境与用户系统完全隔离，互不污染
4. **速度快**：uv 用 Rust 写的，比 pip 快 10-100 倍

**目录结构**：
```
%APPDATA%\blink\python\
  uv\uv.exe              — uv 二进制（本地安装，不污染 PATH）
  venv\                  — Python 虚拟环境（uv 创建，Python 3.12）
    Scripts\python.exe   — venv 中的 Python
```

**工作流**（`src/infra/platform/python/mod.rs`）：
1. `ensure_uv()` — 查找 PATH 中的 uv → 本地下载安装（GitHub releases ~15MB）
2. `create_venv()` — `uv venv --python 3.12`（uv 自动下载 Python standalone build）
3. `install_packages()` — `uv pip install funasr`（10 倍于 pip 的速度）
4. `venv_python()` — 获取 venv Python 路径，用于启动 funasr-server

**用户侧体验**：设置页「语音输入」→「本地模式」→ 点击「安装环境」按钮 → 自动完成。无需命令行操作。

**作为 Blink 调用 Python 项目的方针**：后续如果有其他 Python 依赖（如 0.11 本地模型/RAG），同样走 uv 自管理路线——在 `%APPDATA%\blink\python\venv\` 中安装，用户零手动操作。

### 4.2 三层架构(工具箱 × 服务 × 模型)

> | 层 | 职责 | Blink 的定稿 |
> |---|---|---|
> | **① 工具箱** | 模型生态 + 推理 + VAD + 标点 | **FunASR**(modelscope / 阿里达摩院) |
> | **② 服务** | OpenAI 兼容 API | **funasr-server**(Python, localhost:8000) |
> | **③ 模型** | 具体网络 | 按场景挑(SenseVoice 起步 / Paraformer 真流式) |

> **架构变更说明**：旧方案使用 sherpa-onnx（C++ ONNX 引擎 + 第三方 ONNX 模型转换），
> 因下载不稳定、版本不匹配、无 VAD/pipeline 等问题已废弃。
> 新方案直接使用 FunASR 原生 Python 工具箱，通过 `funasr-server` 提供
> OpenAI 兼容 API，Rust 侧复用现有 CloudSttEngine HTTP 代码。

#### 模型层(FunASR 原生, CPU 可跑)

> **参数量 ≠ 文件大小**：FunASR 官方文档中的「220M」「234M」是模型**参数量**（220 million / 234 million），
> 不是文件下载体积。FP32 PyTorch 格式下，参数量 × 4 bytes ≈ 文件大小
> （如 220M 参数 → ~880MB 文件）。funasr-server 启动时还会自动下载 VAD + 标点模型。

| 模型 | 中文 | 流式 | 参数量 | 下载体积 | 额外能力 | funasr-server 参数 |
|---|---|---|---|---|---|---|
| **SenseVoice-Small** ⭐推荐起点 | ⭐⭐⭐⭐ 强 | ❌ 非流式但极快(CPU 17x 实时) | 234M | ~234MB | 语种识别 + 情绪 + 声学事件 | `--model sensevoice` |
| **Paraformer-zh** | ⭐⭐⭐⭐⭐ SOTA | ❌ 非流式 | 220M | ~880MB | 字符级时间戳 + 热词 | `--model paraformer` |
| Paraformer-zh-streaming | ⭐⭐⭐⭐ | ✅ streaming(zh-en 双语) | 220M | ~880MB | 字符级时间戳 + 热词 | `--model paraformer-zh-streaming` |

> **0.10.2 当前阶段**：funasr-server HTTP 模式为非流式（POST 音频 → 返回文本），
> 无论选哪个模型都是一次性识别。真流式（边说边出字）需要 0.10.3 切换到
> `AutoModel` Python API + WebSocket，届时使用 `paraformer-zh-streaming`。

**模型管理**:FunASR 在首次启动 `funasr-server` 时自动从 ModelScope（国内 CDN）下载模型，Rust 侧零管理。

**云端**(零体积,走 OpenAI 兼容 API):OpenAI / Groq / Azure / Gemini / HuggingFace / Mistral / OpenRouter。

### 4.3 &lt; 100MB 安装包硬约束

- **模型 weights**:FunASR 自动管理,首次启动 `funasr-server` 时从 ModelScope 下载。Blink 安装包不含模型。
- **Python 依赖**:Blink 通过 uv 自管理（首次使用时自动下载 uv + Python 3.12 + funasr），安装包不含 Python。用户零手动安装。

### 4.4 VAD + 降噪

hold-to-talk 下用户手动控制录音起止,VAD 的「说完判定」职责被消解。VAD 降级为 0.10.2 优化项(静默裁剪 + 可选降噪)。0.10.0 云端 STT 可依赖供应商侧 VAD。

### 4.5 选型建议 ——「云端先行验证 + 产品默认本地」双轨

1. **0.10.0 云端优先验证闭环**(工程顺序)——快、中文好、零体积
2. **产品默认 = 本地 STT,默认模型 FunASR SenseVoice-Small**(经 funasr-server 跑)
3. **真流式场景**(0.10.3+)切 `paraformer-zh-streaming` + AutoModel Python API
4. **工具箱固定 FunASR**

---

## 五、文本注入通道(G2 的硬骨头,从零搭)

### 5.1 TSF / IME 调研结论

> **用户问题**:IME 形式是否有个打字上屏的缓冲区?其他 AI 语音输入法能边转文字边优化最后一起上屏,怎么实现的?

**Windows 文本注入的三条技术路径**:

| 路径 | 机制 | composition buffer | 适用场景 |
|---|---|---|---|
| **TSF (Text Services Framework)** | `ITfThreadMgr` → `ITfContext` → `ITfComposition`。创建 composition 后可多次更新 text range(流式 partial),commit 时文本"固化"到应用 | ✅ **有**:composition range 就是应用的文本缓冲区,可多次更新 | IME 输入法、高级文本注入 |
| **Clipboard + Ctrl+V** | 暂存剪贴板 → `SendInput(Ctrl+V)` → 恢复剪贴板 | ❌ 无:一次性注入完整文本 | 简单可靠,兼容性最好 |
| **SendInput 逐键** | 逐字符模拟 `WM_KEYDOWN/UP` | ❌ 无:逐字符,中文需走 IME 注入 | ASCII 文本,不碰剪贴板 |

**TSF Composition 的原理**(即 IME 的"打字缓冲区"):
1. 通过 `ITfThreadMgr::GetFocus` 获取前台应用的 `ITfContext`
2. 调 `ITfContextComposition::StartComposition` 创建 composition
3. 在 composition 的 text range 上多次 `SetText` —— 应用会实时显示"正在输入"的文本(就像拼音输入法显示候选词)
4. 调 `ITfComposition::EndComposition` —— 文本"提交",变为应用中的最终文本

**这就是其他 AI 语音输入法的实现原理**:边说边转文字 → `SetText` 更新 composition(用户看到文字逐渐出现)→ 松开时 commit composition(最终文本固化)。还可以在 commit 前做一轮文本优化(标点/分段/纠错),用户只看到最终结果。

**但 TSF 的工程复杂度**:
- TSF 是 COM 接口,需在目标应用的 UI 线程上操作(或通过 `ITfThreadMgrEx` 跨线程)
- 部分应用(游戏、控制台、UAC 窗口)不支持 TSF
- Rust 生态无成熟 TSF 绑定,需手写 COM interop

**Blink 的务实方案——分两阶段**:

| 阶段 | 方案 | 流式展示 | 最终注入 | 复杂度 |
|---|---|---|---|---|
| **0.10.1(先做)** | Mini overlay + Clipboard+Ctrl+V | mini overlay 显示 partial | 松开后 clipboard+Ctrl+V 注入完整文本 | 低 |
| **0.10.2+(后做)** | TSF Composition | composition range 实时更新(真·原地流式) | commit composition | 高(COM interop) |

> **0.10.1 的 mini overlay 就是"可视化 composition buffer"**:hold 期间在小悬浮窗中显示流式 partial 文字,松开时注入最终文本。用户感知与 TSF composition 一致,但实现简单 10 倍。

### 5.2 0.10.1 实现方案:Clipboard + Ctrl+V

**注入时序**:
```
HoldRelease → STT 最终文本
  → 备份当前剪贴板内容
  → 设置剪贴板为 STT 文本
  → SendInput(Ctrl down → V down → V up → Ctrl up)
  → 等待 50ms 让应用处理粘贴
  → 恢复原剪贴板内容
```

**平台抽象**:`src/infra/platform/inject/mod.rs` + `windows.rs`
```rust
pub fn inject_text(text: &str) -> Result<()>;  // Clipboard + Ctrl+V
// 未来: pub fn inject_text_composition(text: &str) -> Result<()>;  // TSF
```

### 5.3 看门狗失焦检测的影响

G2 时主窗口不显示(无 tap),blink 进程不在前台。注入时序为:hold 录音 → release → STT → `inject_text()`。注入时主窗仍隐藏,看门狗不会误触发。但 `SendInput`/`Ctrl+V` 可能改变前台焦点,需验证注入后前台应用 PID 不变。

---

## 六、STT 配置与模型下载

### 6.1 配置分片设计

新增 `SttConfig` 配置分片,`ConfigKey::KEY = "stt:config"`,与 AIConfig 同级(独立于 AppConfig 门面)。

```rust
pub struct SttConfig {
    /// STT 总开关
    pub enabled: bool,
    /// STT 模式:Cloud / Local
    pub mode: SttMode,
    // ── 云端 ──
    /// 云端供应商(kind + base_url + model_id,复用 AIConfig 的 Provider 结构)
    pub cloud_provider: Option<SttCloudProvider>,
    // ── 本地 ──
    /// 本地引擎配置(sherpa-onnx)
    pub local_engine: LocalEngineConfig,
    /// 当前选用的本地模型 id(如 "sensevoice-small")
    pub local_model_id: Option<String>,
    /// 模型存储路径(默认 %APPDATA%\blink\models\stt\)
    #[serde(default)]
    pub model_dir: Option<String>,
    /// 音频输入设备 ID(None = 系统默认设备)
    /// 使用 cpal 设备名称作为 ID(WASAPI 枚举)
    #[serde(default)]
    pub audio_device_id: Option<String>,
    /// 流式识别开关(hold-to-talk 下先 false,0.10.2+ 开)
    pub streaming: bool,
}

pub enum SttMode {
    Cloud,
    Local,
}

pub struct SttCloudProvider {
    pub kind: String,        // "openai" / "groq" / "azure" 等
    pub base_url: Option<String>,
    pub model_id: String,    // "whisper-large-v3" 等
}

pub struct LocalEngineConfig {
    /// onnxruntime 动态库路径(None = 默认搜索)
    pub onnxruntime_path: Option<String>,
    /// CPU 线程数(None = 自动)
    pub num_threads: Option<u32>,
}
```

### 6.2 模型注册表与下载

**模型注册表**(硬编码在代码中,不进配置——它是"可选什么",配置是"选了什么"):

```rust
pub struct ModelDescriptor {
    pub id: &'static str,           // "sensevoice-small"
    pub display_name: &'static str, // "FunASR SenseVoice-Small"
    pub engine: &'static str,       // "sherpa-onnx"
    pub model_type: &'static str,   // "sense_voice" / "paraformer" / "whisper"
    pub streaming: bool,            // 是否支持流式
    pub size_mb: u32,               // ~230
    pub download_urls: &'static [&'static str],  // ModelScope + mirror
    pub languages: &'static [&'str],            // ["zh", "en"]
}
```

**预置模型清单**:
| id | display_name | streaming | params | size |
|---|---|---|---|---|
| `sensevoice-small` | FunASR SenseVoice-Small | ❌ | 234M | ~234MB |
| `paraformer` | FunASR Paraformer-zh | ❌ | 220M | ~880MB |

**下载管理**:
- 下载到 `%APPDATA%\blink\models\stt\{model_id}\`
- 下载进度通过 Tauri event `blink://stt-model-progress` 实时通知前端
- 支持取消、断点续传(简单版:失败重试)
- 下载完成后校验文件完整性(文件大小 / hash)

**Tauri commands**:
```rust
list_stt_models() -> Vec<ModelDescriptor>     // 列出可用模型 + 下载状态
download_stt_model(model_id: String)          // 开始下载
delete_stt_model(model_id: String)            // 删除已下载模型
get_stt_config() -> SttConfig                 // 读取配置
set_stt_config(config: SttConfig)             // 保存配置
test_stt()                                    // 测试 STT(录 3s → 识别 → 返回文字)
```

### 6.3 设置页 UI

设置页新增「语音输入」Tab:
- 总开关
- 模式切换(Cloud / Local)radio
- **云端模式**:供应商选择(OpenAI/Groq/Azure 等) + model_id + base_url(可选) + API Key(复用 secret 管理)
- **本地模式**:
  - 模型列表(显示名称 + 大小 + 状态:未下载/已下载/下载中)
  - 下载/删除按钮 + 进度条
  - 当前选用模型 radio
- 测试按钮(录 3s → 识别 → 显示结果)
- 隐私提示(云端模式时显示)

---

## 七、0.10 里程碑

| 版本 | 内容 | 复杂度 | 状态 |
|---|---|---|---|
| **0.10.0** | Hold 事件 + 麦克风采集 + STT trait + Mock engine + G1 主窗口语音输入(云端) + STT 配置 + 设置页 | 中 | ✅ 已完成 |
| **0.10.1** | G2 文本注入通道(Clipboard+Ctrl+V) + mini overlay + 语音输入法上屏 | 中→大 | ✅ 已完成 |
| **0.10.2** | 本地 STT(FunASR Python 工具箱) + uv 自管理 Python 环境 + 统一日志 + 空间管理 | 大 | ✅ 已完成 |
| **0.10.2.1** | CPU/CUDA 设备切换 + 流式识别默认开 + 按钮样式优化 | 小 | ✅ 已完成 |
| **0.10.3**(可选) | 真流式(Paraformer-streaming 边说边出字) + TSF Composition 注入 | 中→大 | 🔲 未开始 |

> Agent 对话窗口**移至 0.11**(与本地化生态一起)。0.10 聚焦语音输入闭环。

### 前置门 vs 调研项

- **前置门**:
  - ✅ Hold 事件 hook 线程 timer(§3.2)
  - ✅ 调研 #1(FunASR 集成)→ funasr-server 子进程方案落地
- **已完成**:
  - ✅ 调研 #2:rig TranscriptionModel 内置清单(7 家)
  - ✅ TSF 调研:结论见 §5.1(0.10.1 用 Clipboard+Ctrl+V,0.10.2+ 探索 TSF)
  - ✅ cpal 替换 waveIn:WASAPI 后端解决麦克风隐私静默拦截问题
  - ✅ CPU/CUDA 设备切换:设置页可切换推理设备,CUDA 版 PyTorch 自动安装
  - ✅ 空间管理:Python 环境/模型缓存空间占用展示 + 一键清理
  - ✅ 统一日志窗口:FunASR 运行日志 + 安装进度 + 诊断输出共用一个窗口
- ✅ 默认设备透明化:设备列表标注 cpal 认定的默认设备,下拉框「系统默认」选项旁显示实际设备名

### FunASR 服务管理

> **用户问题**:funasr 是不是也得有管理服务？还是说启动一次后就不用管了？

**结论**:FunASR server 是一个长期运行的本地 HTTP 服务。启动后常驻后台,每次语音识别通过 HTTP POST 请求调用,无需反复重启。用户可在设置页手动启动/停止。

**当前行为**:
- 点击「启动服务」→ 启动子进程,加载模型到内存 → HTTP API 就绪
- 后续语音识别直接调用 `localhost:8000/v1/audio/transcriptions`
- 点击「停止服务」→ kill 子进程,释放内存
- **Blink 退出时不会自动停止 funasr-server**(已知限制,见下方)

**已知限制**（0.10 可接受,后续优化）:
1. **Blink 退出后服务可能成为孤儿进程**:Blink 没有注册 shutdown handler 来 kill 子进程。下次启动时 `is_server_ready()` 会检测到服务已在运行,直接复用,但 `stop_funasr_server` 命令会因 child handle 为 None 而无法停止。用户可手动在任务管理器中结束 `funasr-server.exe` 进程。
2. **start_funasr_server 自动安装环境不发射详细进度**:`start_funasr_server` 中调用的 `python::setup()` 不发射 progress 事件,用户需先点「安装环境」按钮获取详细进度。

---

## 八、P0 不回归保证

| 新增物 | 对 P0 的威胁 | 保证措施 | 状态 |
|---|---|---|---|
| **FunASR Python 子进程** | 启动加载若进热路径 | **lazy-link**:进程启动不加载 STT | ✅ 子进程方案,启动零加载 |
| **~230MB 模型 weights** | 撑爆 <300MB 常驻预算 | **lazy-load + 用完可卸载** | ✅ 按需下载,默认不捆绑 |
| **麦克风采集线程** | 阻塞 LL hook | 独立 std::thread,**绝不进 hook 线程** | ✅ cpal 采集在独立线程 |
| **cpal 依赖** | 编译/体积负担 | cpal 零系统依赖,Windows 上走 WASAPI | ✅ 已验证编译通过 |
| **Hold 事件(hook 线程 timer)** | timer 阻塞 LL hook | timer 仅发事件,不执行录音逻辑 | ✅ |
| **G2 文本注入** | 触发失焦隐藏 | G2 时主窗不显示,注入不抢前台 | ✅ |

**验收红线**:0.10 任何改动不得使「Alt+Space tap → 窗口出现 → 自动 Focus → 输入」这条主链路的延迟/成功率劣化。

**特别注意**:tap 路径的 `send_event(Tap)` 必须在 keyup 时立即触发,不受 hold timer 影响。

---

## 九、语音隐私升级

- **默认本地 STT**(若已下载模型);云端 STT 需用户显式开启
- **第一次启用语音必须有强告知门**:发给谁、发什么(录音还是文本)、存不存、能关什么
- 录音不持久化、不入 SQLite;成功只记时长
- 敏感应用黑名单生效时跳过(银行、密码管理器)
- 详见 [product-context-future.md §8](./../product-context-future.md#8-隐私与安全)

---

## 十、架构设计

### 10.1 新增模块

```
src/domain/stt/
  mod.rs          — SttEngine trait + SttError + 模型注册表 + create_engine()
  mock.rs         — MockSttEngine(开发用,返回假文本)
  cloud.rs        — CloudSttEngine(OpenAI 兼容 API,走 reqwest)
  local.rs        — LocalSttEngine(FunASR server HTTP 转录)
  funasr.rs       — FunASR 环境状态 + server 生命周期（使用 python 模块管理 venv）

src/infra/platform/
  audio/          — 麦克风采集
    mod.rs        — AudioCapture trait + AudioChunk + AudioFormat + AudioDevice
    windows.rs    — cpal / WASAPI 实现(✅ 已替换 waveIn)
  inject/         — 文本注入
    mod.rs        — inject_text() 接口
    windows.rs    — Clipboard + Ctrl+V 实现
  python/         — Python 环境自管理（uv）
    mod.rs        — UvEnvManager: uv 下载/检测 + venv 创建 + 包安装 + 状态检查

src/app/
  stt_config.rs   — SttConfig + SttMode + ConfigKey impl + 内存缓存
  voice.rs        — VoiceService(hold→录音→STT→注入/fill-query 管线编排)
```

### 10.2 STT Engine trait

```rust
#[async_trait]
pub trait SttEngine: Send + Sync {
    /// 流式识别:接收音频 chunk,返回 partial text
    /// hold 期间持续调用,每次返回当前累积识别结果
    async fn transcribe_chunk(&self, audio: &[f32]) -> Result<String, SttError>;

    /// 最终识别:录音结束后调用,返回最终文本
    async fn finalize(&self) -> Result<String, SttError>;

    /// 重置状态(新录音会话)
    fn reset(&self);
}

pub enum SttChunk {
    Partial(String),   // 流式中间结果
    Final(String),     // 最终结果
}
```

### 10.3 VoiceService 管线

```
Hold 事件 → VoiceService::start_recording()
  → 启动 AudioCapture(独立 tokio task)
  → 循环: audio_chunk → SttEngine::transcribe_chunk → emit partial

HoldRelease 事件 → VoiceService::stop_recording()
  → 停止 AudioCapture
  → SttEngine::finalize() → 最终文本
  → G1: emit "blink://chord-fill-query"(文本)
    G2: inject_text(文本) + 隐藏 mini overlay
```

### 10.4 Hold 事件流转

```
LL hook (hook 线程)
  ├── keydown → armed + SetTimer(threshold)
  ├── timer fire → send_event(Hold)     ← 开始录音
  └── keyup →
       ├── timer 已 fire → KillTimer + send_event(HoldRelease)  ← 停止录音
       └── timer 未 fire → KillTimer + send_event(Tap)           ← 现有 tap

main.rs hotkey rx (主线程)
  ├── Hold → spawn VoiceService::start_recording()
  └── HoldRelease → VoiceService::stop_recording()
```

---

## 十一、验收标准

```bash
# 0.10.0
# Hold 事件
 Alt+Space hold(无先验 tap) → 触发 Hold(不触发 Tap → 主窗不出现)
 Alt+Space tap → 触发 Tap(主窗出现,现有行为不变)
 tap 路径延迟 ≤50ms 不变

# G1 主窗口语音输入(Mock STT)
 Alt+Space tap → 主窗出现 → Alt+Space hold → 录音 → 松开 → STT 转文字
 文字填进 #query → 自动触发搜索
 第一次启用语音 → 强告知门弹出
 P0 主链路零回归

# STT 配置
 设置页「语音输入」Tab 可见
 云端/本地模式可切换
 模型列表显示 + 下载(0.10.0 可 mock 下载进度)

# 0.10.1(G2)
 前台应用 → Alt+Space hold → mini overlay 显示流式文字 → 松开 → 文字注入前台
 注入不触发 blink 失焦隐藏

# 0.10.2(本地 STT)
 下载 SenseVoice-Small → 离线语音转文字
 lazy-link / lazy-load 生效

# 0.10.3(可选,真流式 + TSF)
 边说边出字(Paraformer-streaming)
 TSF composition 原地流式展示
```
