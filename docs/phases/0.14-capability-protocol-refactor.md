# Blink 0.14 — 能力协议与架构收敛（Capability/Action 边界 + Cap 协议 + 分层清理）

> **范围**：① Capability 与 Action 的职责边界从"口头约定"升级为"类型系统钉死"（删 `ActionTool` 适配器，AI 永不直接调 Action）+ ② Cap 协议分层——插件只吐纯 `data`，投影规则（pointer / desc / actions）上移到 manifest 做代理 + ③ 四出口投影引擎收敛（主窗口 AI / 对话窗口 AI / CLI / MCP 共用一套 canonical 投影）+ ④ 0.14.6 延续清理——在不改变能力语义的前提下，收敛分层违例、重复协议和前后端巨石文件。
>
> **状态**：🚧 进行中（0.14.0~0.14.6 ✅；0.14.7 📋）。 
>
> **动机**：0.13.7 收尾时发现三个协议债——(1) `ItemResult { title, subtitle, payload }` 把"给人看的 UI 文案"和"给 AI 看的语义数据"焊在一起，导致对话窗口 AI 收到带 emoji / "按 Enter 复制"的噪音；(2) 能力结果 4 个出口（AI/CLI/MCP/前端）只有 2 个复用 canonical 投影，CLI/MCP 各自手写、Blob 摘要重复 4 份、MCP 还漏去 score；(3) `ActionTool` 把 lock/shutdown 等危险系统操作也暴露给了 AI tool 池。协议重构完成后的整体审查又确认：架构骨架健康，但领域层仍夹有框架/平台依赖，Schema、错误、事件和前端文件也存在工程债，因此在 0.14.6 做延续收敛。
>
> **前置**：0.13 完成（能力体系已收敛——`ActionOutcome::Items` 变体已删，插件 Action 已迁入 Capability）。
>
> **原则**：**插件只吐纯 data，投影是配置出来的不是代码写死的**。manifest 投影只支持静态值 + JSONPath 取值，不做格式化（格式化由能力单元自己管）。

---

## 一、背景与动机

0.13.7 收尾审查发现三个互相牵连的债，趁架构向"Action=副作用专用、Capability=数据专用"收敛的窗口一次性清掉。三个债：(1) `ItemResult` 字段语义混淆（UI 文案污染 AI）；(2) 四出口投影分裂（CLI/MCP 手写、重复、漂移）；(3) `ActionTool` 过度暴露（危险系统操作进 AI tool 池）。详见 §二/三/五的核心决策。

---

## 二、Capability 与 Action 的职责边界（核心决策）

**Action**（0.8 四域，Execution 域）：已发生的副作用描述（Copy/Open/Emit/Nop），**永远不进 AI tool 池**——lock/shutdown/open_settings/chord 动作等。
**Capability**（0.9.7）：纯数据能力（取数/计算/翻译/打开），**进 AI tool 池的唯一合法形态**——search_files/translate/capture_screen/open_url 等。

**边界用类型系统钉死**：删除 `ActionTool` 适配器，AI 永远只能通过 `CapabilityTool` 调能力。把"AI 该不该调"的决策从运行时 danger_class 标记前置成编译期约束。

**3 个 Action 提升为 Capability**：`open_url` / `open_path` / `reveal_in_explorer`（AI 常用的"打开"能力，作为 Capability 返回 `Done`）。其余 9 个 Action（lock/shutdown/...）保留在 Action trait，不进 tool 池。

**不合并 Capability 与 Action**：曾考虑全合成 Capability，否决——四域架构里 Action 承载"已执行副作用"语义，且 9 个保留 Action 本就不该让 AI 调（详见 §十一 ADR）。

---

## 三、Cap 协议分层——插件只吐纯 data

**两层解耦**：层 1 = manifest 投影规则（pointer/desc/actions，相对静态）；层 2 = 插件运行时输出（纯 data JSON）。投影引擎用 manifest 规则把 data 投影成各出口形态。精髓：展示和投影是配置出来的，不是代码写死的。

**三个关键设计点**：
- **JSONPath 取值**用 `jsonpath-rust` crate（纯 Rust ~30KB，零 C 绑定）。选型理由见 §十一 ADR。
- **desc 三来源优先级**：`desc_pointer`（JSONPath 动态取值）> `desc`（静态字符串）> None（不展示）。manifest **不做格式化**——需要 `format_size(bytes)` 时由能力单元自己在 data 算好填进去。
- **双轨制**：轨道 A（manifest 投影，简单返回如 translate/IP/weather）+ 轨道 B（直接构造 CapabilityResult，复杂返回如 builtin search_files）。

> **轨道 B 实现澄清**（0.14.5）：插件协议侧最终只保留轨道 A（插件经 JSONL 返回纯 data，core 用 ProjectionRule 投影）。轨道 B 退化为"仅 builtin capability 可直接构造 CapabilityResult"——builtin 本就不走 JSONL 协议。早期 commit `df85f912` 曾遗留旧 `PluginItem` 协议路径冒充轨道 B（0.14.5 已删）。

---

## 四、新 Cap 协议结构

**插件运行时输出**（轨道 A）：`PluginRawResult { data: Value, error: Option<String> }`——只吐纯 data，零展示逻辑。

**manifest 投影规则**（ProjectionRule）：`result_shape`（text/items/blob/done）+ `pointer`/`desc`/`desc_pointer`（text/blob 用）+ `items_pointer`/`item_pointer`/`item_desc_pointer`/`item_actions`（items 用）。字段全为 JSONPath 或静态值。

**CapabilityResult**（四出口共用）：四变体 `Text{content,desc}` / `Items{items}` / `Blob{mime,bytes,desc}` / `Done{summary}`。`Text`/`Blob` 加了可选 `desc`。

**ItemResult 关键变化**（对比旧 `{title,subtitle,payload,score}`）：
- 删 `title`/`subtitle`/`score`（主窗口展示概念，不该在协议层）；主标题由前端从 `data` 派生（`derive_title()`，优先取 `name`/`title` 字段）
- `data` 取代 `payload`（语义明确："给 AI 的纯净数据"）；`desc` 取代 `subtitle`
- `actions: Vec<ItemAction>` 显式声明可用动作（Copy/OpenFile/OpenUrl/Reveal，各带可选 `pointer` 指定从 data 取值）

**多 action 交互约定**（主窗口）：回车执行 `actions[0]`（首个），右键展开全部。manifest 里 actions 顺序即优先级，作者把最常用动作放第一个。

**空 actions 兜底**：无 actions + 纯标量 data → 主窗口隐式提供 copy。这是**展示层 fallback，非协议层派生**——协议层如实传空数组，兜底只活在主窗口前端。

---

## 五、投影引擎（四出口共用）

四个出口（AI / 主窗口前端 / CLI / MCP）共用一套 canonical 投影，不再各自手写 match 四变体。`project()` 用 ProjectionRule 把 PluginRawResult 投影成 CapabilityResult。各出口读什么：AI 只读 `data`；主窗口前端读 `data`（派生主标题）+ `desc` + `actions`；CLI 读 `data`+`desc`；MCP 同 AI。

**消除的重复**：Blob 摘要收进 `CapabilityResult::blob_summary()`（旧 4 处逐字重复删除）；CLI/MCP 改调 canonical；MCP Items 投影的 score 漂移修复（改用 `items_to_llm_json` 去 score）；对话历史加载复用 `summarize_tool_result`。

---

## 六、插件协议迁移

**一次性切换**（破坏性，不留兼容层）：translate/IP/weather 全部从旧 `{title,subtitle,payload,action,score}` 迁到轨道 A `{data}`，manifest 配 ProjectionRule。插件运行时输出反而变简单（翻译插件从 30 行构造 PluginItem 缩成一行 `data: result`）。理由见 §十一 ADR。

**0.14.5 收尾清理**：commit `df85f912` 落地时旧 tool-call 协议链路未按承诺删除（`PluginUpstreamMessage::ToolResult` / `ToolResultPayload` / `PendingToolMap` / `execute_tool` / `plugin_item_to_item_result` 整套休眠），0.14.5 已全部删除（query 路径的 `PluginResponse`/`PluginItem` 保留）。详见 git log。

---

## 七、架构审查结论与保留边界（0.14.6）

源码注释中的历史实施批次标记统一归档到本节及 §八/§九，原编号映射如下：

| 原标记 | 合并后的主题 |
|---|---|
| `0.14.6 §2.1` | §八：配置域下沉 |
| `0.14.6 §2.2` | §八：domain 去 Tauri、`DomainEnv` / `CapabilityEnv` 类型墙 |
| `0.14.6 §2.3` | §八：Win32 平台实现归入 infra |
| `0.14.6 §2.4` | §八：Tauri commands 按域拆分 |
| `0.14.6 §3.1~§3.4` | §八：Schema、错误、事件名与 invoke 路径收敛 |
| `0.14.6 §4` | §九：前端巨石文件拆分 |

整体审查没有发现需要推倒重来的架构问题。以下设计是刻意的，不作为"重复"或"坏味道"合并：

| 保留项 | 原因 |
|---|---|
| `Capability` / `Action` 双 trait、双 Registry | 两者信任边界与返回语义不同；AI 只经 Capability，主窗口/chord 的本地副作用仍走 Action |
| ProviderConfig / AgentConfig 分离 | Provider 描述连接与模型能力，Agent 描述运行时角色，职责不同 |
| `SearchEngine` 与 `SearchService` 分层 | 前者是搜索算法，后者是编排、历史加权和能力结果投影 |
| `ChordAction: Action` | chord 是本地执行入口，复用 Execution 域的副作用语义 |

---

## 八、后端分层与工程契约收敛

### 8.1 分层收敛

- 配置类型与 `ConfigStore` 下沉至 `src/domain/config/`；`src/app/{config,ai_config,stt_config}.rs` 只保留兼容 re-export，依赖方向不再由 domain/infra 反向指向 app。
- 以对象安全的 `DomainEnv` 隔离 `AppHandle`、Emitter 和 managed state，`TauriDomainEnv` 留在 app 层实现；Capability 调用上下文进一步只暴露 `CapabilityEnv`，不能取得窗口、事件、退出或 Action 执行权限，延续了 Capability-only 的类型墙。
- 图标提取、Shell 枚举、锁屏等 Win32 实现归入 `src/infra/platform/`；domain 只消费平台接口或封装结果。
- 原 `src/app/commands.rs` 按 config/search/ai/plugin/mcp/stt/clipboard/diagnostic 等域拆为 `src/app/commands/` 子模块，并继续把 AI 对话/管理、截图 OCR/右键菜单、FunASR 维护拆成职责子模块；所有 command 文件均低于 1000 行，IPC 公开路径保持不变。
- domain 生产路径中的异步任务统一改用 `tokio::spawn`；Tauri runtime 只剩需要异步初始化设计的 `ChatService` 构造期 `block_on`。

### 8.2 工程契约收敛

- `ToolSchema` 成为 Action/Capability schema 的三字段公共基；`CapabilitySchema` 只扩展 `sensitive`，rig 投影触点收敛到一处。
- 核心错误类型改用 `thiserror` 并补必要的类型转换；跨层错误仍需保留分类信息，禁止无差别拍平成字符串。
- Rust 与前端各有集中事件名清单；前端 invoke 统一经 `frontend/js/tauri.js`，禁止业务模块直接访问 `window.__TAURI__`。

---

## 九、前端拆分与回归护栏

### 9.1 文件拆分

前端三个巨石按职责拆分：截图 overlay 拆为 `ss-*` 模块，AI 设置拆为 `settings/tabs/ai/*`，对话样式拆为 `css/views/chat/*`；入口文件只负责装配和初始化。

### 9.2 拆分护栏

结构拆分必须保持外部契约不变，尤其要逐项核对：

1. Tauri `generate_handler!` 中的 command 与前端所有 `invoke()` 字面量；
2. Rust `blink://*` 事件常量与前端事件常量；
3. HTML/CSS/JS 入口的加载顺序、导出名和初始化时序；
4. 自定义协议与 IPC 的职责边界。截图像素继续通过 `blink-screenshot` 自定义协议读取，不能因拆分臆造 `screenshot_get_image` command。

这条护栏来自实际回归：截图模块拆分后一度把自定义协议误改成不存在的 command，静态核对后已恢复原取图链路。

### 9.3 0.14.7 工作

以下项目需要独立设计或重新做真实 UI/Windows 回归，不夹带在 0.14.6 的机械收尾中。每个工作包必须独立提交、独立验收；除下文明确列出的迁移面外，不顺手扩张到相邻模块。

#### W1：domain 彻底去 Tauri runtime

**目标**：`src/domain/` 不再依赖 Tauri runtime；生产初始化使用明确的 async 边界，测试也不借 Tauri executor。

**实施顺序**：

1. 把 `ChatService` 构造期的 `tauri::async_runtime::block_on(Self::load_selected_model(...))` 改为显式异步初始化。可采用 `async fn new`，或在 app/CLI wiring 中先 await 初始化数据再构造；无论采用哪种形态，async 边界必须收敛在 wiring，禁止在同步构造函数里嵌套 runtime。
2. 更新 GUI setup、CLI/chat 最小运行时等所有 `ChatService` 构造点，保持懒构造 Agent、不增加 Alt+Space 主链路工作量。
3. 将 `app_config`、AI provider、Builtin/Calc engine、intent router 等测试中的 `tauri::async_runtime::block_on` 改为 `#[tokio::test]` 或统一的 Tokio test helper；禁止换成散落的 `futures::executor::block_on` 来掩盖依赖。
4. `domain/ai/spike/` 改用 Tokio，或迁出正式 domain 模块；Spike 不得成为 domain 引入 Tauri 的理由。

**不做**：不重写 SearchService/ChatService 业务流程，不改变模型选择持久化语义，不借机更换 async runtime。

**验收**：`rg "tauri::" src/domain` 仅允许迁移说明注释或为 0；`cargo test --bin blink` 通过；GUI 与 CLI 均能读取已选模型；不存在 “cannot start a runtime from within a runtime” 类 panic。

#### W2：WinRT OCR 迁入 infra

**目标**：Windows SDK/WinRT 只存在于 `infra/platform/ocr/`；domain 保留 OCR 领域结果、智能文本拼接和 Capability 投影，依赖方向仍为 domain → infra。

**实施顺序**：

1. 在 `infra/platform/ocr/` 建立平台入口、Windows backend 和平台原始 DTO（raw word/line/rect/rotation）；backend 只负责 PNG 解码、WinRT 调用和 SDK 数据提取，不引用 `crate::domain::*`。
2. `domain/capability/builtins/ocr_engine.rs` 保留 `OcrResult` / `OcrLine` / `OcrWord`、`join_words_smart` 和 raw DTO → 领域结果映射；CJK/Latin 拼接不得下沉到 infra。
3. 将 backend 获取/注入边界改为平台抽象；Fake backend 测试平台调用与 domain 拼接测试分开，删除到期兼容别名 `OcrEngine` / `get_ocr_engine` 前先确认无生产调用。
4. `ocr_image` Capability 与截图 OCR command 只消费领域入口，不直接认识 WinRT 类型。

**不做**：不更换 OCR 引擎，不改变 `OcrResult` 对外 JSON 结构，不把 domain DTO 搬进 infra 后反向依赖。

**验收**：`rg "use windows::" src/domain` 为 0（排除 `src/domain/search/mod.rs` 的本地 `mod windows` re-export，那是 Shell 扫描模块不是 WinRT）；智能拼接与 Fake backend 单测通过；Windows 真机验证中英文、word rect、line rect、旋转图片和无文字图片；前端选区 OCR/全文 OCR 契约不变。

#### W3：command 结构化错误协议

**目标**：在 app/IPC 边界建立稳定、可序列化的错误 wire schema，前端能按错误类别展示，同时兼容尚未迁移的字符串错误。

**实施顺序**：

1. 在 `app` 层定义 `CommandError`，至少包含稳定 `code`、用户可读 `message`、可选 `detail` 与 `retryable`；code 使用稳定 snake_case 值，不把 Rust 类型名或 Debug 文本当协议。
2. 实现 `CapabilityError → CommandError`，并为序列化形状、每个 CapabilityError kind 的映射和敏感 detail 边界补单测。
3. 第一批只迁直接执行 Capability 的 command：`run_builtin_action` 的 Capability fallback、`confirm_ai_action`、`ocr_image`、`translate_text`、`translate_lines`。Action/ExecError 可在 `run_builtin_action` 同一 IPC 出口映射，但不得因此迁全仓 Action API。
4. 在 `frontend/js/tauri.js` 增加统一错误归一化 helper：识别结构化对象，也能把旧字符串包装成 fallback 形态；上述五个入口的 UI/日志改用 `code/message`，禁止出现 `[object Object]`。
5. 其余 `Result<_, String>` 建清单留待后续自然迁移；本工作包不批量改 config、MCP、STT、插件管理等 command。

**不做**：不一次性消灭所有 `Result<_, String>`，不改变 Tauri command 名和成功返回结构，不把前端展示文案塞回 domain error。

**验收**：wire schema round-trip 测试通过；五个首批 command 的错误均为结构化对象；前端仍能展示旧字符串错误；配置缺失、参数错误、权限/确认拒绝、内部错误可区分。

#### W4：前端显隐状态收敛

**目标**：显隐状态由 class/ARIA 表达，不再通过读取 `element.style.display` 推断状态；按窗口小批迁移，避免一次性机械替换破坏 flex/block 布局。

**批次与顺序**：

1. **A—截图窗口**：`chord-screenshot.js`、`ss-*` 的 toolbar、OCR、error hint、annotation/cursor。
2. **B—设置页 AI**：provider/model/skill modal、dropdown、toast、折叠区。
3. **C—设置页其他模块**：MCP、插件、语音、context；同步修正 ESC、点击外部关闭和 `aria-expanded`。
4. **D—chat / pin**：对话状态块、悬浮截图 OCR 面板和右键菜单。

每批先在 CSS 七层中的正确层定义 `.hidden` / `.is-open` 等状态类，再迁 JS 写入与状态读取。`style.display` 的**读取**必须清零；高频画笔 cursor 等确需逐帧直接写 style 的例外必须逐项列注释，不能用例外恢复 modal/dropdown 的 inline 状态机。

**不做**：不重做视觉设计，不改 DOM 对外 id/command/event 契约，不顺便引入前端 bundler 或框架。

**验收**：每批分别回归亮/暗主题、首次打开、重复开关、ESC、点击外部、焦点恢复和窗口重开；`rg "style\\.display\\s*[!=]==" frontend` 为 0；允许保留的直接写入有明确清单和理由。

#### W5：`DomainEnv` 初始化契约加固

1. `TauriDomainEnv::set_*` 的 `OnceLock::set` 失败不能静默丢弃；重复注入记录结构化 `warn`（含 slot 名），不记录敏感对象内容。
2. `DomainEnv::cap_registry()` 改为 `Option<&Arc<CapabilityRegistry>>`，与 `search_service()` / `chat_service()` 的最小运行时语义对齐；同步主窗口 SearchService 中全部调用点。
3. tool discovery 路径缺失 registry 时记录一次可诊断日志并降级为空 tool 池，普通 fuzzy 搜索继续工作；显式执行某个 tool 时返回可恢复的“运行时能力未初始化”错误，不 panic、不伪装成未知 tool。
4. 增加无 CapabilityRegistry 的 fake/minimal env 测试，以及重复 `set_*` 不覆盖首次注入的测试。

**验收**：环境 getter 内无 `expect`/panic；GUI/CLI 正常 wiring 不新增 warn；最小运行时缺 registry 时搜索仍可用、显式 tool 执行返回稳定错误。

#### W6：事件 helper 死代码清理

复核 `emit_to_serialized` 的真实消费方；若仍无调用则删除函数及 `#[allow(dead_code)]`。只有能写出具体近期消费方、启用条件和对应工作项时才允许保留，禁止以“以后可能用”作为 lint 豁免理由。验收以全仓引用检查、`cargo check` 和相关事件回归为准。

#### W7：根目录职责收敛

不把所有非源码粗暴合并进 `assets/` / `tools/`，按工具链边界做最小整理：

1. 保留 `capabilities/`、`gen/schemas/`、`icons/`、`xtask/`：前三者分别是 Tauri ACL 真源、Tauri 生成 Schema、安装包图标，`xtask` 是 Rust workspace 的标准构建编排入口；补充根目录说明，明确 Tauri `capabilities/` 与 `src/domain/capability/`、安装包 `icons/` 与 `frontend/assets/icons/` 不是同一概念。
2. 将 `resources/python/blink_stt_server.py` 收敛为 `resources/stt/funasr/blink_stt_server.py`，同步 `include_str!` 路径和嵌入/释放测试；`resources/` 只存会进入程序产物的运行时资源，不接纳开发脚本或生成文件。
3. 将 `scripts/fetch-lucide-icons.py` 移入 `xtask` 管理范围，并增加统一入口 `cargo xtask icons`；保留 Python 标准库实现，由 xtask 锚定 workspace 路径并调用，避免为一次性下载器引入 Rust HTTP 依赖。迁移后删除根目录 `scripts/`。
4. `.gitignore` 增加全局 Python 缓存规则（`**/__pycache__/`、`*.pyc` / `*.pyo`），清除 `resources/python/__pycache__/` 这类工作树噪声。

**验收**：`cargo xtask icons` 可复现生成 `frontend/assets/icons/{sprite.svg,manifest.json,LICENSE.lucide.txt}`；FunASR 嵌入脚本测试通过；`git status --short` 不再出现 Python 缓存；`capabilities/*.json` 的 `$schema` 引用仍有效；`cargo xtask release` 与 CI 的 `plugins → copy → tauri build` 链路不变。

#### 工作包依赖与并行边界

- W1、W2、W4、W6、W7 互相独立；W4 内部按 A → B → C → D 小批串行。
- W5 应先于 W3 收尾：缺失 CapabilityRegistry 的可恢复错误由 W3 投影成 IPC wire error；两者可并行开发，但合并/验收顺序为 W5 → W3。
- W3 只影响 W4 中相关错误展示，不阻塞 W4 的显隐状态迁移；禁止两个工作包在同一提交中混改 UI 状态与错误文案。
- 每个工作包完成后单独跑相关定向测试；0.14.7 总验收再跑 `cargo fmt -- --check`、`cargo check --bin blink`、`cargo test --bin blink` 和 Windows/UI 手工回归。

---

## 十、里程碑

### 10.1 0.14 版本里程碑

| 版本 | 内容 | 优先级 | 依赖 |
|---|---|---|---|
| **0.14.0** ✅ | Cap 协议分层（PluginRawResult + ProjectionRule + CapabilityResult 新结构 + ItemAction）| P0 | 0.13 完成 |
| **0.14.1** ✅ | 投影引擎收敛（四出口共用 canonical + 删重复 + 修 MCP score 漂移）| P0 | 0.14.0 |
| **0.14.2** ✅ | Capability/Action 边界钉死（删 ActionTool + open_url/open_path/reveal 提升为 Capability）| P0 | 0.14.0 |
| **0.14.3** ✅ | 插件协议迁移（translate/IP/weather + manifest 投影规则）| P1 | 0.14.0 + 0.14.1 |
| **0.14.4** ✅ | 主窗口前端适配（ItemResult 新结构 → AppEntry 投影 + fallback 派生 + Action 版本删除）| P1 | 0.14.0 |
| **0.14.5** ✅ | 收尾清理（删旧 tool-call 死链路 + 修过时注释）| P2 | 0.14.3 |
| **0.14.6** ✅ | 架构与工程债收敛（分层剥离 + Schema/Error/事件/invoke 收敛 + 后端/前端文件拆分） | P1 | 0.14.5 |
| **0.14.7** ✅ | 深层工程债收尾（domain runtime / WinRT OCR / IPC 结构化错误 / 前端显隐状态 / 根目录职责收敛） | P1 | 0.14.6 |

**依赖关系**：
- 0.14.1 / 0.14.2 / 0.14.3 / 0.14.4 都依赖 0.14.0 的协议新结构
- 0.14.1（投影引擎）是其他三项的基础设施
- 0.14.2（边界钉死）相对独立，可与 0.14.3/0.14.4 并行
- 0.14.5 是纯死代码删除，依赖 0.14.3 确认无插件再走旧协议
- 0.14.6 是对 0.14 完成态的整体审查与结构清理，**不重开 Capability/Action 决策，不改变 Cap 协议语义**
- 0.14.7 处理需要接口设计或平台/UI 回归的剩余工程债，仍不改变 Capability/Action 与 Cap 协议

### 10.2 已有基础设施复用盘点

| 模块 | 来源 | 0.14 复用点 |
|---|---|---|
| **CapabilityResult 四变体**（0.9.7） | Text/Items/Blob/Done | 结构保留，字段语义重定义（加 desc、删 score/title/subtitle）|
| **CapabilityRegistry**（0.9.7） | inventory 自动注册 | 不变，open_url 等新 Capability 走同样注册 |
| **CapabilityTool**（0.12.0） | ToolDyn 适配层 | 保留，成为 AI 进 tool 池的唯一入口（删 ActionTool 后）|
| **PluginCapabilityAdapter**（0.13.7） | 插件 → Capability | 改造：返回 PluginRawResult，经投影引擎转 CapabilityResult |
| **统一投影 to_rig_tool_result()**（0.12.0） | canonical 投影入口 | 保留并扩展为四出口共用 |

### 10.3 不进 0.14 的项

| 项 | 理由 |
|---|---|
| 向量召回 / RAG | 移到 0.20（原 0.14），依赖向量基础设施 |
| 第三方插件市场 / manifest 升级 | 1.0 前不做（AGENTS.md §6 不过度工程）|
| 插件协议自动向后兼容迁移工具 | 0.x 阶段插件都是自研，手动迁移即可 |

## 十一、关键决策记录（ADR）

- **11.1 插件协议迁移 —— 一次性切换**：旧 JSONL 协议直接废弃不留兼容层。理由：0.x 插件全自研，维护两套解析是纯负担。
- **11.2 ItemAction 声明 —— manifest 全显式**：不做"data 类型→默认 actions"自动派生。**职责分离**：协议层 actions 由 manifest 显式决定；展示层（主窗口）对无 actions 的标量 result 兜底隐式 copy。
- **11.3 多 action 交互 —— 回车首个/右键全部**：回车执行 `actions[0]`，右键展开全部。actions 顺序即优先级，作者把最常用动作放第一个。
- **11.4 JSONPath crate —— jsonpath-rust**：纯 Rust 零 C 绑定，符合无重依赖偏好。中文取值/数组通配/缺失降级已补单测。

---

## 十二、验收

- **0.14.0~0.14.5 协议重构**：投影引擎单测覆盖 4 shape × 3 desc 来源；AI tool 池只含 Capability（对话窗口 tool 列表无 lock/shutdown）；对话窗口调 translate/IP 时 AI 收到纯净 data（无 UI 噪音）；0.14.5 grep 验证旧 tool-call 死链路全清。落地时 `cargo test --bin blink` 为 1123 passed / 0 failed，三个插件 `cargo check` 通过。
- **0.14.6 架构清理**：`cargo fmt --check` 与 `cargo check --bin blink` 通过；拆分前 113 个 command 全部保留，前端 154 个字面量 invoke 全部命中后端注册，所有 command 文件低于 1000 行；Capability/Action 边界、Cap 投影链路、command/event 对照及前端模块链接静态检查通过。截图取图链路修复后，用户完成主链路冒泡测试，未发现功能回归。此前全量测试 1103/1108 通过，余下 4 项为 FunASR 沙箱目录写入失败、1 项为物理音频采集无数据，不属于结构拆分引入的断言失败。

---

## 十三、关联文件速查

按域聚合（同一文件跨子版本的改动合并）：

**capability 域**：
- `src/domain/capability/projection.rs` — **新建**（ProjectionRule + project() 投影引擎 + JSONPath 取值工具，单测覆盖 4 shape × 3 desc 来源）
- `src/domain/capability/result.rs` — 改造（CapabilityResult 加 desc + ItemResult 重定义为 {data,desc,actions} + ItemAction 枚举 + blob_summary() + derive_title()）
- `src/domain/capability/builtins/open_url.rs` / `open_path.rs` / `reveal_in_explorer.rs` — **新建**（3 个"打开"能力提升为 Capability，返回 Done）

**plugin 域**：
- `src/domain/plugin/protocol.rs` — 改造（加 RawResult 轨道 A；0.14.5 删旧 ToolResult/ToolResultPayload 死链路；保留 query 路径的 PluginResponse/PluginItem）
- `src/domain/plugin/process.rs` — 改造（加 PendingRawToolMap + execute_tool_raw；0.14.5 删旧 PendingToolMap + execute_tool 死链路）
- `src/domain/plugin/capability.rs` — 改造（PluginCapabilityAdapter 加 projection 字段，invoke 只走轨道 A；0.14.5 删 plugin_item_to_item_result）
- `src/domain/plugin/manifest.rs` — 改造（加 ProjectionRule 投影规则字段）

**ai 域**：
- `src/domain/ai/tool_adapter.rs` — 改造（删 ActionTool 适配器 + exec_error_to_string，build_agent_tools 签名删 action_registry 参数，AI tool 池只留 CapabilityTool）

**execution 域**：
- `src/domain/execution/builtin.rs` — 改造（删 OpenUrlAction/OpenPathAction/RevealInExplorerAction，12→9 builtin）
- `src/domain/execution/registry.rs` / `group.rs` / `mod.rs` — 改造（9 builtin + 删 FILE_GROUP + 文档更新）

**四出口收敛**：
- `src/cli/commands.rs` — 改造（blink run 改调 to_display_text()，删内联 match）
- `src/domain/mcp/server.rs` — 改造（call_tool 改调 canonical，修 Items 投影 score 漂移）
- `src/app/commands/ai.rs` / `search.rs` — 改造（对话历史加载复用 summarize_tool_result；run_builtin_action/confirm_ai_action 路由到 CapabilityRegistry）
- `src/domain/search/service.rs` — 改造（items_to_entries pointer 提取 + action 映射 + 隐式 copy 兜底；Turn 2 open_url 降级确认）
- `src/domain/ai/chat_service.rs` / `src/main.rs` — 改造（ChatService 删 action_registry 参数）

**插件示例 + manifest**：
- `plugins/examples/{translate,ip,weather}/` — 改造（tool-call 返回 {data}，0.14.5 删 ToolResultPayload 死代码）
- `plugins/builtin/{translate,ip,weather}/manifest.json` — 改造（加 ProjectionRule 投影规则）
- `frontend/js/results.js` — 无需改造（后端 items_to_entries 完成全投影，前端只渲染 AppEntry）
- `Cargo.toml` — 加 jsonpath-rust 依赖

**0.14.6 架构与工程收敛**：
- `src/domain/config/` + `src/app/{config,ai_config,stt_config}.rs` — 配置域下沉 + 兼容 re-export
- `src/domain/event.rs` + `src/app/domain_env.rs` — `DomainEnv` 抽象与 Tauri 实现；Capability 侧使用更窄的 `CapabilityEnv`
- `src/domain/schema.rs` + `src/domain/{execution,capability}/schema.rs` — `ToolSchema` 公共基
- `src/domain/event_names.rs` + `frontend/js/event-names.js` — 前后端事件名清单
- `src/infra/platform/{icon,shell,lock}.rs` — Win32 平台实现归位
- `src/app/commands/` — Tauri commands 按域拆分；`ai/{conversations,management}.rs`、`search/{media,context_menu}.rs`、`stt/maintenance.rs` 继续按内部职责拆分
- `frontend/js/{chord-screenshot,ss-*}.js` — 截图 overlay 入口与职责模块
- `frontend/js/settings/tabs/ai/` — AI 设置子域模块
- `frontend/css/entries/chat.css` + `frontend/css/views/chat/` — 对话样式入口与组件分片
