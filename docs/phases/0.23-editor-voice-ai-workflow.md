# Blink 0.23 — 通用文本编辑工作台（Source/MD、连续听写与可确认 AI）

> **范围**：把现有内容编辑器收敛为单窗口、单活动会话的临时文本工作台；统一 Source 原文与 Markdown 所见编辑、来源与保存目标，再接入连续听写、“整理选中内容 / 整理本次听写”和确认 Diff。0.23.7 补齐长语音 VAD 窗口配置与验证，0.23.7.1 追查轻声短句停顿不定稿；0.23.8 为编辑区补页内右键菜单。
>
> **状态**：🚧 0.23.0～0.23.8 的既有切片已完成（0.23.7.1 已完成，待用户复核实机体感与 case_07 manifest 重基线；全文档精简按 spec-phase §六 待执行；0.23.8 真窗交互待人工复核）。
>
> **动机**：当前编辑器仍由 `origin`、`format`、`save_policy` 和两套 DOM 编辑逻辑驱动。直接叠加语音、AI 与文件保存会继续放大分支和竞态；0.23 先建立一个简单、可信的编辑会话，再完成“捕获 → 整理 → 交付”闭环。
>
> **前置**：0.10 语音输入、0.12 AI Provider、0.16/0.18 内容编辑器与 Tiptap、0.21 Capability/Interaction 边界、0.22 STT 本地模型运行时。
>
> **原则**：UTF-8 文本是持久化真源；0.23 只保留一个预热编辑器窗口和一个活动 EditorSession；VoiceSession 与 AI 请求均服从现有全进程单活跃约束；AI 未经确认不改正文；所有保存目标真实可见；不影响主窗口 P0 链路；前端运行时仍无 npm/bundler，Tiptap 由 xtask 预打包。

---

## 一、里程碑总览

| 版本 | 状态 | 内容 | 优先级 | 依赖 |
|---|---|---|---|---|
| **0.23.0** | ✅ | 产品原型、Markdown 保真与性能 spike、协议定案 | P0 | 0.22 |
| **0.23.1** | ✅ | 单 EditorSession、Source/MD 双视图与统一 Editor Adapter | P0 | 0.23.0 |
| **0.23.2** | ✅ | 来源/保存目标解耦、剪贴板/便签/文件/AI 对话流转 | P0 | 0.23.1 |
| **0.23.3** | ✅ | 通用 voice-overlay、Editor VoiceTarget 与连续听写 | P0 | 0.23.1 |
| **0.23.4** | ✅ | 听写整理、稳定 Diff 与确认替换 | P0 | 0.23.1、0.23.3 |
| **0.23.5** | ✅ | 异常、设置/i18n、性能与回归收尾 | P1 | 0.23.2～0.23.4 |
| **0.23.6** | ✅ | Review 收口：异步回流、保存/关闭、听写范围与 AI 取消竞态 | P0 | 0.23.5 |
| **0.23.7** | ✅ | 长语音 VAD 窗口高级配置、真实链路参数矩阵与切片质量验证 | P1 | 0.23.3、0.22 |
| **0.23.7.1** | ✅ | 轻声短句停顿不定稿：实机录音复现、根因确认、切句修正与回归 | P0 | 0.23.7 |
| **0.23.8** | ✅ | 编辑器页内右键菜单与选区整理入口 | P1 | 0.23.4 |

**依赖关系**：0.23.0～0.23.8 的既有交付已完成；0.23.7.1 基于 0.23.7 的窗口接线与私有语料回放，复核 G1/G2 和编辑器共用的 STT 切句链路。

---

## 二、背景与动机

### 2.1 当前问题

- 后端只有一个会被后续请求覆盖的 `PendingEditorPayload`，前端也用模块级变量保存正文、来源和保存策略。
- Plain 使用 textarea，Markdown 使用 Tiptap，两套实现没有统一的 selection、revision、事务和清理契约。
- 保存逻辑由裸字符串分支直接执行；来源、保存目标和窗口生命周期混在一起。
- VoiceService 只认识 G1/G2/G3；编辑器连续听写需要稳定目标、confirmed/preview 分离和顺序保护。
- 现有 AI 全进程单活跃，编辑器整理不能另建并发体系。

### 2.2 明确“不是问题的”

- Plain 与 Markdown 都以文本保存，不建立两套内容产品，也不把 ProseMirror JSON 变成真源。
- Source 与 MD 使用不同 Engine 是合理分工；统一的是 Adapter 和产品语言，不是底层 DOM。
- 编辑器是临时 Interaction，不建立笔记库、文档数据库、标签页或工作区。
- 0.23 只允许一个活动 EditorSession。再次打开同一来源时激活现有窗口；打开不同来源时返回 `EditorBusy` 并激活当前任务，绝不覆盖正文。
- 全进程仍只有一个 VoiceSession 和一个运行中的 AI 请求。
- Chat 的记忆、Agent tool loop 与 MCP 不进入编辑器整理。

---

## 三、核心设计 / 决策

### 3.1 单窗口、单活动 EditorSession（已定）

继续复用现有预热 `content-editor` 窗口，不建设动态 N+1、多窗口池或多任务管理器。窗口无活动会话时保持预热；绑定会话后是普通任务窗口，失焦不隐藏；结束会话后完整 reset 并回到预热态。

`EditorSessionRef` 是后端生成的不可猜测引用。所有 IPC 至少校验 `session_ref + generation`，命令还必须校验调用窗口就是绑定的 `content-editor`。涉及保存或正文替换时再校验相应 revision/request id。

新请求规则固定：

- 无活动会话：绑定并显示。
- 与当前持久来源相同：激活已有窗口。
- 来源不同：返回结构化 `EditorBusy`，激活已有窗口并提示先完成当前任务。
- 用户结束当前任务后可重新发起原操作；0.23 不排队、不覆盖、不自动合并。

### 3.2 状态所有权与正交状态（已定）

后端拥有 session 身份、来源、保存目标、来源 revision、便签租约、窗口绑定和异步操作 id；前端拥有当前工作文本、selection、undo/redo、Engine 状态与单调 `content_revision`。保存时前端提交完整 UTF-8 文本和 revision，后端校验后执行目标副作用。

状态不压成一个大 `phase`，而是四个正交切片：

| 切片 | 状态 |
|---|---|
| `lifecycle` | `binding / active / closing / idle` |
| `save` | `idle / running / conflict / failed` |
| `voice` | `idle / recording / paused / finalizing` |
| `transform` | `idle / running / candidate / stale / failed` |

异步结果必须携带 operation/request id；旧 generation、旧 operation 或旧 request 只能清理自身，不能修改新会话。

### 3.3 Source/MD 是同一文本的双视图（已定）

内部使用 `source / ir`，界面固定显示“原文 / MD”。Source 精确保真；MD 使用 Tiptap/ProseMirror 所见编辑。来源以 `MarkdownViewPolicy` 声明入口：

| 策略 | 行为 |
|---|---|
| `disabled` | 只显示原文 |
| `available` | 默认原文，可手动进入 MD |
| `preferred` | 通过风险门后默认 MD |

空白和便签为 `preferred`；剪贴板、选区和普通能力结果为 `available`；明确的代码/配置等结构化来源可为 `disabled`。风险门优先于来源偏好，弱内容检测只提示，不自动改变视图。

Tiptap 以仓库锁定版本和 0.23.0 corpus 结果为准，不承诺任意 Markdown 字节级无损：

- 未发生 MD 编辑时，切回 Source 必须逐字符保留原文。
- 首次 MD 编辑前保存会话级原文检查点。
- 发生 MD 编辑后只对已验证子集承诺语义正确；未知或可能丢失的结构拒绝进入可编辑 MD。
- 切换视图会取消运行中的整理请求并丢弃候选，不做跨 textarea/ProseMirror 的复杂 anchor 迁移；正文不受影响。
- Source 不受 MD/Diff/AI 阈值限制，但 0.23.0 必须给出整个编辑器可支持的文档大小 envelope，不能宣称无限文本。

首批验证子集：标题、段落、粗体、斜体、删除线、链接、有序/无序/任务列表、引用、行内代码、围栏代码块和分隔线。表格、脚注、数学、任意 HTML 与自定义语法未逐项通过前保持 Source。

### 3.4 Editor Adapter 只暴露当前 Engine 能可靠完成的语义（已定）

SourceEngine 与 MarkdownIrEngine 共同提供：文本快照、当前 selection 文本、当前 Engine 内的 opaque range handle、单事务替换、追加文末、undo/redo、focus、scroll-to-range、change/selection 订阅和 dispose。

不跨 IPC 暴露 DOM Range、ProseMirror position 或通用字符 offset；后端不按 offset 修改正文。整理请求冻结 `range_handle + selected_text_hash + content_revision`，当前 Engine 负责最终校验和替换。

### 3.5 来源与保存目标分离（已定）

`SourceDescriptor` 只描述来源；`CommitTarget` 只描述主保存去向。首版来源为 `empty / clipboard_item / sticky / selection / capability_result`，目标为 `clipboard_result / update_sticky / return_to_caller / confirmed_file`。

默认映射：剪贴板、空白、选区和不可回写能力结果 → `clipboard_result`；便签 → `update_sticky`；具有可靠 endpoint 的 Blink 调用方 → `return_to_caller`。

主保存区始终显示真实去向。“保存”和 `Ctrl+S` 保存但不关闭：

- `clipboard_result`：第一次保存创建一个会话结果历史项并写系统剪贴板；后续保存更新同一结果项，不堆积历史。内部剪贴板写入必须抑制监听器重复采集。
- `update_sticky`：带 revision 原位更新同一便签。
- `return_to_caller`：只向仍有效的 endpoint/generation 回写；失效后引导改存剪贴板或文件。
- `confirmed_file`：仅由用户完成“保存到…”后建立，后续保存校验文件 identity/mtime/hash 后原位写入。

“保存到…”成功后切换主目标；“另存为副本…”、复制、创建便签和发送到 AI 对话不改变主目标。文件写入复用唯一 Capability，采用 UTF-8、临时文件加原子替换；取消和失败均不改变正文、dirty 或主目标。

clean 关闭直接结束；dirty 关闭显示“保存并关闭 / 放弃修改 / 继续编辑”。用户主动退出 Blink 且存在 dirty 编辑会话时只显示一次汇总确认；系统关机或强制退出不阻塞。0.23 不建立磁盘恢复草稿。

### 3.6 连续听写是单 VoiceSession 的 Editor 模式（已定）

长按说话保持原有 G1/G2/G3 行为。编辑器连续听写由按钮显式开始，冻结当前 `session_ref + generation`，confirmed segment 按 `epoch + segment_seq` 追加到最新文末；首段按需补一个段落分隔。preview 只进装饰层，不进入正文、dirty、undo、保存或 AI 输入。

VoiceService 保存有界 confirmed snapshot。前端记录 `last_seq`：重复 seq 忽略，发现缺号、窗口恢复或重新聚焦时拉取 snapshot 补齐。录音失焦、最小化不中断；STT 失败或关闭目标时保留 confirmed、丢弃 preview。

结束后记录本次追加范围并显示两个显式动作：“定位到本次听写”“整理本次听写”。它们只是用户主动入口，不自动上传或改写，也不增加整次录音管理器。

### 3.7 AI 整理服从全局单活跃并只产候选（已定）

首版只有“整理选中内容”和“整理本次听写”，两者最终都提交明确的非空范围。整理只修正明显误识别、标点、分段和无意义口水词，不总结、扩写、翻译或改变立场。

- 复用全进程 AI 请求协调器；主窗口、Chat 与 Editor 同时最多一个运行中请求。
- 使用无记忆、无 Capability/Skill/MCP tool 的 `AIProvider::complete`；模型复用主窗口当前选中的 AI 模型，未配置时隐藏整理动作，不新增一套模型选择器。请求固定 `temperature=0.1`、关闭 thinking，`max_tokens` 按输入估算加小幅余量且不超过模型上限。
- system instruction 与用户文本分消息传递，不把用户正文拼进可闭合的 XML/Markdown 指令模板。
- 超过安全阈值时不截断发送；模型输出被截断或为空时不生成可应用候选。
- 终稿完整到达后一次性生成稳定 Diff，不显示跳动的增量 Diff。
- 请求冻结完整 `content_revision`。请求后正文任何变化都会把候选标为 stale，只允许复制或按最新内容重新整理；首版不做范围外编辑仍可应用的复杂映射。
- 当前视图切换、窗口关闭或新请求都会取消旧请求并丢弃候选。
- 通过 revision、range handle 和文本 hash 校验后，用 Adapter 单事务替换，一次 undo 恢复。

Diff 对中文使用字符/词组级比较；删除为浅红底加删除线，新增为浅绿底并辅以 `−/+` 等非颜色标识。无法安全保持 Markdown 块边界时只允许复制结果。

### 3.8 能力布局与隐私（已定）

界面只保留：顶部任务/来源/“原文·MD”，正文，底部当前保存目标与保存按钮，常驻连续录音按钮，以及按上下文出现的整理动作。“更多”中放保存到、另存副本、复制、创建便签和发送到 AI 对话；不可用动作隐藏。

“发送到 AI 对话”有选区时预填选区，无选区时预填全文，不自动发送、不关闭编辑器、不改变保存目标。云端 STT/AI 继续服从既有强告知门；日志只记录 session、generation、revision、epoch、seq、长度和耗时，不记录正文、转录或 AI 输入。

### 3.9 最小 IPC 契约（已定）

沿用现有命名入口并收窄为以下最小集合，避免每个子功能另造窗口控制协议：

| IPC | 用途 |
|---|---|
| `open_content_editor(request)` | 创建、激活同源会话或返回 `EditorBusy` |
| `get_content_editor_session(session_ref)` | 前端绑定时拉取只读 snapshot |
| `commit_content_editor(request)` | 按当前 CommitTarget 保存，返回新 baseline/revision |
| `end_content_editor(request)` | 保存后结束或明确放弃，并释放租约、reset 窗口 |
| `start/pause/resume/stop_editor_voice` | 控制 Editor continuous VoiceSession |
| `get_editor_voice_snapshot(epoch, after_seq)` | 缺号、恢复或重新聚焦时补 confirmed segment |
| `start/cancel_editor_transform` | 发起或取消全局单活跃整理请求 |

正文替换、selection、undo/redo 和 Diff 应用只在前端 Adapter 内发生，不增加“任意修改编辑器正文”的 Tauri command。事件统一进入 `EventNames`，至少包括 voice segment/status 和 transform completed/failed；所有事件带匹配的 session/generation 及 epoch/request id。

结构化错误至少包括：`EditorBusy`、`StaleSession`、`StaleRevision`、`SourceConflict`、`TargetUnavailable`、`VoiceBusy`、`AiAlreadyActive`、`Cancelled`、`Unsupported`、`Io`。前端按 kind 显示和决定是否可重试，不解析中文错误字符串。

### 3.10 0.23.0 定案记录（已定，证据见 testdata 报告）

以下结论由 0.23.0 的原型、语料与性能 spike 产出，进入 0.23.1 前为冻结态；改动需重跑对应 runner。

**finish_reason 投影（已定）**：rig-core 0.42 的 `CompletionResponse` 自带规范化 `FinishReason`（`Stop/Length/ToolCalls/ContentFilter/Other`），`truncated_output()` 覆盖 `Length|ContentFilter`；四种 ProviderKind 的非流式 completion 均有 wire 值映射（OpenAI `choices[].finish_reason`、Anthropic `stop_reason`、Gemini `finishReason`、Ollama `done_reason`→Stop/Length，其余归 `Other`）。落地方式：0.23.4 给自有 `CompletionResponse` 增加 `finish_reason: Option<FinishReasonKind>` 投影（类型墙，不泄漏 rig 类型）；`truncated_output()==true` 或文本为空 → 不生成可应用候选；`None/Other` 不宣称截断保护，记非敏感诊断。

**全局 AI 并发（已定）**：复用既有 `ChatService::RequestTracker` 单活跃协调器（主窗口与 Chat 已共用）；0.23.4 把 Editor transform 请求注册进同一 tracker，`ActiveChatStatus.target_window` 增加 `"editor"` 取值；`AiAlreadyActive` 携带当前活跃窗口标识。

**调用窗口校验（已定）**：全部 editor command 校验 `session_ref + generation`；`session_ref` 为后端生成的不可猜测 opaque 引用；`commit/end` 及任何触正文的 command 还须校验调用窗口 label == `content-editor` 与相应 `revision/request id`。

**剪贴板结果项（已定）**：`clipboard_result` 第一次保存创建一条会话结果历史项，后续保存更新同一项（以结果项 id 为键），不堆积历史；应用层内部剪贴板写入必须带 source 标记以抑制监听器重复采集。

**range handle（已定）**：由当前 Engine 签发的 opaque 字符串（形如 `engine-kind:anchor`），仅当前 Engine 可解析与校验；后端永不解析 handle、不按 offset 改正文；整理请求冻结 `range_handle + selected_text_hash + content_revision`，替换前由 Engine 复核三者。

**EOF 换行约定（已定）**：Tiptap `serialize()` 不输出文末换行；0.23.1 的 MD→文本保存路径在序列化后统一补一个末尾换行。round-trip"逐字节一致"按补换行后比较；纯视图切换的逐字符保真由会话级原文检查点保证，与序列化无关（原型已验证）。

**Markdown 风险门规则（已定，语料证据见 `testdata/editor/markdown/roundtrip-report.md`）**：拒绝进入可编辑 MD 的结构 = GFM 表格（整块内容丢失）、脚注（被改写为链接）、`$$` 数学块（命令被转义污染）、HTML 块/内联/注释（标签转义为字面文本）、行内代码含反引号（Tiptap 3.29.2 序列化器缺陷，二次解析不稳定）。未识别自定义语法（`:::`、`==`）字面保留，安全。规范化重写（`*`→`-`、`__`→`**`、Setext→ATX、宽松列表收敛、autolink、实体解码、`~~~`→```` ``` ````、嵌套缩进 2/3 空格）不阻断，首次 MD 编辑保存时提示"已按编辑器规范重写"。

**阈值与 envelope（已定，测量见 `testdata/editor/markdown/perf-report.md`）**：

| 门 | 冻结值 | 依据 |
|---|---|---|
| 可编辑 MD 尺寸门 | ≤32KB 正常进；32–128KB 可进但不自动、提示卡顿；>128KB 拒绝（保持 Source） | 混合文档 parse 超线性：32KB p95≈41ms、128KB≈0.5s、512KB≈7.5s |
| Diff 输入门 | ≤200KB（≈2 万 token）；D>4000 熔断 | 5% 编辑率 200KB p95≈35ms；500KB/5% 已现熔断 |
| AI 整理输入门 | `estimate_text_tokens(input) ≤ 24,000`；`max_tokens = clamp(input_est+2048, 1024, context−input_est−margin)` | tiered fallback 最小窗 32K 推导（perf 报告 §三） |
| Source envelope | 2,000,000 字符硬上限，超限拒绝载入 | 字符串操作 p95=0.2ms@2MB；textarea 体验由 0.23.5 真窗复测 |

风险门（内容结构）优先于尺寸门，尺寸门优先于 `MarkdownViewPolicy` 来源偏好；Source 不受任何增强门限制，始终可编辑保存。

---

## 四、复用与范围边界

### 4.1 复用盘点

| 现有基础 | 0.23 用途 |
|---|---|
| 静态预热 `content-editor` | 保留单窗口，增加 session bind/reset |
| `EditableContentPayload` / pending pull | 迁移为结构化 session snapshot，完成后删除单槽 |
| Tiptap 3 + `@tiptap/markdown` | 共享 MarkdownIrEngine、corpus 与风险门 |
| textarea | SourceEngine |
| Sticky revision | `update_sticky` 冲突防线和编辑租约 |
| CapabilityRegistry / SurfacePort | 打开编辑器和原子输出动作唯一入口 |
| VoiceService / voice-overlay | Editor target、连续听写和通用浮窗 |
| AI Provider / 全局请求协调 | 无记忆整理和跨窗口单活跃 |
| 自绘确认卡片 | 保存冲突与 Diff 面板 |

### 4.2 不进 0.23

| 项 | 理由 |
|---|---|
| 多 EditorSession、动态 N+1 编辑器窗口池 | 不是连续听写与整理闭环的必要条件，显著增加窗口和竞态复杂度 |
| 文档数据库、笔记库、标签页、工作区、崩溃恢复中心 | 编辑器仍是临时任务 Interaction |
| 任意 Markdown 字节级无损 IR | Source 是保真出口 |
| 跨 Source/MD 的 selection/anchor 映射 | 切换视图直接取消候选，保持规则简单可靠 |
| 范围外编辑后仍应用旧 AI 候选 | 任何 revision 变化都使候选过期 |
| 自动 AI 整理、多个 preset、自定义指令 | 首版只做两个显式入口和一套整理语义 |
| 从任意既有文件打开并覆盖、编码识别 | 0.23 只写用户经“保存到…”确认的新目标 |
| 全局永久录音、整次录音撤销管理器 | 隐私和状态成本过高 |
| Tiptap Pro/Cloud AI、编辑器内完整 Chat/Agent | 复用 Blink Provider 和独立 Chat Surface |
| 新前端框架或运行时 npm/bundler | 违反前端工程约束 |

---

## 五、子版本执行方案

### 5.1 0.23.0：原型、语料与协议定案 ✅

- ✅ 完成单窗口原型（`testdata/editor/prototype/index.html`，浏览器直接打开，无后端依赖），验证顶部信息、Source/MD、主保存区、连续录音、本次听写动作和非模态 Diff。
- ✅ 在 `testdata/editor/markdown/` 建立支持/规范化/拒绝语料（33 样本 + manifest 期望标注）；输出固定 Tiptap 3.29.2 的 round-trip 报告，runner 全绿。
- ✅ 测出文档总体 envelope 以及 MD、Diff、AI 阈值（perf 报告：机器、样本、p50/p95、内存）。
- ✅ 冻结 DTO、正交状态、错误码、调用窗口校验、剪贴板结果项和全局 AI 并发语义（§3.10）；确认四种 ProviderKind 均可投影 `finish_reason`（rig-core 0.42 原生支持），`None/Other` 不宣称截断保护。
- 本子版本不迁移生产入口（未改 `frontend/`、`src/` 任何生产文件）；未决项已在 §3.10 关闭。

### 5.2 0.23.1：单会话编辑底座 ✅

- ✅ 新建框架无关 `src/domain/editor/mod.rs`：`SourceDescriptor` / `EditorError`（serde tagged）/ `OpenEditorRequest` / 快照与 commit/end DTO、`decide_open` 纯决策、`markdown_view_policy`、2M 字符 envelope 校验；`rg "tauri::" src/domain/editor` 为 0（arch_guard 覆盖）。
- ✅ 新建应用层 `src/app/editor.rs` `EditorSessionService`：单活动会话槽、`content-editor` 调用窗口校验、generation 单调自增、不可猜测 session_ref；便签来源打开时从 DB 读正文与 revision 真源、提交成功后前移基线；Mutex guard 不跨 await，异步副作用完成后按 `session_ref + generation` 复查才落状态——旧会话迟到结果只被丢弃。
- ✅ IPC 收敛为 §3.9 最小集合的 0.23.1 子集：`open_content_editor` / `get_content_editor_session` / `commit_content_editor` / `end_content_editor`；`EditorError` 经 `CommandError` 投影稳定 code（`editor_busy` / `source_conflict` / `stale_session` / `target_unavailable`…），前端按 code 分类展示；新增 `blink://editor-session-changed` 事件（bound/ended，携带 sessionRef + stickyId），替代旧 eval 重载钩子。
- ✅ 旧协议全量删除：`PendingEditorPayload`、`get_content_editor_payload`、`save_content_editor` 与裸 `format/origin/save_policy` 分支（含 `domain_env` capability 路径、`start_content_editor` schema、chord `extra_args`）；`rg "PendingEditorPayload|save_policy" src frontend` 命中为 0。
- ✅ 前端拆分：`main.js` 收敛为装配层（376 行）；新建 `editor-session.js`（commit/end 代际防护状态机）、`editor-adapter.js`（双视图唯一操作面：检查点 / revision / 单事务 replaceAll / syncFromExternal）、`engines/source-engine.js`（textarea，execCommand 撤销栈）、`engines/markdown-engine.js`（Tiptap，edited/suppress/normalized 标志）、`engines/markdown-gate.js`（纯函数风险门）、`shared/tiptap-editor.js`（vendor 封装 + EOF 换行约定收敛）。
- ✅ Source/MD 切换：顶部"原文/MD"分段控件；风险门（表格/脚注/公式/HTML/行内代码含反引号，剥离行内代码与围栏块后扫描）+ 尺寸门（32KB 不自动进 / 128KB 拒绝）按**当前正文**实时重估；门拒绝保持 Source 并分档提示；切换即弃旧引擎，正文经检查点衔接；MD 未编辑时切回 Source 与保存正文逐字符等于原文。
- ✅ 便签编辑租约：会话绑定 sticky 来源时便签窗口进入只读横幅（"返回编辑器"激活现有窗口），`ended` 事件解除；租约期间便签窗口防抖保存被抑制，保存责任归编辑器；保存冲突返回 `source_conflict`（完整冲突 UI 留 0.23.2）。
- 定向测试：`domain::editor` / `app::editor` 单测 11 项；前端 `markdown-gate.test.mjs`（33 样本语料逐样本对齐 manifest 期望 + 尺寸/围栏/货币误伤单元用例）、`editor-adapter.test.mjs`（检查点还原 / 门实时重估 / reset 清空）、`editor-session.test.mjs`（同 ref 去重 / 迟到结果不落状态 / 结构化错误透传）。`cargo test --bin blink` 3251 全绿；`node frontend/run-tests.mjs` 68/69（唯一失败 `ss-control-hints` 为存量问题，干净 main 可复现，与本 phase 无关）。

### 5.3 0.23.2：保存目标与能力流转 ✅

- ✅ 类型化 `CommitTarget`（`clipboard_result` / `update_sticky` / `return_to_caller` / `confirmed_file`）与 `plan_commit` 纯决策；快照与提交结果携带目标，主保存区始终显示真实去向（底部保存区：目标 + dirty 点 + 状态 + 保存[Ctrl+S]，顶部"更多"菜单，删除旧顶部保存/取消按钮）。文件冲突投影冻结错误集内的 `SourceConflict`，前端按目标 kind 分流三选。
- ✅ 剪贴板结果项：首存创建、后续按原 id 原位更新（新增 `clipboard::update_item_text`，id/hit_count/source_app 不变、顶置 created_at）；新增 `ClipboardWriteSource::EditorResult`（`blink:editor`，skip_persist）抑制监听器重复采集；结果项被用户删除后按原 id 重建。
- ✅ 便签保存带 revision（沿用 0.23.1 基线）；冲突只提供 复制当前内容 / 放弃修改并重新载入（重载时 revision 基线一并前移）/ 取消，不提供覆盖。
- ✅ 新建 `write_text_file` Capability（local-only、`Dangerous`、绝对路径校验、同目录临时文件 + `MoveFileExW(REPLACE_EXISTING|WRITE_THROUGH)` 原子替换、identity(size+mtime_ms) 冲突检查在写入同闭包内无 TOCTOU）；原 `save_text_file`（导出对话）收敛到同一实现；`infra/utils/fs.rs` 提供原子写原语（对齐 local_engine 暂态重试先例）。
- ✅ "保存到…"切换主目标为 confirmed_file；"另存为副本…"只写副本不改主目标；外部修改过的文件不被静默覆盖（identity 失配 → SourceConflict → 三选：仍要覆盖 / 另存为副本 / 取消）。
- ✅ 次级动作：复制全文（User 源、进历史）、创建便签（全文）、发送到 AI 对话（选区优先否则全文，经 `open_chat` prefill 只预填不发送）；均不改主目标、不关编辑器。
- ✅ clean/dirty 关闭三态保留（ESC/标题栏 X/onCloseRequested 全覆盖）；主动退出一次汇总确认：托盘退出与 `exit_blink` Capability 统一收敛 `request_user_exit`——无会话或 clean 直接放行，dirty 时唤起并聚焦编辑器窗口展示一次确认（请求-应答协议 `blink://editor-exit-request` + `resolve_editor_exit`，不匹配应答忽略，10s 超时放弃本次退出）；系统关机/强制退出走 `RunEvent::Exit`，不经确认路径。
- 定向测试：domain `plan_commit` 分派/默认映射/serde 5 项；`update_item_text` 2 项；`write_text_file` 与 `atomic_write_bytes`（路径校验/首存/冲突拒绝覆盖/外部删除/参数契约）6 项；inventory 注册断言；前端 `actions.test.mjs` 12 项（目标映射/取消无副作用/副本不改主目标/冲突透传/i18n 对齐）+ `editor-session.test.mjs` 新增 3 项（目标覆盖回填/revision 前移/reset 清空）。`cargo test --bin blink` 3266 全绿；`node frontend/run-tests.mjs` 69/70（唯一失败 `ss-control-hints` 为存量）；`cargo fmt --check`、`cargo check --no-default-features` 0 警告、`git diff --check` 通过；`cargo xtask lint` 的 3 处 CSS hex fallback 基线违规为 main 存量（download-progress / settings-local-runtime-panels / welcome.css，本次未触碰，归 0.23.5 收敛）。

### 5.4 0.23.3：连续听写与通用浮窗 ✅

- ✅ 新建 `src/domain/stt/dictation.rs`：`EditorDictationTracker` 纯逻辑——伪流式 confirmed 单调增长（0.22.15 事务化句尾）前提下按累积快照推导增量段并分配单调 seq；Final 全文按已交付前缀精确裁剪出收尾段，前缀异常时退化为最长公共字符前缀（宁可裁剪不丢段）；7 项单测覆盖增长/重复/回退忽略/多字节边界。
- ✅ `VoiceTarget` 增加 `Editor`；`VoiceService` 增加连续模式（`continuous` + `paused` 原子标志）与听写 epoch（`dictation_epoch` 单调递增，与 recording epoch 是两个边界）；有界 confirmed snapshot（`EditorDictationState`，256 段 FIFO，淘汰计数暴露给前端感知不可补齐区间），结束后保留到下一次听写开始。
- ✅ IPC 落地 §3.9 的 0.23.3 子集：`start/pause/resume/stop_editor_voice` + `get_editor_voice_snapshot(epoch, after_seq)`；`start` 只接受 `content-editor` 窗口且校验冻结的 `session_ref + generation`（`EditorSessionService::verify_active_session`），pause/resume/stop/snapshot 白名单为编辑器 + 浮窗；新增 `EditorError::VoiceBusy`（投影 `voice_busy`）与 `focus_content_editor` 命令。G1/G2/G3/Editor 互斥沿用 `session.recording` 单槽——编辑器听写期间 hold 直接忽略。
- ✅ 事件：`blink://editor-voice-segment`（confirmed 段，携带 sessionRef/generation/epoch/seq）与 `blink://editor-voice-status`（phase: recording/paused/finalizing/ended/error + preview/message）；preview 只进事件与浮窗装饰层，从不写正文。STT 引擎 Error 事件在 Editor 路径做终态清理（保留 confirmed、丢弃 preview、回收录音资源）。
- ✅ 通用浮窗：`voice-overlay.html` 按 target/mode/phase 投影——g2 hold 布局保留，editor 模式新增拖拽条（`data-tauri-drag-region`，含状态点脉冲 + 收起/展开 + 语音设置入口 `open_settings_tab(tab=voice)`）、波形/预览区、暂停/继续（幂等）、结束、返回编辑器（`focus_content_editor`）；状态以状态点颜色 + 文案双通道表达，不依赖单一颜色。
- ✅ 前端听写接入：新建 `content-editor/voice.js` `EditorVoiceController`——启动冻结会话身份、seq 去重、缺号拉 snapshot 补齐、无法补齐时跳号继续并提示；结束后保留本次追加文本供"定位到本次听写"（Source/MD 双视图文本搜索选中 + 滚动可见）与"整理本次听写"（0.23.3 只记录范围，动作入口显示 0.23.4 提供整理）。编辑器 ESC 分层：听写中先停听写不关窗；关闭/生命周期强制关闭前停听写（confirmed 已在正文，未定稿 preview 按 §3.6 丢弃）；窗口重新聚焦触发 snapshot 补齐。
- ✅ 追尾语义：Engine 层新增 `appendText`（textarea 聚焦走 execCommand 并入原生 undo 栈，未聚焦直写并手动通知——**不抢焦点**）与 `appendParagraph`（首段分隔：Source 按 `dictationGapPrefix` 补空行，MD 在末段非空时插入新段落/围栏块收尾后新起段落）；追加单事务、恢复用户此前 selection（光标原在文末则自然跟随追加内容，打字语义）；confirmed 追加计入正文/dirty/undo。
- 定向测试：`dictation.rs` 7 项；前端 `voice.test.mjs` 14 项（start 冻结身份/按序追加与首段分隔/重复与异身份异 epoch 过滤/缺号快照补齐/无法补齐跳号/preview 不进正文/paused-recording-ended 投影/stop 回落/voice_busy 错误/聚焦补齐/会话结束联动/首段分隔纯函数/adapter 分派）。`cargo test --bin blink` 3273 全绿；`node frontend/run-tests.mjs` 70/71（唯一失败 `ss-control-hints` 为存量，干净 0.23.2 复现）；`cargo fmt --check`、`cargo check --no-default-features` 0 警告、`git diff --check` 通过；`cargo xtask lint` 的 3 处 hex fallback 为 main 存量（归 0.23.5）。

### 5.5 0.23.4：整理、Diff 与确认 patch ✅

- ✅ 接入全局 AI 单活跃协调：`ChatService` 新增 `register_editor_transform` / `release_editor_transform`，Editor 请求与主窗口/Chat prompt 共用同一 `RequestTracker`（`start_gate` 串行化"检查+安装"）；`ActiveChatRequest` 增加 `origin`（Chat/Editor），abort/abort_active 对 Editor 来源直接拒绝——**取消路径唯一**（`cancel_editor_transform` / 自然完成），`target_window` 增加 `"editor"` 取值，`AiAlreadyActive` 携带 `active_window`。
- ✅ 建立无记忆、无工具、关闭 thinking 的 Editor Transform 请求：模型复用主窗口当前选中（`resolve_current_entries(Ephemeral)` + registry `resolve_explicit`，thinking 默认关闭补丁随池内 provider 实例继承）；system instruction 与用户正文分消息（正文不进任何可闭合模板，§6.5 提示词注入验收）；固定 `temperature=0.1`、硬超时 120s（用户显式成段任务，不套主窗搜索 20s SLO）。
- ✅ `CompletionResponse` 增加可选 `finish_reason: Option<FinishReasonKind>` 投影（`Stop/Length/ToolCalls/ContentFilter/Other`，rig 0.42 归一化枚举的类型墙映射，`Other` 不携带 wire 原文）；`truncated_output()==true`（Length/ContentFilter）或文本为空 → 不生成可应用候选（failed 事件 code `length`/`content_filter`/`empty`）；`None/Other` 不宣称截断保护，记非敏感诊断。
- ✅ 输入门与预算（§3.10 冻结公式，纯函数 `plan_transform`）：`estimate_text_tokens(input) ≤ 24,000` 超限拒绝发送（不截断）；`max_tokens = clamp(input_est+2048, 1024, context−input_est−margin)`，上下文不足以容纳下限时拒绝（`context_too_small`）。
- ✅ 实现整理选中（选区冻结）和整理本次听写（拼接文本 Engine 内定位），终稿到达后一次性生成稳定 Diff：`diff.js` Myers O(ND) 回溯版（0.23.0 spike 的正式实现），中文按字符粒度 tokenize、D>4000 熔断、200KB（UTF-8 字节）输入门，超门退化为仅复制展示；删除浅红底+删除线+`−`、新增浅绿底+`＋`（非颜色双通道）。
- ✅ 范围冻结三元组（§3.4）：Engine 签发 opaque range handle（Source 为 `{start,end}` 锚点、MD 为 `{from,to}` PM 锚点）+ 冻结文本 + `content_revision`，随请求上行、完成事件回显；候选 stale（请求后任意 revision 变化）只允许复制或重新整理，Engine 替换前复核冻结文本（syncFromExternal 等 revision 外变化由 Engine 复检兜底拒绝）。
- ✅ 确认应用走当前 Engine 单事务替换（Source：execCommand 并入原生 undo 栈；MD：单 textblock 内 insertText/按行拆段落 replaceWith，一次 undo 恢复）；MD 范围跨块边界（`!sameParent || !isTextblock`）时 `blockSafe=false`，仅允许复制（§3.7）。
- ✅ 取消与生命周期联动：视图切换/新请求/ESC/关闭/会话结束均取消运行请求并丢弃候选；`end_content_editor` 后端兜底 `cancel_active_for_session`（防跨会话迟到事件）；早到完成事件（start 响应前）按"至多一个未决请求"契约采纳，不卡 running。
- ✅ 入口与可见性（§3.8）：动作条按上下文投影"定位/整理本次听写"（听写结束）与"整理选中内容（N 字）"（AI 可用且有选区）；AI 未配置（`get_chat_status.provider_configured`）时隐藏整理入口并监听 `config-changed` 热刷新；候选卡为非模态卡片（标题/stale 条/Diff 区/应用·复制·放弃 + 单事务提示）。
- 定向测试：`domain::editor::transform` 8 项（输入门/公式 clamp/截断与空输出判定/提示词不含模板边界/scope serde）；`chat_service` tracker +4 项（editor 与 chat 双向互斥/abort 拒绝 editor 来源/释放后恢复）；`message` finish_reason 2 项；前端 `diff.test.mjs` 10 项（tokenize/重建无损/合并/熔断/200KB 门/模糊测试 200 组随机编辑对/HTML 转义与非颜色标识）、`transform.test.mjs` 15 项（冻结三元组/requestId 与身份过滤/stale 拒绝应用/单事务替换/复核失败作废/失败码文案/早到事件采纳/新请求取消旧请求/SourceEngine range freeze·replace 复核拒绝）。`cargo test --bin blink` 3286 全绿；`node frontend/run-tests.mjs` 73/74（唯一失败 `ss-control-hints` 为存量）；`cargo fmt --check`、`cargo check --no-default-features` 0 警告、`git diff --check` 通过；`cargo xtask lint` 的 3 处 CSS hex fallback 为 main 存量（归 0.23.5）。

### 5.6 0.23.5：可靠性与发布收尾 ✅

- ✅ 收口会话竞态与失败恢复：非便签来源打开不再被权威加载分支误清空正文；open 的决策与单槽写入改为同锁原子操作；end 仅在后端确认后清前端正文，失败保留会话可重试；generation 水位拒绝 reset 后迟到快照复活旧会话；会话结束统一清听写段、候选、引擎与外围控制器。
- ✅ 收口听写与整理生命周期：end 后按 `session_ref + generation` 释放匹配的麦克风、任务和 snapshot，锁序与 Arc 身份复核防止误杀新会话；stop 丢事件仍回 idle；STT 错误新增稳定 `stt_failed` code 并由前端本地化；Transform 极快取消改用可保留 permit 的单 waiter 通知，避免后台任务漏取消。
- ✅ 补齐设置/i18n/键盘/非颜色通道：语言与主题随配置广播热刷新；动态菜单、保存目标、听写与候选文案同步刷新；Source/MD 使用 tab 语义与方向键/Home/End，更多菜单支持完整方向键/Esc 焦点回归；图标按钮有可访问名称、状态区 live region、听写 `aria-pressed`，所有交互控件有 `:focus-visible`；编辑器与听写浮窗统一使用 Lucide sprite。
- ✅ 多屏与边缘收口：复用编辑器窗口显示前按最近显示器工作区在四边 clamp，使用 i64 计算覆盖负坐标、跨 DPI 与异常尺寸；更多菜单相对窗口右侧锚定，窗口本身先回工作区，避免屏幕边缘溢出。
- ✅ 压力与清理验证：2,000,000 字符 Source 原样载入/reset；100 次 Source↔MD 会话复用后所有 Engine dispose、revision/checkpoint/正文归零；听写 snapshot 固定 256 段并报告淘汰数；33 份 Tiptap corpus round-trip 全绿，Diff 4KB～500KB 阈值复测符合熔断策略。
- ✅ 清理旧债：兼容 DTO/旧命令/裸编辑器事件扫描为 0；修复 `ss-control-hints` 存量语法错误；三处 CSS hex fallback 基线违规收敛为 canonical token；日志补齐 session/generation/revision/request/epoch/seq/耗时，仅记录长度与布尔元数据，不记录正文、转录或 AI 输入。
- 验证：`cargo test --bin blink` 3287 通过（6 ignored）；`node frontend/run-tests.mjs` 74/74 测试文件全绿；Tiptap corpus 33/33；`cargo fmt -- --check`、`cargo check --bin blink --no-default-features`、`cargo xtask lint`、`git diff --check` 全部通过。

### 5.7 0.23.6：Review 发现的竞态与数据一致性收口 ✅

本子版本只修复 0.23 既有链路的可靠性缺口，不增加新入口、保存目标、编辑模式或 AI 能力。Review 时现有编辑器前端、Rust editor 与 Markdown corpus 定向测试均通过；以下问题属于现有测试未覆盖的异步交错与重复文本场景。

- ✅ **便签异步回载加双重版本墙**：`reloadFromSticky` 逻辑下沉到 `EditorSession`（可测）：发起时冻结 `session_ref + generation + sticky_id`，`await` 返回后重新校验会话身份——同便签被重开（新 generation）或来源已切换时丢弃旧回流；非 `force` 路径返回后重查 dirty，读取期间的任何用户编辑都丢弃回流；冲突后的显式强制重载（force）跳过 dirty 重查但同样受身份墙约束。
- ✅ **本次听写使用真实追加范围**：Adapter 新增 `beginDictationRun` / `freezeDictationRun` / `locateDictationRun`，听写开始记录当前引擎文末锚点（首段段落分隔字符不计入范围），两个 Engine 各自实现 `tailCharLength` / `createTailRangeHandle(fromChar)` / `locateTailRange(fromChar)`（MD 侧复用 text-node 拼接映射）。听写一结束即冻结 opaque range handle 供"定位/整理本次听写"直用（`transform.start("dictation", {handle})`），不再通过 `indexOf` 在全文猜测；正文前部相同文本只会命中文末本轮范围，Engine 替换前仍复核冻结文本。视图切换后锚点随旧引擎作废（无跨引擎迁移，§3.3）：听写进行中在新引擎重新锚定，否则清空本次范围并隐藏 chips。
- ✅ **AI 启动期取消可追认**：前端为每次 start 建立本地 operation generation（`_opSeq`）；start 响应未返回期间发生 ESC/视图切换/新请求/关闭/会话结束时本地立即清态，迟到响应按身份判定——已被新一代取代或已取消则标记退役并补发取消（追认），完成事件早到已被采纳则只回填 requestId 不取消；新增 `_retiredRequestIds` 过滤已取消请求迟到的完成/失败事件，防止其在下一请求启动窗口内污染候选/状态。被取消请求经追认取消及时释放全局 AI 单槽，不再等 provider 完成或 120s 超时。
- ✅ **commit/end 同会话串行化**：前端 `EditorSession` 引入 mutation 串行队列（commit/end 排队执行），调用时捕获会话身份、执行时校验（排队期间会话切换的旧操作直接作废）；后端 `EditorSessionService` 增加 `MutationGate`（tokio Mutex，guard 跨 `.await` 持有，锁序恒为 gate → 会话槽），commit 持门覆盖"取状态 → 锁外副作用 → 复查落槽"全程，`end` 改 async 先取门再清槽——保存中关闭/放弃不再产生确认之后的迟到外部写入。
- ✅ **`content_revision` 从簿记升级为提交协议**：domain 新增纯函数 `body_digest`（FNV-1a 64，会话内一致性判定）与 `check_commit_revision` 三态判定（Accept / Idempotent / StaleRevision）；`LiveSession` 记录已接受水位 `committed: Option<CommittedRevision>`（revision + 正文摘要，绑定后首次提交恒 Accept），校验在副作用前完成，成功后水位与新 baseline 同一 guard 内原子前移。旧 revision 与"同 revision、不同正文"的重放返回 `StaleRevision`（投影 `stale_revision`，删除 `#[allow(dead_code)]`）；同 revision、同正文按幂等提交处理（跳过副作用返回当前基线）。
- ✅ **听写快照显式报告真实缺口**：`_resync` 校验 snapshot 首段 `seq` 是否紧接 `last_seq + 1`——不连续即所需区间已被淘汰，显式触发一次 `onGapLost` 后从最早可用段继续消费，禁止静默丢段；首段紧接 lastSeq（客户端已越过淘汰区间）时即使历史 `truncated > 0` 也不误报；快照不可用时保留兜底跳号上报。
- ✅ **补齐竞态回归测试**：前端 `editor-session.test.mjs` 新增 7 项（dirty 期间回流丢弃 / stickyId 失配与同便签重开身份墙 / force 身份约束 / 保存中 end 排队 / commit 失败后 end 按序 / stale_revision 词条）、`voice.test.mjs` 新增 4 项（锚点建立与结束冻结 / 无段·清空·失效不残留 / 快照首段不连续一次上报并继续消费 / 已越过淘汰区间不误报）、`transform.test.mjs` 新增 4 项（start 响应前取消追认 / 新请求取代旧请求追认 / retired 迟到事件过滤 / dictation 冻结 handle 直用）、`editor-adapter.test.mjs` 新增 2 项（前部相同文本命中文末本轮范围 + 确认替换不误伤 / 定位选区与视图切换失效）；Rust 新增 domain 提交协议 6 项（摘要稳定性 / 无水位首提交 / 前进与跳号 Accept / 幂等 / 同 revision 异正文拒绝 / 旧 revision 拒绝）与 `MutationGate` 串行化 1 项。

二次 Review 追加以下收口项（已全部落地）：

- ✅ **幂等只对精确重放生效，不吞显式输出**：提交协议增加 `mutationId`（前端每次显式提交生成唯一意图标识，module 级计数 + UUID 兜底）。`check_commit_revision` 幂等键收敛为 revision + 正文摘要 + mutation id 三元组——只有"同一 mutation 的精确重放"（响应丢失后重试）才早退跳过副作用；同 revision、同正文但意图不同的新提交（"保存到…"切换目标、"另存为副本…"、冲突后"仍要覆盖"、重新复制到剪贴板）判定 `Accept` 并真实执行。补同 revision 下切换目标 / 保存副本 / 覆盖 / 重复复制 / 精确重放 5 项 domain 协议回归 + 前端"两次提交意图标识不同"回归。
- ✅ **AI 连续极早完成不遗留旧 requestId**：候选确认分支（完成事件早于 start 响应被采纳）不再把已完成 id 写回 `runRequestId`，迟到响应只确认候选归属；`start` 进入新一代时显式清空运行槽，上一代遗留 id 不参与本代完成事件过滤。retired 记录改为有界 Map（id → 退役时 opSeq，上限 32，按插入序淘汰最旧），预热窗口长会话不无界增长。补"请求 A 早完成 → 放弃候选 → 请求 B 早完成"连续竞态回归与 retired 有界回归。
- ✅ **听写定位消费结束时冻结的右边界**：两个 Engine 的 `locateTailRange(fromChar)`（锚点到当前文末）收敛为 `locateRange(handle)`——直接选中听写结束时冻结的 opaque handle，右边界为冻结时的文末，听写结束后用户继续输入不会被一并选中；范围内文本被编辑过时定位失效（Engine 复核冻结文本，返回 false）。Adapter `locateDictationRun(frozen)` 按当前 Engine kind 分派并拒绝异 kind handle；main.js 定位 chip 传入 `voice.runHandle`。补 Source"听写后继续输入只选本轮"与 MD 侧分派契约回归。

### 5.8 0.23.7：长语音 VAD 窗口配置与参数验证 ✅

本子版本把三个硬编码切片时间升级为有界高级配置，并用本地语料完成 A～H 候选矩阵验证；不改变 G1/G2 与编辑器听写共用的 STT 处理链路。

**配置落地**：

- `VadConfig` 新增三个秒级字段（旧配置缺字段时 serde 默认 8/12/12，行为不变）：`soft_window_s`（EnergyVad 低能量可切窗口）、`hard_window_s`（EnergyVad 强制切窗口）、`max_uncommitted_s`（伪流式层未提交音频上限，按绝对未提交音频计算，与 VAD speaking 状态无关）。有效组合规则 `soft < hard <= max_uncommitted`，安全范围 3-30/5-30/5-30 秒；`VadConfig::sanitize` 越界 clamp + 顺序前移，前端 `voice-vad.js::normalizeVadWindows` 同规则联动钳制，`set_stt_config` 保存与启动加载（main.rs 持久化回写 / CLI 缓存）双重兜底。
- 接线：`EnergyVad::with_params_and_windows` 注入软/硬窗口；`PseudoStreamingSttEngine` 的 `max_uncommitted_audio_ms` 从常量改为实例字段（由 `from_connection` 从配置注入）；`FunasrEngineConfig`/`VadConfigProjection` 同步投影保持 serde 形状。
- 设置页语音 Tab VAD 卡片新增"切片窗口（高级）"分组三个滑块（含联动钳制、恢复默认、i18n 中英文、aria-label 随语言刷新），纯逻辑抽到 `voice-vad.js` 供无 DOM 单测。

**候选矩阵结果**（固定能量阈值 0.005、最短静音 300ms；四元组为"最短句长 ms / 软窗口 s / 硬窗口 s / 未提交上限 s"；语料 = 5 条 15-26s 新长录音 + 11 条已有标注样本）：

| 组合 | 参数 | 新长录音送模型段 | 词中强制切 | CER(vs 整段转录, 5 条均值) | 旧样本 CER(8 条均值) |
|---|---|---|---|---|---|
| A | 800/8/12/12 | 26 | 3 | 5.0% | 23.1%* |
| B | 1000/8/12/12 | 21 | 3 | 4.4% | 20.6%* |
| C | 1000/10/12/12 | 21 | 1 | 4.0% | 20.6%* |
| D | 1000/8/14/14 | 20 | 3 | 4.4% | 20.6%* |
| E | 1000/10/14/14 | 20 | 1 | 4.0% | 23.1%* |
| F | 1000/10/16/16 | 20 | 0 | 4.0% | 20.6%* |
| G | 1000/6/10/10 | 23 | 6 | 6.5% | 一条 99.4% |
| H | 1000/10/14/16 | 20 | 0 | 4.0% | 20.6%* |

\* 旧样本 8 条可比样本上 A～H（除 G）CER 完全等价；case_05（轻声困难样本）个别组合出现 0.4↔0.6 的抖动，经核对各组合送模型音频完全一致，属 worker 输出非确定性噪声而非参数差异。3 条预期无语音样本不适用 CER。

**关键发现**：

1. 窗口参数只影响"连续说话不换气"场景：16 条语料中 15 条各组合切点完全一致；唯一分化点是最长的 25.9s 连续说话样本——`min_sentence 800→1000` 使 CER 5.1%→2.0%，`soft 8→10` 再降至 0.0%，词中强制切从 3 降到 1。
2. **低窗口方向被证伪**：G（软窗 6s）在 7.5s 样本上切出 1.5s 短尾段，识别输出 994 字符幻觉（实际约 10 字），CER 99.4%——短尾段幻觉是硬约束，软窗不宜低于 8s。
3. **hard/未提交上限加大无收益**：D/E/F/H 在全部真实语料上与 C/B 切点一致（15-26s 音频触及不到 14/16s 上限）。
4. **未提交上限独立生效已证明**：三个合成场景单测（滞回区徘徊时按配置 16s 兜底而非默认 12s、持续有声时先于 12s 硬窗触发且定稿挂起期间不重复切分、句尾交替后按 committed 基点重新计数）+ 真实引擎回放 A/H 对比。

**真实链路回放**（`pseudo_streaming_real_worker_replay`，env 门控 `BLINK_STT_REAL_PSEUDO=1`；真实 Nano worker + 生产 `PseudoStreamingSttEngine` 按实时速率喂入 5 条长录音）：流式定稿真实发生（A/H 确认事件 2-6 次不等，H 的 1000ms 句长显著减少误切：b2cf 5→2 次）；最长的 25.9s 样本上 A 峰值未提交 10.5s / H 11.7s（均在各自 12/16s 上限内，兜底未被迫触发，与离线近似重放的 cap 差异相互印证）；松键收尾全部成功（finalize 179ms-912ms，终稿非空）；峰值缓冲 11.8s（约 1.9MB PCM）有界。报告 `target/stt-vad-real-pseudo-replay.json` 只含数值与匿名 id。

**首轮窗口评估结论**（限当时的 16 条语料；轻声短句反例见 §5.8.1）：

- **窗口默认值保持 8/12/12；当轮最短句长保持 800ms**——当轮收益只存在于 25s+ 连续不换气说话场景，代价（连续说话句尾定稿晚 2s、短句提前句尾延后）在日常停顿说话中不可接受，无一组候选在全部维度稳定占优。新增轻声短句反例后，800ms 的计数语义与默认值须重新验证，不沿用本结论作全场景依据。
- **"长语音"推荐预设（写入设置页提示文案的调参指引）**：`min_sentence_ms=1000`、`soft_window_s=10`（C 组合前两项，硬窗/上限保持 12/12）。适用于长时间连续听写（讲课/口述）场景；如需更大定稿异常兜底余量可上调 `hard_window_s/max_uncommitted_s` 至 14/16（H 组合），正常语料无行为差异。
- `soft_window_s` 不应低于 8（短尾段幻觉风险）；`max_uncommitted_s` 必须不小于硬窗口（sanitize 强制）。

**人工核对说明**：5 条新长录音的对照基准采用"整段无切分转录"（whole）作为自洽参考（文件名仅近似文本），每条录音的逐字文本与可接受停顿区间建议由用户复听复核后再固化到 manifest。

### 5.8.1 0.23.7.1：轻声短句停顿不定稿（✅）

**复现证据**：用户新增一条 15.76s 的私有实机录音（未列入 manifest，匿名 id `new_5ebac8df`，仅保留在 `testdata/stt/corpus/my-wavs/`）。原始 48kHz 双声道波形有约 7.04～8.38s（1.34s）和 10.83～12.42s（1.59s）两段明显低能量停顿；`private_corpus_vad_parameter_sweep` 用生产 `EnergyVad` 和共享音频规范化链路回放，默认 A 组合没有 `natural_silence` 切点，仅在 12.00s 由 `uncommitted_cap` 兜底。B/C 同样在 12s 兜底，G 在 10s、D/E 在 14s 兜底，F/H 在录音结束前无切点。报告位于本地 `target/stt-vad-parameter-sweep.json`，不入库。

**根因确认（0.23.7.1 执行）**：新增 env 门控诊断回放 `private_corpus_vad_frame_diagnostics`（`BLINK_STT_VAD_DIAG=1`，报告 `target/stt-vad-frame-diagnostics.json`，只含匿名 id 与数值），用生产 `EnergyVad` 按生产规范化 PCM 逐 10ms 帧导出 RMS、on/off 阈值、speaking、句长/静默/段长计数。`new_5ebac8df` 共 8 个 speaking 段，`sentence_samples`（**只计 RMS > on 阈值的帧**）峰值仅 50～610ms，全部 < 800ms 最短句长；每个停顿处静默都正常积累到 300ms 判定点（peak_sil=290ms → 触发"句子太短"分支），整句被重置而非切句——**主因是最短句长的计数语义，滞回区并没有阻塞静默计时**（停顿帧确实低于 off 阈值）。轻声语音帧能量大多落在 on/off 滞回区（on≈0.010、off≈0.005，语段 RMS 中位仅 0.0027、峰值 0.0223）。

**修正方案比较（可证伪）**：在帧数据上离线模拟三候选后用全语料验证——①段全时长计数（含句中静默）：在 case_10（非语音噪声，期望空）引入切点，证伪；②**非静默帧计数（RMS ≥ off 阈值，含滞回区）——选中**：切点是基线的严格超集（16 条语料无一自然切点丢失），无语音场景（case_09/10/11）与短词/远场/风扇（case_01/02/04/05）切点完全不变，咳嗽/键盘保护语义不变（非静默时长不足 800ms 仍不切）；③滞回区有界退出：本样本停顿未滞留在滞回区（静默计时正常推进），不需要；④相对底噪阈值调整：属调参掩盖，且 case_05/10/11 的保护依赖现阈值，放弃。实施为 `vad.rs::process_chunk` 的最小改动：句长累计条件从"高于 on"改为"speaking 且 RMS ≥ off"（约 10 行，窗口/cap/裁剪/协议全不动）。

**三段延迟量化（真实 `PseudoStreamingSttEngine` + Nano worker 实时回放，`RecordingTransport` 逐请求计时）**：`new_5ebac8df` A 组合两处自然切点（10.45s、14.24s，均在 12s 兜底前）——①停顿→切句：min_silence 量化 300ms（切点跟在拖尾轻声的静默积累之后，比声学长停顿起点早 0.3～0.4s）；②切句→定稿：finalize 请求在边界后立即发出（无 worker 排队；预览与定稿在客户端串行但预览已完成），推理耗时随段长 483ms（3.5s 段）/1066ms（10.45s 段）；③定稿→正文交付：commit 后下一喂入周期即可见（回放粒度 100ms；生产 cpal 10ms 块，≈10-20ms）。用户可见效果：第二处长停顿（10.79～12.43s）期间 ~11.5s 正文落定，末段在 14.9s（终停顿 14.64～15.76s 内）落定；松键收尾 finalize 248ms、终稿非空（52 字符 ≈ 正常听写速率，无短尾段幻觉特征）。

**回归**：修正后 A～H sweep 前后对照（`target/stt-vad-parameter-sweep{-baseline,-fixed}.json`）——A 组合新增自然切点仅 case_06@5520、case_08@2310、new_4d02@19390、new_ea3d@2330/9440 与 `new_5ebac8df` 的两处；B～H 同向；无自然切点丢失、无语音场景零切点。标注语料过真实引擎（默认 A）：case_09/10/11 流中零定稿请求（仅预览，静默快照被裁剪为空请求并失败，预存量行为）；case_03/06/07/08 流中 commit 正常、终稿字符数合理。已知残留（非本次引入）：Nano 对 case_09/10/11 全量静音的**松键终稿**仍输出 4 字符幻觉（terminal finalize 路径与本次改动无关，预存模型行为）；标注段数预期漂移——case_06 由 0 段修正为精确 1 段、case_08 由 1 修正为精确 2 段（均变为匹配），case_07 检测段数 1→2（新切点 16810ms 跟随 1670ms 真实停顿 + 1090ms 非静默语音，属正确"末句停顿即定稿"行为，manifest `expected_segments` 待用户复听后重基线）；case_01/02/04 段数失配为 0.22.15 自适应 VAD 以来的预存量。

**默认参数结论**：保持 `min_sentence_ms=800` 与 8/12/12 窗口不变——计数语义修正后 A 组合即达成验收，窗口/阈值无需调整；0.23.7 的"长语音预设 1000/10"建议继续有效（新语义下 B～H 同样只增不丢自然切点）。定向测试：`vad.rs` 新增 3 项（滞回区轻声达最短句长切句/短促轻声仍保护/句中静默不计入句长）+ `pseudo_streaming/tests.rs` 引擎级根因回归 1 项（轻声停顿经引擎产生边界并派发定稿）。

### 5.9 0.23.8：编辑器页内右键菜单 ✅

只在 `content-editor` 的 Source/MD 正文区提供自绘右键菜单，不改主窗口、钉图和便签，也不复用主窗口的独立弹窗。菜单挂在编辑器根节点以避开正文裁剪，并在窗口四边钳制位置；鼠标点击菜单时保留编辑焦点与选区，Esc 先收菜单。

剪切、复制、粘贴、全选按当前选区与可编辑状态出现；“整理选中内容”仅在非空选区、AI 可用且整理空闲时出现，直接进入既有 `transform.start("selection")`，后续仍须用户在 Diff 候选卡中确认。菜单动作在会话、视图、版本或选区变化后拒绝迟到执行；异步读取剪贴板返回后再次校验插入位置。只读 MD 预览只提供复制与全选。

---

## 六、验收

### 6.1 0.23.0

- [x] 原型能清楚回答“正在编辑什么、当前保存到哪里、当前是否录音或等待确认”。（`testdata/editor/prototype/index.html`：来源徽章+保存目标区+录音态/候选卡+协议调试条；交互链路已在浏览器 DOM 级验证）
- [x] corpus 覆盖中文、空格、列表、任务列表、引用、链接、代码块、嵌套标记、HTML、脚注和数学。（33 样本，`corpus/manifest.json` 期望标注）
- [x] 纯切换逐字符不变；支持、规范化、拒绝三类结果均有报告。（roundtrip 报告 §三/§四；纯切换保真由检查点保证，原型 `bytePreserved=true` 实测）
- [x] 冻结 session 所有权、状态切片、range handle、AI 全局并发和文件 revision。（§3.10 定案记录）
- [x] 得出文档总体 envelope 及 MD/Diff/AI 阈值；Source 超过增强阈值仍可编辑保存。（perf 报告：MD 32KB/128KB 分级门、Diff 200KB、AI 24K token、Source 2M 字符）

### 6.2 0.23.1

- [x] 任意时刻最多一个活动 EditorSession；不同来源新请求返回 `EditorBusy` 且不覆盖正文。（`decide_open` 纯决策 + 服务单槽；busy 激活现有窗口并携带 active_title，正文不动）
- [x] command 校验调用窗口、session_ref 和 generation；旧异步结果不能污染新会话。（get/commit/end 校验 label == `content-editor`；commit 副作用完成后按身份复查，迟到结果丢弃；事件带 sessionRef 供前端判别）
- [x] Source/MD 共用 Adapter；切换视图取消 AI 状态但不改变正文、来源或目标。（切换仅弃旧引擎、经检查点衔接正文；AI 状态 0.23.4 接入时挂入同一取消点）
- [x] 未编辑 MD 时原文逐字符不变；危险语法拒绝进入可编辑 MD。（检查点 + edited/suppress 标志；gate 对 33 样本语料逐样本回归对齐 manifest，且切换时按当前正文重估）
- [x] reset 后正文、undo、selection、监听、租约和异步状态均为空。（adapter.reset 销毁引擎实例——undo 栈与监听随实例消亡；session 清空会话身份；end 释放便签租约并广播）
- [x] `rg "PendingEditorPayload|save_policy" src frontend` 对编辑器旧协议命中为 0。
- [x] `rg "tauri::" src/domain/editor` 为 0。

### 6.3 0.23.2

- [x] 第一次保存剪贴板结果创建一条历史，后续保存只更新同一项；监听器不重复采集。（`plan_commit` 首存/更新分派 + `update_item_text` 保 id；`EditorResult` 自写标记 skip_persist，§5.3）
- [x] 保存到切换主目标；另存为副本、复制、创建便签和发送到 Chat 不切换目标。（`SaveToFile` 成功后置 `ConfirmedFile`；save_copy 与次级动作均不改 target）
- [x] 文件和便签外部 revision 变化不能被静默覆盖。（文件 identity(size+mtime) 冲突检查在写入闭包内，失配返回 `source_conflict` 并弹三选；便签走既有 revision 基线）
- [x] 保存失败、冲突或取消不改变正文、dirty 或主目标。（副作用全部成功后才按 `session_ref + generation` 复查落槽；任一失败早退，槽内状态不动）
- [x] 所有原子输出最终调用 Capability 唯一实现。（文本文件写统一 `write_text_file`，`save_text_file` 已收敛；剪贴板写统一 `domain::clipboard` 单一实现——与 `write_clipboard` Capability 同源）
- [x] dirty 关闭有三态保护；主动退出只有一次汇总确认，系统退出不阻塞。（三态 choiceDialog 保留；`request_user_exit` 请求-应答 + 前端去重 + 10s 超时兜底；系统关机走 `RunEvent::Exit` 不经确认）

### 6.4 0.23.3

- [x] G1/G2/G3/Editor 任意时刻最多一个 VoiceSession。（`session.recording` 单槽互斥；编辑器听写期间 hold 忽略、chat/编辑器互返 `voice_busy`；listen 侧 G1/G2/G3 均按 target 过滤）
- [x] confirmed segment 重复、乱序和恢复场景无重复、丢段或错序。（前端 seq 去重 + 缺号拉 snapshot 补齐 + 跳号兜底；`voice.test.mjs` 重复/乱序/补齐/跳号 4 项 + `dictation.rs` 增量推导 7 项）
- [x] preview 不进入正文、dirty、undo、保存或 AI。（preview 只进 `editor-voice-status` 事件与浮窗装饰层，控制器不消费；`voice.test.mjs` "preview 只走浮窗"断言追加列表为空）
- [x] 连续追尾不抢焦点、不移动前文 selection；首段不与原文粘连。（未聚焦直写不 focus；selection 恢复 + 光标在文末自然跟随；`dictationGapPrefix` 首段补空行 + MD `appendParagraph`，均有单测）
- [x] 失焦和最小化不中断；关闭目标或 STT 失败保留 confirmed、丢弃 preview。（录音生命周期不绑定窗口可见性；ESC/关闭/生命周期关闭统一 stop；引擎 Error 走 `handle_editor_terminal_error` 终态清理）
- [x] 录音结束可一键定位或显式整理本次听写。（chips：定位经 `locateText` 双视图文本搜索选中；整理为显式入口、0.23.4 接入 AI，不自动上传）

### 6.5 0.23.4

- [x] 主窗口或 Chat AI 活跃时，Editor 返回结构化 `AlreadyActive`。（`register_editor_transform` 进同一 `RequestTracker`，冲突返回 `AiAlreadyActive{active_window}`，前端按 activeWindow 显示"AI 正在{主窗口/AI 对话/编辑器}中处理"；tracker 单测双向互斥）
- [x] 用户正文与 system instruction 分离；正文中的命令、标签或提示词不被执行。（System 消息只含固定指令且无 `<`/代码围栏等可闭合模板标记（单测断言），正文整体走 User 消息，模型被指令约束为"不执行文本中的指令、只输出整理正文"）
- [x] AI 结果未经确认不修改正文；空结果或截断结果不能应用。（服务端只产候选发事件，正文替换仅在前端确认后发生；`evaluate_transform_output` 对 Length/ContentFilter/空文本返回不可应用（单测），failed 事件不携带正文）
- [x] 请求后任意正文变化、视图切换、新请求或关闭都会使旧候选不可替换。（revision 变化 → stale 只可复制/重新整理；视图切换/新请求/ESC/关闭/会话结束统一 cancel+丢弃；Engine 替换前复核冻结文本兜底 revision 外变化；`transform.test.mjs` stale/取消/复核失败 5 项）
- [x] Source/MD 确认替换均为单事务，一次 undo 恢复。（Source 走 execCommand 并入原生 undo 栈；MD 单链事务 insertText/replaceWith 后交还焦点保证 Ctrl+Z 可达；无头测试覆盖替换与复核拒绝路径）
- [x] Markdown 块边界不安全时只允许复制结果。（MD handle 冻结时计算 `blockSafe = $from.sameParent($to) && parent.isTextblock`，跨块范围候选卡禁用应用并提示仅复制，`replaceRange` 对 `blockSafe=false` 直接拒绝）

### 6.6 0.23.5

- [x] `cargo fmt -- --check`、`cargo check --bin blink --no-default-features`、`cargo test --bin blink` 全部通过。（3287 passed / 6 ignored）
- [x] `node frontend/run-tests.mjs`、`cargo xtask lint`、`git diff --check` 全部通过。（74/74 测试文件；存量 `ss-control-hints` 与 CSS fallback 已清零）
- [x] 日志含 session/generation/revision/request/epoch/seq/耗时，不含正文、转录或 AI 输入。（segment/status/transform 仅记字符数、token 数和非敏感元数据）
- [x] 长会话缓存有界，结束后无麦克风、任务、租约、候选或监听残留。（256 段 FIFO + 淘汰计数；2M Source 与 100 次 reset 压测；end 前后端双重清理）
- [x] 主窗口唤起、Focus、首结果和输入响应指标不退化。（未修改主窗口 P0 唤起/搜索热路径；全量 Rust hotkey/search 与前端主窗状态回归通过，真窗最短复核路径见交付说明）

### 6.7 0.23.6

- [x] 便签异步读取期间开始输入，返回结果不覆盖新输入；旧 generation 的同便签读取结果不能写入新会话。（`EditorSession.reloadFromSticky` 冻结身份 + 返回后重查 dirty；`editor-session.test.mjs` 4 项覆盖 dirty 丢弃 / stickyId 失配 / 同便签重开 / force 身份墙）
- [x] 正文前部存在与本次听写完全相同的文本、且听写结束后又继续输入时，“定位/整理本次听写”仍只命中文末本轮冻结范围；Source/MD 均有回归。（定位与整理均消费结束时冻结的 handle：Engine `locateRange` 右边界为冻结文末 + 冻结文本复核；`editor-adapter.test.mjs` 覆盖 Source 继续输入只选本轮 / 范围内被编辑失效 / 视图切换作废 / MD 侧 kind 分派契约）
- [x] AI start 响应前执行 ESC、切换视图、发起新请求、关闭或结束会话，request id 到达后会补发取消；连续两次“完成事件早于 start 响应”均可进入 candidate，不遗留旧 `runRequestId`，retired 状态有界。（候选确认分支不回填已完成 id + start 起步清空运行槽 + retired 有界 Map（上限 32）；`transform.test.mjs` 连续极早完成与有界回归 2 项）
- [x] commit 与 end 在同一会话内不可并发；保存中选择关闭或放弃不会产生确认之后的迟到外部写入。（前端 mutation 队列 + 入队身份捕获；后端 `MutationGate`，end 先取门；`editor-session.test.mjs` 排队 2 项 + Rust gate 串行化测试）
- [x] 后端拒绝旧 revision 和“同 revision、不同正文”；同 revision、同正文只有在提交意图完全相同时才幂等，随后显式“保存到/另存副本/覆盖/复制”均真实执行；`StaleRevision` 有服务测试和前端分类处理测试。（幂等键 = revision + 正文摘要 + `mutationId`，仅同一 mutation 精确重放早退；domain 协议回归 5 项（切换目标/副本/覆盖/重复复制/精确重放）+ 前端 mutationId 唯一性回归；`stale_revision` code 分类文案 + zh/en 词条测试）
- [x] snapshot 最早 seq 晚于 `last_seq + 1` 时明确提示缺段并继续消费可用段；客户端已越过淘汰区间时不误报。（`_resync` 首段连续性判定 + gapReported；`voice.test.mjs` 2 项）
- [x] `cargo fmt -- --check`、`cargo check --bin blink --no-default-features`、`cargo test --bin blink`、`node frontend/run-tests.mjs`、Tiptap corpus、`cargo xtask lint`、`git diff --check` 全部通过。（终验：Rust 3300 passed / 6 ignored、0 警告；前端 74/74 测试文件；corpus 33/33；`accessibility.test.mjs` EOF 空行已清）

### 6.8 0.23.7

- [x] 3 个高级参数可从设置页修改、保存、重置并正确投影到实际录音链路；旧配置无新增字段仍按 8/12/12 秒运行，非法值在前后端均被拒绝或安全归一化，中文/英文与键盘/读屏状态完整。（`VadConfig::sanitize` + 前端 `normalizeVadWindows` 双端同规则；`from_connection` 接线测试断言配置值到达 VAD 与 cap；`set_stt_config`/启动加载归一化持久化；i18n zh/en 7 条新词条 + aria-label 随语言刷新）
- [x] VAD 的低能量可切与强制切、伪流式最长未提交保护分别有定向测试；覆盖 10ms 分帧、短句、长静音、迟滞及 finalize 排队，不发生无限缓冲、重复或丢失片段。（`vad_custom_windows_replace_defaults`/`vad_custom_hard_window_defers_forced_cut` 窗口生效测试；`uncommitted_cap_fires_when_vad_timer_stuck_in_hysteresis`/`..._before_hard_window_and_waits_for_pending_finalize`/`..._resets_with_committed_base` 三个合成场景：滞回区按配置兜底、持续有声先于硬窗且定稿挂起不重复切、句尾交替按 committed 基点重计）
- [x] 5 条新增长句完成可复核的转写基准与可接受停顿标注；A～H 首轮候选在 5 条长句及 11 条已有标注样本上输出逐条切点、切片原因和准确度比较，并实际跑通入围组合的 GGUF Nano 全链路。（sweep 报告 `target/stt-vad-parameter-sweep.json` 含逐条切点时间/原因（natural_silence/soft_window/hard_window/uncommitted_cap）；Nano 评估双参考 CER（文件名近似 + 整段转录自洽参考）；真实 `PseudoStreamingSttEngine` + Nano worker 实时回放 A/H 全链路。逐字基准建议用户复听后固化，见 §5.8 人工核对说明）
- [x] 报告同时比较切点质量、CER、首段/收尾延迟、请求数及资源占用，写明最终默认值或保持基线的证据与回退条件；不记录敏感音频正文。（§5.8 矩阵含段数/强制切/CER；真实回放记录确认事件、峰值缓冲、finalize 耗时；worker 峰值 working set 约 1.34GB；所有报告只含数值与匿名 id）
- [x] 定向测试、`cargo fmt -- --check`、`cargo check --bin blink --no-default-features`、`cargo test --bin blink`、`node frontend/run-tests.mjs`、`cargo xtask lint`、`git diff --check` 通过；设置页真窗手测待用户复核（滑块联动/保存/重置已由 `voice-vad-windows.test.mjs` 静态与逻辑测试覆盖）。

### 6.8.1 0.23.7.1

- [x] 在 `new_5ebac8df` 上复现默认配置“预览可出字、无自然句尾、12s 兜底”的完整链路，量化规范化 PCM 的 VAD 状态、切句至定稿、定稿至正文交付各段耗时；报告只含匿名 id、数值与事件原因。（帧级诊断 `BLINK_STT_VAD_DIAG=1` 复现：8 个 speaking 段句长峰值 50-610ms 全部 < 800ms、每处停顿静默达 300ms 被重置；真实 worker 回放 `RecordingTransport` 拆出三段：300ms 静默量化 + 边界后 finalize 立即发出（零排队）+ 推理 483/1066ms + 交付 ≤1 喂入周期）
- [x] 确认最短句长、滞回区及 worker 排队各自对延迟的贡献，记录可证伪的修正方案与对照；至少一处明显长停顿应在 12s 兜底前自然定稿，且不能依赖缩短软/硬窗口达成。（主因=最短句长只计高于 on 帧；滞回区未阻塞静默计时；worker 排队贡献为 0。四候选对照后选中"非静默帧计数"，段全时长方案在 case_10 引入切点被证伪、滞回区退出与阈值调整无必要；两处自然切点 10.45s/14.24s 均先于 12s 兜底，窗口保持 8/12/12）
- [x] 用短词、轻声/远场、键盘/风扇/咳嗽/静音及 0.23.7 长语音语料比较漏切、误切、CER/幻觉、首段定稿延迟、请求数与资源；真实 `PseudoStreamingSttEngine` + worker 回放确认 confirmed 可及时交付，失败/空结果及松键 finalize 不丢文本。（16 条语料 sweep 前后对照：无语音场景与短词/远场/风扇切点不变、自然切点零丢失；标注语料过真实引擎：case_09/10/11 流中零定稿请求，case_03/06/07/08 commit 正常；A/H × 6 长录音 + 11 标注全链路通过，松键 finalize 220-847ms 终稿非空、峰值缓冲 ≤12.4s 有界、字符速率正常无幻觉特征；残留：Nano 对全静音 case_09/10/11 松键终稿 4 字符幻觉为预存路径）
- [x] 补定向回归并完成适用的 Rust/前端测试、格式、lint 和 diff 自审；报告未自动化的实机 GUI 复核及最终默认参数取舍，不提交私有语料或含正文的产物。（`cargo test --bin blink` 3349 通过 / 6 ignored；`cargo fmt -- --check`、`cargo check --bin blink --no-default-features`、`git diff --check` 通过；前端无改动未跑 node 测试；默认参数维持 800ms + 8/12/12。待人工：实机 GUI 复核轻声停顿落字体验、case_07 段数预期复听重基线、Nano 终段幻觉与预览空请求噪音归后续切片）

### 6.9 0.23.8

- [x] Source/MD 正文区右键显示页内菜单；无选区、只读、AI 未配置或整理忙碌时动作正确收敛；窗口四边定位不裁剪。（`context-menu.test.mjs` 覆盖动作门禁和四边钳制）
- [x] 右键“整理选中内容”复用现有候选确认流程，不直接改正文；Esc 先关闭菜单；切换视图、结束会话和关闭窗口清理菜单。（代码路径自审；前端 83 个测试文件通过）
- [ ] 真窗复核 Source/MD 的剪切、复制、粘贴、全选、整理选中及鼠标/键盘焦点；自动化测试未覆盖 WebView2 原生编辑命令与剪贴板权限。

---

## 七、关联文件速查（规划）

| 子版本 | 关键位置 | 改动 |
|---|---|---|
| 0.23.0 ✅ | `testdata/editor/markdown/corpus/`（33 样本 + `manifest.json`）、`lib/{engine,bench-lib}.mjs`、`run-{roundtrip,perf,diff-bench}.mjs`、`{roundtrip,perf}-report.md` | **新建**：语料/runner/报告 |
| 0.23.0 ✅ | `testdata/editor/prototype/index.html` | **新建**：单窗口布局原型（浏览器直接打开，Tiptap 走 vendor 相对路径，交互全模拟） |
| 0.23.0 ✅ | `docs/phases/0.23-editor-voice-ai-workflow.md` | §3.10 定案记录、里程碑与验收回写 |
| 0.23.1 ✅ | `src/domain/editor/mod.rs`、`src/app/editor.rs`、`src/app/commands/content_editor.rs` | **新建** session 类型/纯决策/EditorSessionService，**重写**四命令（旧协议删除） |
| 0.23.1 ✅ | `frontend/js/content-editor/{main,editor-session,editor-adapter}.js`、`content-editor/engines/{source-engine,markdown-engine,markdown-gate}.js` | main 仅装配；Session/Adapter/Engine 分离，gate 测试直接回归 testdata 语料 |
| 0.23.1 ✅ | `frontend/js/shared/tiptap-editor.js` | **新建**共享 Tiptap 封装（创建/parse/serialize + EOF 换行约定） |
| 0.23.2 ✅ | `src/domain/capability/builtins/write_text_file.rs`、`src/infra/utils/fs.rs`、`src/app/commands/search.rs`（save_text_file 收敛） | **新建**唯一文本文件写入 Capability（原子替换 + identity 冲突检查）与原子写原语；**收敛**导出对话写盘 |
| 0.23.2 ✅ | `src/domain/editor/mod.rs`（CommitTarget/plan_commit）、`src/app/editor.rs`（结果项/文件目标/退出确认）、`frontend/js/content-editor/{actions.js,main.js,editor-session.js}` | 类型化保存目标与纯决策；剪贴板结果项复用 + EditorResult 抑制源；底部保存区/更多菜单/冲突三选/退出汇总确认 |
| 0.23.2 ✅ | `frontend/js/sticky/main.js` | 编辑租约（0.23.1 已落地）；冲突重载 revision 基线由编辑器侧前移（本版无改动） |
| 0.23.3 ✅ | `src/domain/stt/dictation.rs`（**新建**段推导纯逻辑）、`src/app/voice.rs`（Editor target、continuous、epoch/seq、有界 snapshot、终态清理）、`src/app/editor.rs`（verify_active_session） | 听写段推导、连续 VoiceSession 与快照补齐 |
| 0.23.3 ✅ | `src/app/commands/content_editor.rs`、`src/main.rs`、`src/infra/event_names.rs`、`frontend/js/shared/{api,event-names}.js` | `start/pause/resume/stop_editor_voice`、`get_editor_voice_snapshot`、`focus_content_editor` 与两个 `editor-voice-*` 事件 |
| 0.23.3 ✅ | `frontend/voice-overlay.html`、`frontend/css/entries/voice-overlay.css` | 通用浮窗按 target/mode/phase 投影：拖拽/收起/暂停继续/结束/返回编辑器/设置 |
| 0.23.3 ✅ | `frontend/js/content-editor/voice.js`（**新建**）、`engines/{source,markdown}-engine.js`（append/locate）、`editor-adapter.js`、`main.js`、`content-editor.html`、`frontend/css/views/content-editor.css` | 听写控制器（去重/补齐/定位）+ 引擎追尾/定位 + 常驻麦克风与 chips |
| 0.23.4 ✅ | `src/domain/editor/transform.rs`（**新建**）、`src/domain/ai/message.rs`、`src/domain/ai/rig_provider.rs`、`src/domain/ai/chat_service.rs` | **新建**整理纯逻辑（输入门/max_tokens 公式/输出可应用判定/system instruction）；`FinishReasonKind` 类型墙投影；tracker 增加 editor 来源与 register/release（abort 拒绝 editor 请求） |
| 0.23.4 ✅ | `src/app/editor_transform.rs`（**新建**）、`src/app/commands/content_editor.rs`、`src/main.rs`、`src/infra/event_names.rs` | **新建** EditorTransformService（全局单活跃/取消路径唯一/只产候选）；`start/cancel_editor_transform` 命令与 `editor-transform-completed/failed` 事件；end 命令兜底清理悬空请求 |
| 0.23.4 ✅ | `frontend/js/content-editor/{transform,diff}.js`（**新建**）、`engines/{source,markdown}-engine.js`、`editor-adapter.js` | 候选状态机（冻结三元组/stale/确认事务/错误码分类）+ 稳定 Diff（Myers 回溯/D 熔断/200KB 门）；Engine range handle 签发与单事务 replaceRange（MD blockSafe 判定） |
| 0.23.4 ✅ | `frontend/content-editor.html`、`frontend/css/views/content-editor.css`、`frontend/js/shared/{api,event-names}.js`、`frontend/js/i18n/{zh,en}.js` | 非模态候选卡（stale 条/Diff 区/应用·复制·放弃）；整理入口 chips 与 IPC/事件常量、i18n |
| 0.23.5 ✅ | `src/app/{editor,editor_transform,voice}.rs`、`src/app/commands/content_editor.rs` | open/end 原子性与失败恢复；听写/整理取消、终态资源释放、结构化错误与非敏感诊断 |
| 0.23.5 ✅ | `frontend/{content-editor,voice-overlay}.html`、`frontend/js/content-editor/{main,actions,editor-session,voice}.js`、对应 CSS/i18n | 配置热刷新、键盘/ARIA/焦点、非颜色状态、迟到事件与窗口复用 reset 收口 |
| 0.23.5 ✅ | `src/infra/platform/window/windows.rs`、`frontend/js/content-editor/*test.mjs` | 四边工作区 clamp；2M Source、100 次 reset、snapshot 上限与可访问性回归 |
| 0.23.6 ✅ | `frontend/js/content-editor/{main,editor-session,transform,voice}.js`、`engines/{source,markdown}-engine.js`、`editor-adapter.js` | **落地**：便签回载身份墙、mutation 队列、AI 启动期取消追认（候选确认不回填已完成 id + start 清运行槽 + retired 有界 Map）、听写定位/整理均消费冻结 handle（Engine `locateRange` 冻结右边界）与快照缺口显式报告 |
| 0.23.6 ✅ | `src/app/editor.rs`、`src/app/editor_transform.rs`（无改动）、`src/domain/editor/mod.rs`、`src/app/commands/content_editor.rs` | **落地**：`MutationGate` 同会话串行门（end 改 async）、提交水位 + `check_commit_revision` 三态校验（幂等键含 `mutationId`，仅精确重放早退，显式输出真实执行） |
| 0.23.6 ✅ | `frontend/js/content-editor/*test.mjs`、`src/domain/editor/mod.rs`、`src/app/editor.rs` | **落地**：首轮四组前端回归（session 7 / voice 4 / transform 4 / adapter 2）+ Rust 提交协议 6 项与 gate 串行化 1 项；二次 Review 追加同 revision 显式输出协议 5 项、前端 mutationId 唯一性 1 项、连续极早完成与 retired 有界 2 项、听写冻结右边界 Source/MD 2 项 |
| 0.23.7 ✅ | `src/domain/config/stt_config.rs`、`src/domain/stt/vad.rs`、`src/domain/stt/pseudo_streaming/mod.rs`、`src/app/local_engine/funasr/{launch,corpus_runner,tests}.rs`、`src/app/commands/stt/mod.rs`、`src/main.rs`、`src/cli/commands.rs` | **落地**：`VadConfig` 三窗口字段 + `sanitize` 归一化（保存/启动/CLI 三入口）；`EnergyVad::with_params_and_windows`；伪流式 `max_uncommitted_audio_ms` 实例化；投影与接线测试；A-H sweep 与真实链路回放测试 |
| 0.23.7 ✅ | `frontend/settings.html`、`frontend/js/settings/tabs/{voice.js,voice-vad.js}`、`voice-vad-windows.test.mjs`、`frontend/js/i18n/{zh,en}.js`、`frontend/css/views/settings/settings-voice.css` | **落地**：切片窗口高级分组三滑块（联动钳制/恢复默认/aria-label i18n）；纯逻辑模块 + 前端定向测试；中英文词条 |
| 0.23.7 ✅ | `xtask/spikes/stt-vad/evaluate_nano.py`、`testdata/stt/corpus/my-wavs/`、`target/stt-vad-*.json`（运行产物，不入库） | **更新**：评估脚本升级 A-H 矩阵 + 双参考 CER + worker 峰值内存；5 条新增长录音语料；sweep/Nano 评估/真实回放三份报告 |
| 0.23.7.1 ✅ | `src/domain/stt/vad.rs`、`src/domain/stt/pseudo_streaming/tests.rs`、`src/app/local_engine/funasr/{corpus_runner,tests}.rs`、`testdata/stt/corpus/my-wavs/`（私有，不入库） | **落地**：句长计数语义修正（RMS ≥ off 阈值的非静默帧，含滞回区）；帧级诊断回放（`BLINK_STT_VAD_DIAG=1`）；真实回放扩展 transport 逐请求计时 + 离线边界/停顿时间线 + 标注语料真实引擎回归；VAD 3 项 + 引擎级 1 项定向测试 |
| 0.23.8 ✅ | `frontend/js/content-editor/{context-menu.js,context-menu.test.mjs,main.js,editor-adapter.js}`、`frontend/css/views/content-editor.css` | 编辑区页内菜单、双视图可编辑门禁、选区整理入口与定位/动作回归 |
| 全程 | `src/infra/event_names.rs`、`frontend/js/shared/{api,event-names}.js` | 类型化 IPC 与事件常量 |
| 全程 | Rust 模块测试、`frontend/js/**/*.test.mjs` | session、round-trip、保存、voice、Diff 竞态 |

### 7.1 子 agent 交付约束

每个子版本交付给子 agent 时，任务必须包含：本 phase、对应 frontend/backend/architecture spec、前置子版本已完成说明、允许修改文件、禁止改变的协议、必须新增的定向测试和 §六对应验收。子 agent 完成后必须自审 diff、运行定向测试并报告未自动化的 GUI 验收；不得顺手实现 §4.2 排除项。
