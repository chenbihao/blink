# Blink 0.19 — 能力闭环（从“有能力”走向“真实可用”）

> **范围**：补齐窗口、图片、便签和轻量文本闭环；统一 command/Capability 重叠语义；解耦图片编辑；开放受控自然语言设置；收敛 AI 设置、对话能力模式、Skill、MCP client 生命周期与 Blink MCP Server 主进程出口。
>
> **状态**：🚧 进行中（0.19.1–0.19.14 已完成）。
>
> **动机**：0.18 已有基础感知与执行能力，0.19 把高频链路收敛到可完成、可续读、可复用、可理解，并关闭窗口与 MCP 并发可靠性缺口。
>
> **前置**：0.18（含 0.18.7/0.18.8）。
>
> **原则**：用户入口与 AI 入口共享业务语义但保留各自协议；大数据走引用或分页；设置只开放显式白名单；纯对话只影响 Blink AI Agent 装配，不削弱其他 Capability 出口；Blink MCP Server 必须消费主进程的实时能力与运行时状态，不再复制最小运行时。

---

## 一、里程碑总览

| 版本 | 状态 | 内容 | 优先级 | 依赖 |
|---|---|---|---|---|
| **0.19.1** | ✅ | 删除能力 alias，统一 `open_url` 与 OCR 入口 | P0 | 无 |
| **0.19.2** | ✅ | 剪贴板图片、窗口列表与按窗口截图 | P0 | 无 |
| **0.19.3** | ✅ | 便签、pin、截图入剪贴板 | P0 | 0.19.2 |
| **0.19.4** | ✅ | ImageStash 图片引用与静态窗口建窗 single-flight | P0 | 0.19.2–0.19.3 |
| **0.19.5** | ✅ | 便签完整生命周期与受控文本读取 | P0 | 0.19.4 |
| **0.19.6** | ✅ | command/Capability 重复语义收敛 | P0 | 0.19.5 |
| **0.19.7** | ✅ | 来源无关的图片编辑入口 | P1 | 0.19.6 |
| **0.19.8** | ✅ | 自然语言读取/修改常用设置 | P1 | 0.19.6 |
| **0.19.9** | ✅ | AI 设置结构与运行时语义一致 | P1 | 0.19.6 |
| **0.19.10** | ✅ | 纯对话模式与 Skill 运行时开关 | P1 | 0.19.9 |
| **0.19.11** | ✅ | MCP client 连接生命周期 single-flight | P1 | 0.19.10 |
| **0.19.12** | ✅ | AI 档位/对话模式语义与便签、Skill、供应商设置体验收口 | P1 | 0.19.11 |
| **0.19.13** | ✅ | 主进程 Streamable HTTP MCP Server | P0 | 0.19.12 |
| **0.19.14** | ✅ | 截图后处理性能优化 + 控件吸附修复（pin 快路径 / raw IPC / swap 优化 / min_size 语义 / hint 闪烁修复） | P0 | 0.19.7 |
| **0.19.15** | ✅ | 剪贴板独立模式 + 搜索性能优化（LazyCopy / 覆盖索引 / WAL+mmap / 编辑窗口滚动条修复） | P0 | 无 |

**依赖关系**：0.19.1–0.19.4 建立感知、执行、图片引用与窗口可靠性基础；0.19.5–0.19.7 关闭业务闭环并收敛复用；0.19.8–0.19.12 完成设置、纯对话、Skill、MCP client 生命周期与体验收口；0.19.13 在完整主进程 wiring 之上建立对外 MCP 出口；0.19.14 依赖 0.19.7 的截图/编辑解耦基础，优化后处理性能瓶颈。

---

## 二、背景与动机

本版解决的核心问题是：能力虽然存在，但跨调用数据无法续接、便签与文本场景不完整、UI/AI 入口语义可能漂移、设置与扩展边界难以理解，窗口和 MCP 连接还存在并发重复创建。

### 2.1 明确不是问题的

| 项 | 结论 |
|---|---|
| 所有 command 都必须经 CapabilityRegistry | UI 状态机可保留 command；重叠业务语义必须共享实现。 |
| 大文本必须先建 TextStash | 可重放文件用绝对路径 + 有界分页更直接。 |
| 图片编辑必须立即 Capability 化 | 先解耦用户编辑会话；AI 编辑协议另行设计。 |
| 自然语言设置等于任意 KV | 只暴露稳定白名单，不开放 secret 或原始配置分片。 |
| 纯对话应关闭所有 Capability 出口 | 只改变 Blink AI Agent；CLI、内部调用与 Blink MCP Server 不变。 |
| MCP 状态读取可以顺便补连 | 状态与 tool pool 查询必须无副作用；连接只由明确意图触发。 |
| stdio 子进程是 MCP Server 的协议要求 | stdio 只是 transport 之一；它适合自包含 server，但不适合需要复用 Blink 实时插件、搜索、窗口和图片状态的主程序出口。 |
| Streamable HTTP 与 SSE 完全无关 | Streamable HTTP 可在单次响应中使用 SSE 流；本版不实现的是已废弃的独立 HTTP+SSE transport。 |

---

## 三、核心设计与决策

### 3.1 Capability 边界

图片生产者与消费者正交；`CapabilityEnv` 只暴露便签、pin 等必要的最小窗口桥接。Capability 不直接操纵 DOM、前端事件流或模态交互，但可产生打开路径、显示便签/pin 等操作系统级窗口副作用。通用约束见 `spec-architecture.md §A4`。

### 3.2 图片与文本数据流

- `ImageStash` 保持进程内、可复用、固定 15 分钟 TTL；最多 16 项、总计 64 MiB、单项 32 MiB。图片 Capability 的 canonical 结果仍是 Blob，agent 投影返回 `image_ref`，消费者统一解析引用/字节二选一输入。
- `read_text_file` 只接受绝对路径，提供 `metadata/range/head/tail`，正文按行有界读取；单次约 2,000 token 软预算、约 4,000 token 与 32 KiB 硬上限，返回行号、EOF、截断状态和续读游标。拒绝目录、二进制、非 UTF-8 与超长单行。

### 3.3 便签与重叠入口

便签更新用单调 `updated_at` 做乐观并发；不存在、已回收和版本冲突结构化区分。回收同时写库、关闭普通或 spare 窗口并刷新管理器；单纯隐藏/显示由独立 `set_sticky_visibility` 表达，不改变回收状态。`list_sticky` 明确返回 `visible/visibility`。剪贴板、pin、便签与 OCR 的公共校验/平台调用下沉为共享语义，command 与 Capability 只保留协议和 UI 会话差异。

### 3.4 设置与 AI 扩展

自然语言设置只开放 11 个高频可逆字段；写入携带预览旧值并做乐观并发，每次重新确认且不可记忆授权。设置页按“AI 供应商 / AI 对话设置 / AI 扩展”分区，移除没有真实运行时消费的开关；模型请求与 Capability deadline 统一使用 20 秒默认硬超时。

### 3.5 对话能力模式与 Skill

对话能力模式分为“全能力 / 主窗口仅对话 / 全局仅对话”，默认全能力；模式按 `ConversationKind` 决定是否装配内置 Capability、MCP client 工具和 Skill，并纳入 Agent cache key，不影响其他 Capability 出口。主窗口模型可跟随轻量档、主档或固定自选模型。原路由档泛化为超轻档，供标题命名等低成本短任务使用，标题默认绑定超轻档。Skill 总开关和单 Skill 禁用同时覆盖摘要、自动触发、`/skill` 显式激活及输入提示，配置变化即时清理运行时缓存。

### 3.6 窗口与 MCP 并发

- 所有静态窗口按 label 共用 `get_or_create` single-flight；“窗口对象存在”与“前端 ready”分开处理，首个 payload 使用 pending/pull 兜底。右键菜单预热与首次唤起也走同一入口。
- `McpClientManager` 按 server name 维护 generation、唯一连接任务、等待者通知和 30 秒自动失败冷却；不同 server 最多 3 个并发，单次握手/拉工具 15 秒，prompt 最多等待 5 秒。配置变化先推进 generation，旧 transport 不得提交；状态与工具快照查询保持只读。

### 3.7 Blink MCP Server 主进程化 —— Streamable HTTP（已定）

**定案**：Blink MCP Server 由常驻主进程托管，采用 MCP Streamable HTTP transport，在 `127.0.0.1:{port}/mcp` 提供单一 endpoint。旧 `blink mcp-server` 的“另起一个最小 Blink 进程”路径退出生产架构；0.19.13 不实现旧版 HTTP+SSE transport。

**理由**：独立 CLI 进程会重新构造数据库连接、CapabilityRegistry 与不完整的 `TauriDomainEnv`，无法共享主进程中的动态插件 Capability、`PluginEngine`、`SearchService`、窗口/便签状态与 `ImageStash`。主进程托管可让外部 Agent 调用用户当前正在使用的那一个 Blink，同时避免每个 MCP client 各启动一套运行时。

具体约束：

- **协议**：启用 rmcp 的 `transport-streamable-http-server`，复用现有 `BlinkMcpServer` handler、Capability → MCP 投影、暴露清单双重门禁与 `mcp_external` 审计；不自造 JSON-RPC/SSE 编解码。
- **监听边界**：socket 必须显式绑定 IPv4 loopback `127.0.0.1`，不得绑定 `0.0.0.0`、IPv6 wildcard 或局域网地址。首版按本机可信工具场景不引入 token/OAuth，但不配置宽泛 CORS，并校验 `Host`；请求带 `Origin` 时只接受 loopback 来源，降低 DNS rebinding 风险。
- **端口**：`McpServerModeConfig` 增加持久化端口字段，提供稳定默认值并允许用户修改；端口必须位于 `1024..=65535`。端口占用时进入可观察的错误状态，不得静默切换随机端口导致外部 client 配置漂移。
- **生命周期**：新增主进程 `McpServerRuntime`，在 CapabilityRegistry、插件引擎、搜索服务和 `TauriDomainEnv` 完成注入后按配置启动。设置页启停、修改端口与应用退出分别触发串行化 start/restart/stop；重复操作不得创建多个 listener，旧 generation 不得覆盖新状态。
- **实时暴露清单**：listener 使用主进程唯一 CapabilityRegistry。`exposed_capabilities` 更新后立即重建共享 tool snapshot，`tools/list` 与 `tools/call` 必须读取同一代快照并保留 call-time 二次校验；仅修改清单不重绑端口。
- **可观察性**：设置页展示 `disabled/starting/listening/error`、实际 endpoint、暴露 tool 数量和最近错误，并提供复制 Streamable HTTP 配置的入口。状态查询只读，不得隐式启动 listener。
- **CLI 边界**：`search/run/capabilities/config/chat` 是否保留独立 CLI 运行时不由本子版本决定；旧 `mcp-server` 子命令不得继续静默启动能力缩水的 server，应移除公开入口或返回指向主进程 HTTP endpoint 的明确迁移错误。stdio → HTTP 兼容桥若确有 client 兼容需求，后续独立设计。

---

## 四、复用与范围边界

### 4.1 已复用基础设施

| 基础设施 | 本版复用 |
|---|---|
| DWM 窗口枚举、截图 SESSION、图片历史 | 窗口感知、截图、图片来源与输出 |
| `StickyService`、窗口创建与 spare 机制 | 便签生命周期与 pin |
| CapabilityRegistry 与投影协议 | 用户/AI OCR、图片引用与多出口一致性 |
| ConfigStore、运行时热更新与 `CONFIG_CHANGED` | 设置页与自然语言设置共用写入语义 |
| Agent cache、SkillRegistry、MCP 配置存储 | 纯对话、Skill 门闩与连接生命周期 |
| `BlinkMcpServer`、MCP 正向投影与审计 | 复用协议 handler，只替换 server transport 与运行位置 |
| `TauriDomainEnv`、主进程 CapabilityRegistry | 直接共享完整插件、搜索、窗口、便签与图片运行时 |

### 4.2 不进 0.19

完整多模态投影、跨进程图片引用、TextStash、整文件读取/RAG、便签永久删除、图片编辑 Capability、任意配置 KV/secret/Provider/MCP 修改、Action/Chord/Capability 注册表合并、外部 subagent 与同步能力均后置。0.19.13 另外不包含 token/OAuth、TLS、局域网/远程监听、旧版 HTTP+SSE transport、自动端口发现和 stdio → HTTP 兼容桥。

---

## 五、分子版本实现摘要

- **0.19.1–0.19.3**：删除旧 alias；窗口/剪贴板图片感知、窗口截图、便签、pin 与截图入剪贴板进入统一能力体系。
- **0.19.4**：建立 ImageStash 跨调用引用；图片消费者统一输入；静态窗口创建按 label single-flight，首 payload 有 ready 兜底。
- **0.19.5**：完成便签读取、带版本修改与回收；新增轻量文本元数据/分页读取，并补齐文件搜索元数据。
- **0.19.6**：剪贴板、pin、便签与 OCR 的重叠业务语义各自收敛到单一实现。
- **0.19.7**：编辑会话从截图捕获状态解耦，截图、长截图与剪贴板图片共用标注/输出核心；用户会话与 ImageStash 隔离。
- **0.19.8**：建立 ManagedSetting 白名单、字段级更新、逐次确认与设置页共享热更新服务。
- **0.19.9**：AI 设置按真实职责重组，清理失效开关并统一超时/记忆语义。
- **0.19.10**：纯对话阻断工具和 Skill 装配；Skill 总开关、单项禁用与输入提示形成运行时闭环。
- **0.19.11**：MCP 预热、prompt、测试与配置操作共用 generation-aware single-flight；disabled 覆盖导入会同步停止旧连接。
- **0.19.12**：模型档位改为超轻/轻量/主并让命名默认走超轻档；主窗口模型策略与三态能力模式落地；便签显隐语义、管理器辨识度、Skill hint 对比度和供应商预设折叠收口。
- **0.19.13**：将 Blink MCP Server 从独立最小 CLI 进程迁入常驻主进程，以 loopback Streamable HTTP 暴露实时 Capability；补齐 listener 生命周期、配置热更新、运行状态与设置页 endpoint 复制入口，并停止宣传旧 stdio 子进程路径。
- **0.19.14**：截图后处理性能优化与控件吸附修复。性能方面完成四轮优化（blink-pin 协议替代 base64 / raw IPC 替代 JSON 序列化 / u32 指针 swap / DWM cloak 替代 hide-show），全屏 pin 从 1280ms→336ms（3.8x），有标注复制从 3596ms→699ms（5.1x）。控件吸附方面修复 `min_size` 语义（从「只跳过展开」改为「完全跳过」）和窗口 hint 闪烁（hit-test-only + 延迟显示）。
- **0.19.15**：Alt+C 剪贴板模式 bypass SearchService pipeline，前端直接调 `search_clipboard` IPC；搜索路径改为 LazyCopy（不预载 text，激活时按需拉取）；DB 层加覆盖索引 + WAL + mmap + cache_size，315ms→个位数 ms；编辑窗口 textarea 滚动条与 Tiptap 统一。

### 5.1 0.19.13 实施顺序

1. **协议与配置底座**：为 rmcp 启用 Streamable HTTP server feature；扩展 `McpServerModeConfig` 的端口字段、默认值、反序列化兼容与校验，保留现有 `enabled/exposed_capabilities` 数据。
2. **handler 实时化**：让 `BlinkMcpServer` 共享可热更新的 exposure snapshot，移除单实例静态 `cached_tools/config` 造成的旧会话漂移，并为 `tools/list` / `tools/call` 钉住同代双重门禁。
3. **主进程生命周期**：在 app 层实现 `McpServerRuntime` 与 loopback HTTP listener；完成启动 wiring、串行 apply、代际防竞态、取消与退出回收，再挂入设置保存命令。
4. **状态与设置页**：增加只读 runtime snapshot IPC，展示 endpoint、状态、tool 数和错误；端口编辑与复制连接配置使用同一份 canonical endpoint。
5. **旧入口收口**：移除或显式阻断 `blink mcp-server` 独立 server 路径，更新 CLI help/错误文案；不借机重构其他 CLI 子命令。
6. **协议与竞态验收**：用真实 rmcp Streamable HTTP client 覆盖协议闭环，并补齐热更新、快速启停、端口冲突、非法来源和退出释放测试。

### 5.2 0.19.14 截图后处理性能优化 + 控件吸附修复

**性能优化**：通过日志实测定位并解决了四个瓶颈——Tauri IPC JSON 序列化、逐字节 R↔B swap、overlay hide DWM 重组、session_png lazy 编码。四轮优化后全屏 pin 336ms（3.8x）、有标注复制 699ms（5.1x）、快路径 copy 67ms（4.5x）。剩余瓶颈（PNG 编解码往返）待 raw BGRA 协议优化。

**控件吸附修复**：① `min_size` 语义从「只跳过展开子树」改为「完全跳过（不收集 + 不展开）」；② 窗口 hint 闪烁修复——mousemove 中 `updateWindowHover` 拆为 hit-test-only + 延迟显示，控件 hit-test 结果决定窗口 hint 显隐。

**后续方向**：raw BGRA 协议（跳过 PNG 编解码）、交互优化（`resetState` 立即画暗罩）、钉图快路径 raw 协议、有标注复制 `getImageData` 直传。

---

## 六、验收结论

- 图片可沿“截图/剪贴板 → `image_ref` → OCR/pin/写剪贴板”连续调用，引用有固定资源上限。
- 便签可完成创建、读取、带版本更新和可恢复回收；文本文件可按模式有界续读。
- 截图与剪贴板图片进入同一编辑核心，输出后正确结束各自会话。
- 自然语言设置不越过白名单，逐次确认且与设置页运行时状态一致。
- 纯对话不装配工具或 Skill；disabled Skill 不出现在 `/skill` 输入提示中。
- MCP 同名并发连接只提交一个 generation；禁用、删除、覆盖配置不会留下旧工具快照。
- 自动化基线以本版收尾 review 的 `cargo test --bin blink`、前端相关 `.test.mjs`、JS 语法检查、`cargo check --bin blink` 与 `git diff --check` 结果为准；GUI/Win32 行为按最短手动链路验证。

### 0.19.14 验收

性能优化 P0-P3 已完成（blink-pin 协议 / u32 swap / raw IPC / DWM cloak），控件吸附修复已完成（`min_size` 语义修正 + 窗口 hint 闪烁修复）。第二轮优化（raw BGRA 协议、交互优化等）后置。

### 0.19.15 验收

剪贴板独立模式（Alt+C bypass pipeline + LazyCopy + 覆盖索引 + WAL/mmap/cache）已完成，编辑窗口滚动条已修复。`cargo check` 无警告。

---

## 七、关联文件速查

| 范围 | 关键文件 |
|---|---|
| 图片引用与消费者 | `src/domain/capability/{image_stash,result}.rs`、`builtins/{image_input,ocr_image,pin_image,write_clipboard}.rs` |
| 窗口 single-flight / ready | `src/infra/platform/window/windows.rs`、`src/app/commands/search/context_menu.rs`、`frontend/contextmenu-popup.html` |
| 便签与文本读取 | `src/domain/sticky/mod.rs`、`src/infra/data/sticky.rs`、`src/domain/capability/builtins/{read_sticky,update_sticky,trash_sticky,read_text_file}.rs` |
| 共享语义与图片编辑 | `src/domain/clipboard.rs`、`src/app/domain_env.rs`、`src/infra/platform/image_editor.rs`、`frontend/js/screenshot/` |
| 自然语言设置 | `src/domain/config/managed_settings.rs`、`src/app/setting_service.rs`、`src/domain/capability/builtins/{get_settings,update_setting}.rs` |
| AI 设置 / 纯对话 / Skill | `src/domain/{config/ai_config,ai/chat_service,ai/skill}.rs`、`frontend/js/settings/tabs/ai/`、`frontend/js/chat/{composer,main}.js` |
| MCP 生命周期 | `src/domain/mcp/client.rs`、`src/app/commands/mcp.rs`、`frontend/js/settings/tabs/mcp*.js` |
| MCP Server 主进程 HTTP 出口（0.19.13） | `Cargo.toml`、`src/domain/mcp/{server,server_config}.rs`、`src/app/mcp_server_runtime.rs`（新建）、`src/app/commands/mcp.rs`、`src/main.rs`、`src/cli/{mod,commands}.rs`、`frontend/js/settings/tabs/mcp-server.js` |
| 截图性能优化与控件吸附修复（0.19.14） | `src/app/commands/search/media.rs`（pin 快路径 / raw IPC / timing logs）、`src/infra/platform/screenshot/mod.rs`（swap 优化 / `session_rgba` 待实现）、`src/infra/platform/clipboard/mod.rs`（swap 优化）、`src/infra/platform/window/windows.rs`（blink-pin 协议 / overlay DWM cloak）、`src/main.rs`（blink-pin 协议注册）、`src/infra/platform/uia.rs`（min_size 完全跳过过滤）、`src/domain/config/shards.rs`（control_snap_min_size 注释）、`frontend/js/shared/api.js`（raw IPC）、`frontend/js/screenshot/index.js`（mousemove hint 显隐逻辑重排）、`frontend/js/screenshot/ss-hover.js`（skipShowHint + showWindowHintIfPending / hideWindowHintIfVisible）、`frontend/js/screenshot/ss-output.js`（doPinSelection 快路径）、`frontend/js/i18n/{zh,en}.js`（label 改为「最小识别尺寸」） |
| 剪贴板独立模式与搜索性能优化（0.19.15） | `frontend/js/main-window/clipboard-mode.js`（模式状态机）、`src/app/commands/clipboard.rs`（`search_clipboard` / `get_clipboard_text`）、`src/domain/search/clipboard_engine.rs`（LazyCopy + 并行查询 + trace timing）、`src/domain/search/engine.rs`（`SearchAction::LazyCopy`）、`src/domain/search/service.rs`（`search_clipboard_mode`）、`src/infra/data/clipboard.rs`（`ClipboardMeta` / `query_recent_meta` / `query_recent_days_meta` / `get_text_by_id` / 覆盖索引）、`src/infra/data/clipboard_images.rs`（`ClipboardImageListItem` / `search_image_list` / 覆盖索引）、`src/infra/data/pools.rs`（`SqliteConnectOptions` + WAL + mmap + cache）、`frontend/css/views/content-editor.css`（textarea 滚动条）、`frontend/js/main-window/results.js`（hitId 去重键） |
