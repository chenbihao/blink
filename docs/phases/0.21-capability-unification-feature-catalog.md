# Blink 0.21 — Capability 唯一执行入口与统一功能目录

> **范围**：建立调用来源、运行时与出口策略类型墙；将旧 `execution::Action` 分流为 Capability / Interaction / descriptor；统一 Search、Chord、菜单、AI、CLI、MCP 的原子执行落点；提供功能目录和 AI/MCP 授权管理。
>
> **状态**：🚧 进行中（0.21.0–0.21.10 已完成；0.21.11–0.21.14 审查收尾规划中）。
>
> **动机**：本地入口与 AI 曾分属 Action/Capability 两套执行体系，metadata、真实执行目标、风险策略和设置状态可能漂移。本版把 Capability 收敛为唯一原子执行目标，持续人机流程留在 Interaction，入口只保留 descriptor/binding。
>
> **前置**：0.14 Capability 协议、0.17.8 AI 确认记忆、0.19 能力闭环、0.20.0 统一可见错误反馈。
>
> **原则**：架构与信任边界以 [`spec-architecture.md`](../specs/spec-architecture.md) 为准；代码级出口策略是硬上限，用户授权不得绕过风险确认；功能、Capability、binding id 分栏；本地 binding 保留各自真源；插件不得取得通用 Blink UI 控制权。

---

## 一、里程碑总览

| 版本 | 状态 | 内容 | 优先级 | 依赖 |
|---|---|---|---|---|
| **0.21.0** | ✅ | Policy、InvocationOrigin、RuntimeRequirement、SurfacePort 类型墙与 invoke-time 门禁 | P0 | 无 |
| **0.21.1** | ✅ | 13 个内置 Action 迁为 Capability，存量 Capability 补 policy | P0 | 0.21.0 |
| **0.21.2** | ✅ | 6 个 Chord Action 分流为 Capability / Interaction / binding | P0 | 0.21.1 |
| **0.21.3** | ✅ | ResultAction/SearchAction 副作用收敛，删除双 Registry fallback | P0 | 0.21.2 |
| **0.21.4** | ✅ | FeatureCatalog 聚合、稳定身份、来源可用性与 binding adapter | P1 | 0.21.3 |
| **0.21.5** | ✅ | AI 推荐 allowlist、缓存失效、MCP policy 双门禁与热更新 | P0 | 0.21.4 |
| **0.21.6** | ✅ | 设置页“功能”信息架构首版；实现缺陷由 0.21.8 纠正 | P1 | 0.21.5 |
| **0.21.7** | ✅ | 删除 `execution` 遗留并切换架构文档为落地态 | P0 | 0.21.6 |
| **0.21.8** | ✅ | 三出口矩阵重做，AI/MCP 真源投影与唯一管理入口 | P1 | 0.21.7 |
| **0.21.9** | ✅ | 目录视觉重设计、组级按列批量、幽灵 token 与死关键词修复 | P1 | 0.21.8 |
| **0.21.10** | ✅ | 能力与操作页打磨、可读标题与排序、MCP 安全能力默认暴露 | P1 | 0.21.9 |
| **0.21.11** | 📋 | AI 授权种子闭环、CapabilityTool 统一经 Registry invoke | P0 | 0.21.10 |
| **0.21.12** | 📋 | 前端测试 runner、存量失败、clippy 与迁移 dead code 收尾 | P1 | 0.21.11 |
| **0.21.13** | 📋 | 本地 Capability 参数协议收敛、ResultAction 去 id 特判、Registry identity 硬化 | P1 | 0.21.11 |
| **0.21.14** | 📋 | DomainEnv 小接口拆分与 infra → domain 反向依赖清理 | P1 | 0.21.13 |

0.21.11 是授权与唯一执行语义的 P0 收口；0.21.12 可在 0.21.11 后独立推进，0.21.13–0.21.14 串行完成协议与分层架构收敛。

---

## 二、背景与动机

### 2.1 本版解决的问题

- 原子副作用曾同时存在 Action 与 Capability 两套实现和 Registry fallback。
- Search/Chord/Menu 的触发 metadata 与实际执行、风险、授权缺少统一身份投影。
- AI/MCP 需要独立授权真源、运行时门禁与热更新，设置页则需要一个面向用户的统一目录。

### 2.2 明确不是问题的

- Capability 负责一次原子调用；截图、取色、编辑、录音等持续会话继续属于 Interaction，不机械包装成 Capability。
- Copy/Preview 等展示短路径可留在 ResultAction；Open/Reveal 等业务副作用必须落到 Capability。
- Search、Chord、Context 和插件生命周期继续使用各自 binding/config 真源；功能目录只聚合与批量适配，不制造第二份总开关。
- CLI `capabilities` 保留 CapabilityRegistry 语义，无需改成 FeatureCatalog 展示投影。

---

## 三、核心设计与决策

### 3.1 Capability 是唯一原子执行目标（已定）

所有原子副作用最终经 `CapabilityRegistry::invoke`。Search/Chord/Menu 只保存 descriptor/binding 和 target；Interaction 状态只存在于所属领域。旧 `src/domain/execution/`、ActionRegistry 和双 fallback 已删除。

### 3.2 Policy 与运行时是 invoke-time 类型墙（已定）

`CapabilityPolicy` 是 origin、runtime、danger、sensitive、AI/MCP 默认与确认策略的唯一真源。调用者必须携带真实 `InvocationOrigin` 和 `RuntimeCapabilities`；Registry 在实际 invoke 前检查，UI 隐藏和 tool list 过滤仅是前置投影。GUI 能力只获得语义化 `SurfacePort`，插件能力永远拿不到该端口。

### 3.3 功能、Capability、binding 身份分栏（已定）

一个 Feature 可以聚合一个 Capability target 和多个本地 binding。功能目录分别展示 `feature_id`、`capability_id`、`binding_id`；本地列投影 binding 状态，AI/MCP 列投影 Capability 授权状态。旧 Chord `screenshot` 配置键作为 migration alias 保留，不与 headless `screenshot` Capability 混淆。

### 3.4 AI 与 MCP 授权独立（0.21.10 修订）

- AI 首次缺少配置时生成推荐 allowlist：普通 Safe（含 sensitive）默认进入 tool 池，Dangerous、local-only、诊断采集/恢复默认关闭；sensitive 调用仍需确认。生成后以持久化集合为真源，新增能力不自动加入老用户配置。
- MCP 的 `exposed_capabilities` 是独立真源，list-time 与 call-time 都复核 policy。Dangerous、Forbidden 和不允许 `Mcp` origin 的能力不可由配置绕过。
- **0.21.10 策略翻转**：MCP 不再“一律默认关”。首次没有种子记录时，静默持久化“允许 MCP 来源、非 Dangerous、非 sensitive、非 Forbidden”的安全能力；写入 `exposure_seeded=true` 后不再自动补新能力，用户清空列表也不会重生成。

### 3.5 功能目录是真源投影，不复制策略（已定）

FeatureCatalog 聚合 builtin descriptor、Chord binding、builtin/plugin Capability，并投影 schema、policy、availability 与 AI/MCP 真源。未知或已移除 id 保留为可诊断残留。目录页是 AI/MCP 授权的唯一完整管理入口；MCP Server 页只保留状态、Endpoint、暴露摘要和深链。

### 3.6 关键纠偏与维护约定

- 0.21.6 首版曾按臆造的 IPC 字段路径渲染，三出口状态和组级操作不可用；0.21.8 已按 `capability_projection`、`bindings` 和真实枚举契约重做。后续不得恢复顶层假字段。
- Context 门禁动作的关键词不能单独唤起；目录显示环境触发条件并沿用 action 级开关，不制造 ContextBinding 第二开关。
- 新增内置 Capability 时必须同步补 `CAPABILITY_TITLES` 的中英文短名；schema 长句只进入 description，未收录 id 才回退人类化。

---

## 四、复用与范围边界

### 4.1 已复用基础设施

| 基础设施 | 本版用途 |
|---|---|
| `CapabilityRegistry` / `CapabilityPolicy` | 唯一调用目标与代码级门禁 |
| `ConfigStore<T>` / `blink://config-changed` | AI/MCP 授权持久化、热更新和目录刷新 |
| Search/Chord/Context DisableConfig | 本地 binding 真源与批量 adapter |
| AI Agent cache fingerprint | 撤销授权后使旧 Agent cache 失效 |
| MCP ExposureSnapshot | 暴露集合排序、list/call 双门禁和热更新 |
| 前端 CSS token / Lucide sprite / 通用 switch、button | 目录矩阵、风险提示和链接按钮 |

### 4.2 不进入 0.21 的项

| 项 | 理由 |
|---|---|
| MCP Dangerous 逐次确认协议 | 本版代码级禁止；未来需独立 session/确认通道设计 |
| 任意插件控制 Blink 窗口或事件 | 违反最小权限与信任边界 |
| 统一所有本地 binding 为一个总开关 | 会丢失 Search/Chord/Context 各自语义 |
| 巨型 `system_operation` / `open` Capability | 会损失单项授权、确认和审计粒度 |
| descriptor 运行时动态包装为 AI tool | AI tool 只能来自代码允许且用户授权的 Capability |

---

## 五、分子版本实现总结

### 5.1–5.3 策略墙与执行收敛 ✅

建立 origin/runtime/policy/SurfacePort；把 13 个内置 Action 和 6 个 Chord 行为逐项迁为 Capability、Interaction 或 binding；Open/URL/Reveal 等结果副作用改走 Capability；删除 Registry fallback，保留必要 wire/config 兼容壳。

### 5.4 FeatureCatalog 后端 ✅

新增目录类型、聚合器、binding adapter 和 IPC。目录聚合 builtin descriptor、Chord、builtin/plugin Capability，按稳定身份去重并投影 availability、binding 与 policy；写操作仍落到原配置真源。

### 5.5 AI allowlist 与 MCP 门禁 ✅

新增 AI 推荐集合持久化和批量命令，Agent cache key 纳入授权 fingerprint。MCP ExposureSnapshot 与 call-time 同时过滤 policy 和暴露集合，配置变更可热更新并广播目录刷新。

### 5.6 设置页首版 ✅（已由 0.21.8 取代）

建立“功能”分组和能力目录入口，移除插件页内置动作卡片与 MCP Server 重复清单。首版字段契约和交互未真正接通，最终实现以 0.21.8 为准。

### 5.7 删除遗留与文档切换 ✅

删除整个 `src/domain/execution/`、Action 类型与 wiring；AI tool 投影迁入 capability 域；product、architecture、roadmap 和 0.14 文档从目标态改为 0.21 已落地。

### 5.8 三出口矩阵重做 ✅

能力与操作页改为本地/AI/MCP 三出口矩阵，按真实 IPC 契约渲染并写回三份真源；增加组合过滤、Chord 深链、高级详情、竞态防护和中英文静态契约测试。MCP Server 摘要改为目录深链。

### 5.9 目录视觉重设计 ✅

修复未定义 CSS token 和 Context 死关键词；改为单表 sticky 表头、组分隔行与按列三态批量开关；筛选工具栏、文本层级、默认徽章降噪和无匹配状态统一落到真实主题 token。

### 5.10 0.21.9 视觉续修 ✅

目录改为独立 sticky 表头 + 纵向组块矩阵；新增风险图标列、危险开关警示色、姿态摘要与“恢复推荐”。组级小号 switch 仅在 hover/focus-within 显示，binding chip 内联并截断；MCP 摘要本身成为超链，通用 `.btn-link` 补齐并复用于快捷键“修改”。

### 5.11 0.21.10 — 目录打磨与 MCP 默认姿态 ✅

- capability-only 行使用 `CAPABILITY_TITLES` 中英文短名，schema 描述落到 description；插件能力同样拆分标题/描述。组内按 Chord → Plugin → Builtin/BuiltinCapability → title 排序。
- 语音 Chord 的空格键统一显示为 `Alt+Space`；binding chip 单行缩略，description 降至 faint 层级；MCP Server 双分割线与标题行 binding 布局修正。
- MCP 配置新增 `exposure_seeded`，首次静默生成安全能力暴露集合并持久化；旧版非空列表原样保留并只补 seeded 标记，空列表在 seeded 后保持用户选择。启动 wiring 与种子/迁移/幂等测试已补。
- 前端推荐态与后端一致：恢复推荐会补加 safe 能力并移除 sensitive/dangerous，确认文案同步为“MCP 仅暴露安全能力”。
- 新增聚合排序、Space label、MCP 默认生成和前端推荐 diff 测试。全量验证：Rust 1742 passed / 5 ignored；前端 203/205，2 个 screenshot 存量失败与本版无关。

### 5.12 0.21.11 — 授权种子与唯一 invoke 收口 📋

- 为 AI Capability allowlist 增加独立的首次种子状态，不能再用“推荐 profile + 空集合”推断未初始化；用户主动关闭全部能力后，跨重启必须保持纯对话姿态，只有显式“恢复推荐”才能重新填充。
- `CapabilityTool` 保留 AI 危险/敏感确认适配，但确认通过后的真实调用必须经 `CapabilityRegistry::invoke`，删除适配器内重复的 origin/runtime 门禁，统一来源检查、运行时检查、SLO 与 perf 埋点。
- tool 池仍在 list/build 阶段按代码 policy 与用户 allowlist 过滤；invoke-time Registry 门禁是不可绕过的最终边界。

### 5.13 0.21.12 — 验证可信度与迁移收尾 📋

- 新增统一前端测试 runner：遍历测试文件、累计所有失败并以非零退出；不得再由 PowerShell pipeline 最后一个进程掩盖中途失败。
- 核实并修复当前 `image-editor-integration`、`ss-selection-geometry`、`scroll/replay` 失败，若确属预期差异则更新契约与理由，禁止仅放宽断言。
- `cargo clippy --bin blink --all-targets -- -D warnings` 必须通过；清理 0.21 迁移产生的未使用 import、悬空 doc comment、未消费 helper/enum variant/常量和过时注释。
- 测试数量只作观测，不写死为验收真源；验收以零失败、零 warning、真实非零退出传播为准。

### 5.14 0.21.13 — 本地协议与身份契约硬化 📋

- Builtin `ParamSource` 直接生成 Capability schema 所需的最终 JSON object，删除 `convert_legacy_arg_to_capability_args` 兼容桥；wire 层不再按 capability id 猜参数形状。
- 诊断文本复制改由显式 ResultAction/结果投影表达，删除 `run_builtin_action` 中按诊断 capability id 写死的剪贴板副作用；本地入口按 Capability 语义更名，必要的旧 IPC 名仅作为无业务逻辑兼容 alias。
- Capability 重复 id 在测试/debug 启动时 fail fast，release 产生可诊断的启动错误；不得由 inventory 遍历顺序静默决定生效实现。
- 增加 descriptor 参数 → Registry invoke、ResultAction → Capability、重复 identity 三类契约测试。

### 5.15 0.21.14 — DomainEnv 与分层依赖收敛 📋

- 将 `DomainEnv` 按消费者拆为最小 port，至少分离领域事件、Chat runtime、截图 Interaction、应用生命周期；Capability 继续只能取得 `CapabilityEnv` 与显式 `SurfacePort`。
- 将 `infra/platform/window/windows.rs` 中 Tauri managed-state 定位和 Search/Chat/Sticky 业务编排上移到 app/wiring；infra 只保留参数化 Win32 primitive，不反向定位领域服务。
- 清理 `infra/data/config.rs`、`infra/platform/context` 等对 domain 类型的直接依赖：共享纯数据下沉到无反向依赖的位置，业务配置适配留在 app/domain。
- 迁移分批保持 Alt+Space → focus → input 主链路行为与性能不变；不得以巨型通用 dispatcher 替代旧 God Interface。

---

## 六、验收

- [x] 原子副作用统一经 CapabilityRegistry；`src/domain/execution/` 与双 Registry fallback 已删除。
- [x] Registry invoke-time 检查 origin/runtime/policy；AI/MCP 授权不能绕过代码级禁止。
- [x] Search/Chord/Menu 不复制业务实现；Interaction-only 项不制造空 Capability。
- [x] 功能、Capability、binding id 分栏，本地/AI/MCP 分别写回各自真源。
- [x] 目录按真实 IPC 契约渲染，代码禁止/Interaction-only/无本地入口显示不可操作状态。
- [x] MCP Server 无第二份完整能力清单，摘要与目录授权状态联动。
- [x] 0.21.10 MCP 首次默认仅暴露安全能力，seeded 后不自动重建；前后端恢复推荐策略一致。
- [x] `cargo test --bin blink`：1742 passed / 0 failed / 5 ignored。
- [x] 前端 Node 全量测试：203/205，只有 2 个 screenshot 存量失败，无新增失败。
- [x] `git diff --check`、编译诊断与 diff 自审通过。

### 0.21.11 验收

- [ ] 用户关闭全部 AI Capability 后重启，allowlist 仍为空；显式恢复推荐后才重新生成。
- [ ] 缺失旧配置的首次升级只生成一次推荐集合；新增 Capability 不自动进入已 seeded 用户配置。
- [ ] 生产代码中除 `CapabilityRegistry` 内部外不存在直接 `cap.invoke(...)` 调用。
- [ ] Local AI 的 Safe、sensitive、Dangerous、origin denied、runtime unsupported、timeout 与 cancelled 均覆盖 Registry 调用测试和统一 perf 分桶。

### 0.21.12 验收

- [ ] 统一前端测试命令在任一测试文件失败时最终退出码非零，并能汇总全部失败文件。
- [ ] 前端全量测试零失败；不保留“pipeline 成功但中途 assertion 失败”的假绿状态。
- [ ] `cargo test --bin blink` 零失败，`cargo clippy --bin blink --all-targets -- -D warnings` 通过。
- [ ] 0.21 相关未使用兼容 helper、死 enum variant、悬空注释和临时性能日志已清理或有仍被消费的证据。

### 0.21.13 验收

- [ ] `rg "convert_legacy_arg_to_capability_args|blink_print_debug_info.*blink_debug_inithook" src/app/commands/search.rs` 无业务特判命中。
- [ ] 所有 builtin descriptor 产出的参数可直接传给对应 Capability schema/invoke，无 command 层二次猜测。
- [ ] 重复 Capability id 的测试确定性失败并指出冲突 id；正常注册顺序稳定。
- [ ] 本地搜索、Context、Chord、右键结果操作烟测无回归。

### 0.21.14 验收

- [ ] `src/infra/` 不再直接引用 `crate::domain` 或 `crate::app`；例外必须在 spec 中有明确边界决策，本子版本不默认新增例外。
- [ ] `DomainEnv` 不再暴露未被生产消费者使用的方法，各消费者只依赖所需最小 port。
- [ ] `infra/platform/window/windows.rs` 不再通过 Tauri state 定位 SearchService、ChatService、StickyService 或领域配置。
- [ ] `cargo test --bin blink`、clippy、前端全量测试通过；手动验证 Alt+Space 唤起、自动 focus、直接输入、ESC/失焦隐藏。

手动烟测仍应覆盖本地搜索、Context、Chord、右键、主窗口 AI、对话 AI、CLI、MCP 七类入口，以及 GUI/headless 和 Safe/sensitive/Dangerous 组合。

---

## 七、关联文件速查

| 路径 | 职责 |
|---|---|
| `src/domain/capability/` | policy、registry、内置能力、tool projection；唯一原子执行域 |
| `src/domain/event.rs` / `src/app/domain_env.rs` | CapabilityEnv、SurfacePort 与运行时注入 |
| `src/domain/chord/` | binding descriptor、Capability target 与 Voice Interaction |
| `src/domain/search/builtin_engine.rs` | builtin descriptor、Context 门禁与 Capability target |
| `src/domain/feature_catalog/` | 目录 identity、聚合、标题、排序、availability 与 binding adapter |
| `src/domain/config/ai_capability_access.rs` | AI 推荐 allowlist 与持久化 |
| `src/domain/mcp/server_config.rs` | MCP 配置、首次安全暴露种子与 `exposure_seeded` |
| `src/domain/mcp/server.rs` / `src/app/mcp_server_runtime.rs` | MCP ExposureSnapshot、双门禁与热更新 |
| `src/app/commands/feature_catalog.rs` / `ai/management.rs` / `mcp.rs` | 目录与授权 IPC |
| `frontend/js/settings/tabs/capabilities-core.js` | 三出口状态、批量变更、推荐 diff 与姿态摘要纯逻辑 |
| `frontend/js/settings/tabs/capabilities.js` | 能力与操作页矩阵渲染和写回 |
| `frontend/js/settings/tabs/mcp-server.js` | MCP Server 摘要和目录深链 |
| `frontend/settings.html` / `frontend/css/views/settings/` | 设置页结构、矩阵和组件视觉 |
| `frontend/js/i18n/{zh,en}.js` | 双语目录、风险、摘要与确认文案 |
| `frontend/js/settings/tabs/capabilities.test.mjs` | 前端纯函数与静态契约回归测试 |
| `src/domain/ai/tool_adapter.rs` / `src/domain/capability/registry.rs` | 0.21.11 统一 AI invoke 与门禁/埋点 |
| `src/domain/config/ai_capability_access.rs` / `src/domain/config/shards.rs` | 0.21.11 AI 授权 seeded 状态与迁移 |
| `frontend/js/**/*.test.mjs` / `xtask/` | 0.21.12 前端统一测试 runner 与失败收口 |
| `src/domain/search/builtin_engine.rs` / `src/app/commands/search.rs` | 0.21.13 参数协议与本地入口兼容壳收敛 |
| `src/domain/event.rs` / `src/app/domain_env.rs` | 0.21.14 最小 port 拆分与 app 层 wiring |
| `src/infra/platform/window/windows.rs` / `src/infra/data/config.rs` | 0.21.14 移除 infra 对 domain 的反向依赖 |
