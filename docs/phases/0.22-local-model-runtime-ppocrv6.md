# Blink 0.22 — 本地模型运行时与 PP-OCRv6（从专用维护到多引擎生命周期）

> **范围**：建立受限的本地模型运行时，统一进程、Provider、模型资产、状态与设置管理；FunASR 迁入管理面并以常驻 GGUF worker 原位替换 Python/PyTorch 链路；OCR 经 PP-OCRv6 资格验证后切换 ONNX 并删除 Python/uv 重依赖；STT 侧 ONNX 候选（ParaformerOnline/FSMN-VAD）实测后退役，最终保留「OCR ONNX + GGUF 三模型」矩阵。
>
> **状态**：✅ 已完成（0.22.0～0.22.10 全部落地，工程门禁全绿；最终 release gate 已实机收口，全新安装/卸载场景按用户决策豁免。剩余 UI/UX 微调不设独立子版本，随日常迭代处理）
>
> **动机**：FunASR 的安装、进程、模型和诊断逻辑过度绑定 STT，PP-OCRv6 需要一套可复用的引擎管理面；ONNX 用于消除 OCR 的 Python 重依赖，STT 按模型价值取舍 runtime，不追求形式上的单 runtime。
>
> **前置**：0.10 本地 STT、0.11/0.17 OCR、0.21 Capability 体系。
>
> **原则**：不影响 `Alt+Space → Focus → 输入` 主链路；分层、日志、错误、事件与前端约束分别以 `spec-architecture.md`、`spec-backend.md`、`spec-frontend.md` 为准；未知进程绝不按端口自动终止。

---

## 一、里程碑总览

| 版本 | 状态 | 内容 | 优先级 | 依赖 |
|---|---|---|---|---|
| **0.22.0** | ✅ | PP-OCRv6 Windows/CPU spike、资格门与最小协议 | P0 | 0.11 / 0.17 |
| **0.22.1** | ✅ | `ManagedProcess`、日志管道与安全进程回收 | P0 | 0.22.0 |
| **0.22.2** | ✅ | Runtime Provider、单 active deployment 与首版 PythonVenv | P0 | 0.22.0 |
| **0.22.3** | ✅ | 本地引擎领域协议、唯一 Manager、注册表与 FunASR 迁移 | P0 | 0.22.1 / 0.22.2 |
| **0.22.4** | ✅ | PP-OCRv6 接入、按需生命周期与 WinRT 回退 | P0 | 0.22.3 |
| **0.22.5** | ✅ | 本地模型运行时设置 UI、通用事件与空间管理 | P1 | 0.22.3 / 0.22.4 |
| **0.22.6** | ✅ | 模型/部署事务、错误与可观测性硬化 | P0/P1 | 0.22.5 |
| **0.22.7** | ✅ | 常驻 GGUF FunASR、三模型、旧链路删除；失效能力清理与唯一模型选择入口 | P1 | 0.22.6 |
| **0.22.8** | ✅ | OCR ONNX 实现与切换：`oar-ocr` 原位替换 Python PaddleOCR | P0 | 0.22.7 |
| **0.22.9** | ✅ | STT 双 runtime 与 VAD/真流式实现（实机切模与 lifecycle 通过；注册门/采用门证据项按用户决策收口） | P0 | 0.22.8 |
| **0.22.10** | ✅ | ASR 侧 ONNX 线路退役 + Python/uv/PaddlePaddle 栈清理 + UI/UX 打磨两轮 | P1 | 0.22.9 |

**依赖关系**：0.22.0～0.22.7 串行打底（spike → 进程/Provider/Manager → 接入与 UI）；0.22.8/0.22.9 的 ONNX 路线由 spike feasibility 解除阻塞后依次落地；0.22.10 只删除已被实测证明冗余的线路与资产。决策证据见 `xtask/spikes/onnx-spike/decision.md` 与 `followup/decision.md`；spike 中仅用于 feasibility 的指标不得写成 release 验收事实。

---

## 二、背景与动机

### 2.1 为什么 0.22 做这件事

把散落在 FunASR/STT 中的安装、进程、模型、日志和清理职责收敛为本地引擎管理面，用 PP-OCRv6 与常驻 GGUF FunASR 验证「共享生命周期契约、不共享推理实现」的多引擎形态；OCR 随后切 ONNX 删除 Python 重栈，STT 依据实测模型价值收敛为「OCR ONNX + GGUF 三模型」。

### 2.2 明确"不是问题"的

- 模型按需下载，不进入安装包；安装包只携带锁、协议、许可与必要 worker 产物。
- WinRT OCR 永久保留为零安装 fallback；高质量后端（ONNX）是可选。
- 管理页的状态、安装和日志命令不是 Capability；除非未来确有 CLI/MCP/AI 入口及授权设计。
- 本地引擎不全部常驻：FunASR 由用户策略控制，OCR 默认按需并在空闲后回收。
- 前端继续纯静态 HTML/CSS/JS，无 bundler、npm 或构建步骤。

---

## 三、核心设计与决策

### 3.1 分层与注册边界（已定）

`domain/local_engine` 定义框架无关协议，`infra/local_engine` 提供进程、部署和 Provider 原语，`app/local_engine` 持有唯一 `EngineManager` 并投影 IPC/事件。注册表是编译期 allowlist；前端只能提交受限 engine/model/action，不能注入 executable、URL、脚本、argv 或环境变量。

### 3.2 状态与资产提交（已定）

环境、进程、服务、模型、desired state 和 operation 彼此正交；状态以 `(service_epoch, revision)` 防止旧实例覆盖，每个引擎同时只允许一个变更操作。引擎 deployment 与模型资产都只有一个稳定 active pointer；安装在 staging/candidate 中完成校验，原子切换失败时保留旧 active；journal、slot 和 residue 是事务实现，不是面向用户的版本列表。selected、installed 和 active 模型必须分开表达，切换失败不得静默改选其他模型。

### 3.3 进程与信任边界（已定）

受管进程并发排空 stdout/stderr，日志有界；Windows 用 Job Object、instance token、PID 创建时间、可执行路径和 health 身份共同验证。终止和异常恢复均 fail-closed；端口被未知进程占用时只换端口或报错，绝不自动 kill。

### 3.4 Provider 与计算设备（已定）

Provider 只实现声明过的锁定安装计划；现存 Provider 为 `ManagedBinary`（GGUF worker）与 `OnnxRuntime`（版本化不可变的共享 ORT DLL），可共享只读、hash 精确的公共 artifact，active 环境保持隔离。`PythonVenv` Provider 已随 0.22.10 退役。计算设备区分用户 preference、解析后的安装 profile 与 health 回报的 actual backend；显式设备失败不得静默回退。

### 3.5 OCR 路由与生命周期（已定）

默认 OCR 为 WinRT；显式 `paddleocr` 失败返回可行动错误。`auto` 原为 hot-only，0.22.10 修订为「已安装本地引擎优先，允许按需冷启动」。PaddleOCR 首次调用 singleflight，取消、session/epoch 与 idle TTL 共同防止旧结果覆盖与进程泄漏；主动停止优先调用带 token 的 `/shutdown`，超时再由 `ManagedProcess` 回收，`Stopped { code: Some(1) }` 保留诊断信息但不进异常退出错误态。

PP-OCRv6 spike 结论为 `conditional-go`：准确率明显优于当时 WinRT 基线，但 CPU 热识别约 4 秒、峰值工作集约 1136MB，**不得描述为轻量默认后端**。原始数据见 `xtask/spikes/ppocrv6/decision.md`。

### 3.6 FunASR 模型治理与交互归属（已定）

产品侧始终只有稳定的 `funasr` engine id，由 `ManagedBinaryProvider + ManagedProcess + NDJSON stdio worker` 承载（stdout 仅承载协议，诊断走 stderr），一次只常驻当前选择的一个模型；旧 Python/PyTorch/HTTP 链路已删除。SenseVoice、Paraformer-zh、Fun-ASR-Nano 是同引擎的受限模型候选，来源固定 revision/SHA-256，不跟随浮动分支；SenseVoice 常驻实测约 251MiB。

- FunASR 卡片是模型选择的唯一写入口，语音页只读展示并跳转；`selected` 与进程实际加载的 `active` 分开表达。
- 未知旧模型 id 保持「不可用而不静默换模」；退役模型（如 `onnx/paraformer-online`）的存量选择同语义，磁盘残留经设置页存储管理显式清理。
- 运行中切模经用户确认后 stop → commit selection → start，失败恢复可解释状态；用户 stop、切模重启、空闲回收和应用退出不得被上层投影成「进程意外退出」。
- 前端只暴露「是否实时预览」：模型支持真流式走 native partial，否则伪流式；runtime/transport/VAD 类型仅作只读诊断，不设 GGUF/ONNX、True/Pseudo 技术开关。
- GGUF worker 不消费热词与可控 ITN，相关 UI/配置投影/协议字段/能力声明已删除（旧配置安全读取并自然收敛）；SenseVoice 归一化与 GGUF 固有后处理不是可切换 ITN，未误删。EnergyVad 参数保留。

### 3.7 多 implementation 边界与 executor topology（已定）

架构不允许同一 engine id 直接换 runtime（`EngineDefinition` 只有一组 runtime/transport/install plan，registry 按 id 唯一映射 adapter，deployment 只有一个 active pointer），因此 0.22.9 引入受限的内部 implementation 层：

- 产品层仍只有稳定 engine id；implementation 编译期闭合，各自声明 runtime、transport、install plan 与可承载模型。
- 模型 descriptor 绑定 implementation；用户选模型后由编译期映射解析，前端不能提交 runtime、可执行路径或 URL。
- start 时冻结 selected model、resolved implementation、runtime profile 和 deployment identity；运行中切模走 stop → commit → start，失败保持可解释回滚。
- 产品状态只投影一个 selected model 和一个 active model/implementation，一次只允许一个 ASR implementation 活跃。
- 该层只服务编译期内置实现，不演化为任意第三方 runtime 注册或前端可注入通道。

最终 topology：**OCR in-process lazy Session + idle TTL；三模型 GGUF worker**（ParaformerOnline 的独立 ONNX worker 已随退役移除）。in-process OCR 不具备 native crash isolation，不得对外宣称；上层只呈现闭合的 `StreamingSttExecutor`/OCR executor 能力，transport、DLL 路径不穿透 domain 与前端。生产音频热路径不得使用 JSON 浮点数组或 Base64，应采用有界二进制 framing 或等价的流式背压协议。

### 3.8 ONNX 双 runtime 结论与 ASR 侧退役（已定，0.22.10 收口）

- **OCR ONNX 保留**：`oar-ocr + ORT` 原位替换 Python PaddleOCR（0.22.8），稳定 `paddleocr` id 与 `OcrResult` 契约不变。
- **GGUF 三模型保留**：GGUF 不是脚本，而是锁定构建的 C++ worker 二进制及其供应链锁与 NDJSON 协议；Fun-ASR-Nano 无等价 ONNX 实现且有独特中文识别价值，不借双 runtime 改造删除或静默迁移既有模型。
- **ASR 侧 ONNX 退役**：ParaformerOnline 注册门 NO-GO——first_partial p50≈1.27s（门限 400ms）、CER 相对 Nano 基线 Δ≈6.2pp，其差异化价值（低延迟真流式）恰是实测最差项，已从模型目录、implementation 注册、安装/启动链路整体移除；FSMN-VAD 采用门关闭（生产伪流式直持 EnergyVad，从未接线，移除无生产风险）。证据见 `xtask/spikes/onnx-spike/followup/decision.md`。
- **恢复真流式的重启条件（关键）**：从 git 历史恢复被删提交，先完成 CIF 首窗校准把 first_partial p50 压进 400ms，再重跑全量注册门（句尾前非空 partial、final-after-release p95≤800ms、RTF p95<0.8、中文 CER 相对同语料 Nano 恶化≤1pp、100 次 start/stop/cancel 与 10 次 kill/restart 无 orphan/协议死锁/旧 generation 泄漏）；不得降低阈值换取完成。「流式 VAD + 离线 ASR」必须标为伪流式；VAD 与 ASR runtime 正交，收益不得错误归因。

### 3.9 OCR ONNX 安装与执行边界（已定）

- **供应链**：生产 asset lock 保存完整 URL、版本/revision、SHA-256、大小与 license；CPU-only，锁定必要 DLL，禁止隐式下载和 CUDA/TensorRT provider；ORT DLL 与 ONNX 模型按需下载不进安装包，lock metadata 随程序发布。
- **联合提交，指针最后切换**：ORT 与模型分别 staging、校验和 promote；只有两者联合 self-test 通过才提交 deployment pointer，未被引用的中间产物是可清扫 residue。
- **安装自检隔离**：真实 Session + 最小推理 self-test 运行在一次性隔离验证进程中，主进程不从 staging 加载 DLL；该进程不是用户 CLI，也不是第二种 executor topology。
- **desired/loaded/pending_restart**：主进程未初始化 ORT 时首次安装可立即加载 active runtime；已加载不同 DLL identity 时，更新/回滚只改 desired 并投影 `pending_restart`，当前 loaded deployment 不说谎；只切模型 generation 不要求重启。
- **单 Session 有界执行**：一个 OAROCR pipeline/Session 由专用 blocking executor 承载，一次一个请求、最多 4 个 pending，queue 满返回结构化 Busy；ORT 默认 `inter_op=1`、`intra_op≤4`，不建 Session pool，不阻塞 Tokio worker。
- **取消语义诚实**：queued 请求立即移除；active 请求取消后立即返回 Cancelled 并丢弃迟到结果；不可中断的 native call 允许有界收敛，不得宣称 ORT kernel 已被强制终止。
- **hot 定义**：只有 Session Ready 才算 OCR hot；「资产已安装」「DLL 已加载」不算，`auto` 不得据此走本地引擎。
- **结果契约**：优先 oar-ocr 原生 region/word boxes；缺 word boxes 时以 region 生成单个 fallback word，禁止按字符等宽伪造框；坐标校验有限值、正面积和图片边界后进既有 line grouping 与 `OcrResult`。

升级用户的旧 Python deployment 曾作为降级资产独立保留（不参与运行时 fallback），0.22.10 已完成默认迁移并删除。

---

## 四、复用与范围边界

### 4.1 已有基础设施复用

| 基础 | 0.22 用法 |
|---|---|
| `ConfigStore<T>` 与配置事件 | 引擎偏好、OCR 路由和 STT 模型选择 |
| `OcrBackend` / `OcrResult` | PP-OCRv6/ONNX 接入并保持截图结果契约 |
| WinRT OCR | 默认后端和降级逃生口 |
| 截图 session/epoch | OCR 启动、取消与结果回流竞态墙 |
| 设置页模块/CSS token | 本地运行时卡片、日志、诊断和清理 |
| 事件名常量与结构化错误 | local-engine 状态、日志和可行动错误投影 |
| `ManagedBinaryProvider` / deployment 事务模式 | ONNX Provider 复用其 staging、hash、自检与 active/rollback 设计 |
| `model_storage` 模型资产管理 | ONNX 模型（.onnx + config + 字典）复用此管理 |

### 4.2 不进 0.22

| 项 | 理由 |
|---|---|
| 第三方任意本地引擎注册 | 需要独立签名、授权和任意代码执行信任设计 |
| 通用模型商店 | 下载、版本和校验语义依赖具体工具链 |
| PP-Structure、表格、公式、OCR-VL | 输出协议与资源预算超出截图文字识别 |
| 完整 GPU 调度、多 ASR 同时常驻 | 需要 RAM/VRAM 仲裁和独立产品设计 |
| Linux/macOS 本地引擎 | 当前实现与验收以 Windows 为目标 |
| AI/MCP 安装、启动或清理引擎 | 管理操作尚无外部授权与危险确认模型 |

---

## 五、子版本完成摘要

| 版本 | 完成结论 |
|---|---|
| **0.22.0** | PP-OCRv6 thin wrapper、协议、锁、benchmark 和 golden corpus 落盘；资格 `conditional-go`。 |
| **0.22.1** | 通用进程 start/stop/wait、日志洪泛、竞态、异常退出、同步 shutdown 和 Windows 进程树回收闭合。 |
| **0.22.2** | Provider-neutral 安装契约、隔离环境、single-active deployment、事务回滚和引用保护落地。 |
| **0.22.3** | 领域协议、唯一 Manager 和 registry 落地；FunASR 生命周期迁入管理面。 |
| **0.22.4** | PaddleOCR adapter、路由、singleflight、取消/TTL、几何结果和 WinRT fallback 闭合。 |
| **0.22.5** | 引擎页统一状态、日志、模型、空间与清理；事件按 epoch/revision 和 identity 去重。 |
| **0.22.6** | 模型/部署事务、崩溃恢复、OCR 几何裁剪、结构化错误和模块拆分完成。 |
| **0.22.7** | 三模型常驻 GGUF worker 接管 `funasr`，旧 Python/PyTorch/HTTP 链路删除；热词/ITN 清理与唯一模型选择入口两项契约收口（含 PaddleOCR 主动停止误报修复）。 |
| **0.22.8** | OCR ONNX 原位替换：`OnnxRuntime` 贯穿协议/安装/Manager/cleanup，ORT+模型联合 staging、隔离 validator、in-process executor（单 Session、有界 queue、TTL、generation 取消），契约不变。 |
| **0.22.9** | STT 双 runtime：implementation 层、ParaformerOnline ONNX worker（有界二进制音频协议）、FSMN-VAD 可插拔前端；实机 GGUF↔ONNX 双向切模与 lifecycle（100/100 start/stop/cancel、10/10 kill/restart、0 orphan）通过。注册门/采用门证据项按用户决策收口，后由 0.22.10 退役。 |
| **0.22.10** | ASR 侧 ONNX 线路退役；`PythonVenvProvider`、uv 安装链、PaddleOCR Python server 与嵌入资源全部删除（legacy serde 变体与存储页清理路径保留）；UI/UX 打磨两轮（引擎卡/存储页排版、OCR 面板尺寸与引擎标注、`auto` 语义修订、字符级划选、推荐徽章、校验态派生、诊断图标与列表降噪等）。 |

### 最终 release gate（已收口）

- 实机以 0.22.x 制品自 0.21.17 原地升级并走查 Alt+Space 输入、设置页运行时、G1/G2 本地语音、Alt+A OCR/WinRT fallback，profile 沿用；MSI/NSIS 打包清单零 ORT DLL/ONNX 模型/Python，仅随包 3 个 GGUF worker；未加载/ready/识别中/停止后与 OCR 冷/热态的整进程树资源已记录。全新安装与卸载场景按用户决策豁免。
- 0.22.10 清理后由工程门禁复绿兜底（见 §六）。过程 handoff 文档已移除，细节见 git 历史。

---

## 六、验收

最终工程门禁（2026-09-06，0.22.10 收尾）：`cargo fmt --all -- --check`、`cargo clippy --bin blink --all-targets -- -D warnings`（0 warning）、`cargo test --bin blink`（2889 passed / 0 failed）、`node frontend/run-tests.mjs`（56 个测试文件）、`cargo xtask release-check`（版本/资源/许可/GGUF+ONNX 供应链/Python 回流守卫/分层）全部通过。

各子版本逐项验收条件与实机证据细节见 git 历史（0.22.0～0.22.7 最终审查提交 `99368e0f`，其余随各子版本提交）。

---

## 七、关联文件速查

**保留的核心改动**：
- `src/domain/local_engine/` — 引擎身份、描述符、implementation 声明、模型与 adapter 协议。
- `src/infra/local_engine/` — 受管进程、runtime/deployment store、模型存储、Provider（`binary.rs`/`onnx.rs`）、`onnx_ocr/` in-process executor、worker 协议。
- `src/app/local_engine/` — 唯一 Manager、registry、`funasr/` 与 `paddleocr/` adapter、`ocr_coordinator/`、模型安装和事件投影。
- `src/app/commands/local_engine/` — 受限 catalog/lifecycle/model/storage/diagnostics/preferences IPC。
- `src/domain/ocr/` — OCR 契约与输入预算。
- `resources/ocr/paddleocr-onnx/asset-lock.json` — ORT + PP-OCRv6 det/rec/字典供应链真源（只嵌 metadata）。
- `resources/stt/funasr-gguf/worker-lock.json`、`xtask/funasr-worker/` — GGUF worker 与模型供应链真源及构建入口（Nano 继续使用）。
- `frontend/js/settings/tabs/engines/`、`voice.js` — 本地运行时卡片、唯一模型选择入口与 STT 配置。
- `frontend/css/views/settings/settings-local-runtime*.css` — 本地运行时 views 层样式。
- `xtask/src/main.rs` — release-check 门禁（含「Python 栈回流」守卫与 GGUF+ONNX 供应链校验）。

**0.22.10 已删除**：`src/infra/local_engine/providers/python/`、`src/infra/platform/python/`（收敛为 `gpu.rs`）、`resources/ocr/paddleocr/`、`paddleocr/{launch,health,locks}.rs`；`RuntimePlan::PythonVenv` / `ManifestExtension::PythonVenv` serde 变体与存储页 legacy 清理路径保留（旧用户资产安全读取/清理的唯一通道）。

**spike / 实测产物**：
- `xtask/spikes/ppocrv6/decision.md` — PP-OCRv6 资格结论（conditional-go）。
- `xtask/spikes/funasr-runtime/` — GGUF worker 构建与可复现实测产物。
- `xtask/spikes/onnx-spike/decision.md`、`followup/decision.md`（含 `followup/results/` 证据 JSON）— ONNX 路线与 ASR 侧退役决策。
- `xtask/spikes/fsmn-vad/` — FSMN-VAD 调研产物。
