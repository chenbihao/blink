# Blink 0.22 — 本地模型运行时与 PP-OCRv6（从专用维护到多引擎生命周期）

> **范围**：建立受限的本地模型运行时，统一进程、Provider、模型资产、状态与设置管理；FunASR 迁入管理面并以常驻 GGUF worker 原位替换 Python/PyTorch 链路；OCR 经 PP-OCRv6 资格验证后切换 ONNX 并删除 Python/uv 重依赖；STT 侧 ONNX 候选（ParaformerOnline/FSMN-VAD）实测后退役，最终保留「OCR ONNX + GGUF 三模型」矩阵。0.22.11～0.22.16 追加交互与首启体验收尾：设置页关闭按钮、编辑器关闭三态确认、chord 动作提升为独立全局快捷键、首次引导向导分步化，下载进度、OCR 文本划选、截图多屏 DPI 修正与 AI 净化截图/窗口管理，EnergyVad 自适应升级和语音文本防回退，以及常见 WAV 解码规范化与 STT Capability。
>
> **状态**：🚧 进行中（0.22.0～0.22.13 已完成；0.22.14 已落地下载进度、OCR 划选与截图多屏 DPI 修正；AI 净化截图（三态 `blink_visibility` + DWM cloak 事务串行化）、`window_ref` 与 `manage_window` 已实现并通过编译/clippy/单测，待 Windows 实机验收；0.22.15 EnergyVad 自适应升级与语音文本防回退已完成调研和代码定位，待实现；0.22.16 常见 WAV 解码规范化与 `transcribe_audio` Capability 已完成范围梳理，待实现）
>
> **动机**：FunASR 的安装、进程、模型和诊断逻辑过度绑定 STT，PP-OCRv6 需要一套可复用的引擎管理面；ONNX 用于消除 OCR 的 Python 重依赖，STT 按模型价值取舍 runtime，不追求形式上的单 runtime。
>
> **前置**：0.10 本地 STT、0.11/0.17 OCR、0.21 Capability 体系。
>
> **原则**：不影响 `Alt+Space → Focus → 输入` 主链路；分层、日志、错误、事件与前端约束分别以 `spec-architecture.md`、`spec-backend.md`、`spec-frontend.md` 为准；未知进程绝不按端口自动终止。

---

## 一、里程碑总览

| 版本 | 状态 | 内容 | 优先级 | 依赖 |
|---|---|---|---|---|
| **0.22.0** | ✅ | PP-OCRv6 Windows/CPU spike、资格门与最小协议 | P0 | 0.11 / 0.17 |
| **0.22.1** | ✅ | `ManagedProcess`、日志管道与安全进程回收 | P0 | 0.22.0 |
| **0.22.2** | ✅ | Runtime Provider、单 active deployment 与首版 PythonVenv | P0 | 0.22.0 |
| **0.22.3** | ✅ | 本地引擎领域协议、唯一 Manager、注册表与 FunASR 迁移 | P0 | 0.22.1 / 0.22.2 |
| **0.22.4** | ✅ | PP-OCRv6 接入、按需生命周期与 WinRT 回退 | P0 | 0.22.3 |
| **0.22.5** | ✅ | 本地模型运行时设置 UI、通用事件与空间管理 | P1 | 0.22.3 / 0.22.4 |
| **0.22.6** | ✅ | 模型/部署事务、错误与可观测性硬化 | P0/P1 | 0.22.5 |
| **0.22.7** | ✅ | 常驻 GGUF FunASR、三模型、旧链路删除；失效能力清理与唯一模型选择入口 | P1 | 0.22.6 |
| **0.22.8** | ✅ | OCR ONNX 实现与切换：`oar-ocr` 原位替换 Python PaddleOCR | P0 | 0.22.7 |
| **0.22.9** | ✅ | STT 双 runtime 与 VAD/真流式实现（实机切模与 lifecycle 通过；注册门/采用门证据项按用户决策收口） | P0 | 0.22.8 |
| **0.22.10** | ✅ | ASR 侧 ONNX 线路退役 + Python/uv/PaddlePaddle 栈清理 + UI/UX 打磨两轮 | P1 | 0.22.9 |
| **0.22.11** | ✅ | 设置页右上角关闭按钮 + 编辑器关闭确认三态化（保存并关闭/放弃更改/继续编辑） | P0 | 无 |
| **0.22.12** | ✅ | chord 动作提升为独立全局快捷键：RegisterHotKey 注册、双层冲突探测、设置页 UI | P0 | 0.21（chord 触发已收敛 `CapabilityRegistry::invoke`） |
| **0.22.13** | ✅ | 首次引导向导分步化：常用开关 + OCR 引擎一键下载（PP-OCRv6 ≈17MB）+ 语音引擎深链引导 | P1 | 0.22 本地引擎链路 |
| **0.22.14** | ✅ | 已完成共享下载进度、OCR 文本划选/降级提示与截图 overlay 混合 DPI 修正；AI 净化截图（三态 `blink_visibility` + DWM cloak 事务串行化 + `window_ref` 身份二次核验）、`manage_window`（通过 `SurfacePort` trait 调用，不直接依赖 infra）已实现并修复 review P1/P2 问题，通过编译/clippy/单测，待 Windows 实机验收 | P1 | 0.22.13 / 0.18.8 截图坐标契约 / 0.21 Capability 边界 |
| **0.22.15** | 📋 | EnergyVad 自适应抗噪升级 + 伪流式句子事务化 + G1 语音文本防空/防回退 | P0 | 0.22.7 GGUF 伪流式链路 / FSMN-VAD 两轮 spike |
| **0.22.16** | 📋 | 常见 WAV 解码规范化 + `transcribe_audio` STT Capability + 私有语料分层验收 | P1 | 0.22.15 标准 PCM/STT 链路 / 0.21 Capability 边界 |

**依赖关系**：0.22.0～0.22.7 串行打底（spike → 进程/Provider/Manager → 接入与 UI）；0.22.8/0.22.9 的 ONNX 路线由 spike feasibility 解除阻塞后依次落地；0.22.10 只删除已被实测证明冗余的线路与资产。决策证据见 `xtask/spikes/onnx-spike/decision.md` 与 `followup/decision.md`；spike 中仅用于 feasibility 的指标不得写成 release 验收事实。0.22.11～0.22.16 为交互与可用性收尾，设计档案统一并入本文；0.22.15 复用两轮 VAD spike 结论，不重新引入已关闭的 FSMN-VAD runtime；0.22.16 在 0.22.15 标准化音频与文本提交语义稳定后接入，不另建一套 STT executor。

---

## 二、背景与动机

### 2.1 为什么 0.22 做这件事

把散落在 FunASR/STT 中的安装、进程、模型、日志和清理职责收敛为本地引擎管理面，用 PP-OCRv6 与常驻 GGUF FunASR 验证「共享生命周期契约、不共享推理实现」的多引擎形态；OCR 随后切 ONNX 删除 Python 重栈，STT 依据实测模型价值收敛为「OCR ONNX + GGUF 三模型」。

0.22.11～0.22.13 动机来自三条真实用户反馈与首启缺口：设置窗口无可见关闭路径（唯一出口是 ESC）、编辑器关闭确认缺「保存」出口（被迫在继续编辑/放弃更改二选一）、chord 只有主窗可见时可用；外加首启只有快捷键列表，缺常用开关与引擎引导（Win OCR 中文识别弱，需要把「一键下载 PP-OCR 增强」讲清楚）。

### 2.2 明确"不是问题"的

- 模型按需下载，不进入安装包；安装包只携带锁、协议、许可与必要 worker 产物。
- WinRT OCR 永久保留为零安装 fallback；高质量后端（ONNX）是可选。
- 管理页的状态、安装和日志命令不是 Capability；除非未来确有 CLI/MCP/AI 入口及授权设计。
- 本地引擎不全部常驻：FunASR 由用户策略控制，OCR 默认按需并在空闲后回收。
- 前端继续纯静态 HTML/CSS/JS，无 bundler、npm 或构建步骤。
- 设置窗口 **hide 而非 close** 是刻意的复用/预热设计；0.22.11 的关闭按钮只 hide，不引入销毁语义。
- `confirmDialog` 两按钮 boolean 契约本身没有 bug，约 25 处调用点依赖；0.22.11 的三态扩展向后兼容，不传第三动作时语义不变。
- chord 门禁要求主窗可见是 0.18 输入状态流转的刻意设计；0.22.12 不改 chord 模式本身，只经 `RegisterHotKey` 增加平行入口。

---

## 三、核心设计与决策

### 3.1 分层与注册边界（已定）

`domain/local_engine` 定义框架无关协议，`infra/local_engine` 提供进程、部署和 Provider 原语，`app/local_engine` 持有唯一 `EngineManager` 并投影 IPC/事件。注册表是编译期 allowlist；前端只能提交受限 engine/model/action，不能注入 executable、URL、脚本、argv 或环境变量。

### 3.2 状态与资产提交（已定）

环境、进程、服务、模型、desired state 和 operation 彼此正交；状态以 `(service_epoch, revision)` 防止旧实例覆盖，每个引擎同时只允许一个变更操作。引擎 deployment 与模型资产都只有一个稳定 active pointer；安装在 staging/candidate 中完成校验，原子切换失败时保留旧 active；journal、slot 和 residue 是事务实现，不是面向用户的版本列表。selected、installed 和 active 模型必须分开表达，切换失败不得静默改选其他模型。

### 3.3 进程与信任边界（已定）

受管进程并发排空 stdout/stderr，日志有界；Windows 用 Job Object、instance token、PID 创建时间、可执行路径和 health 身份共同验证。终止和异常恢复均 fail-closed；端口被未知进程占用时只换端口或报错，绝不自动 kill。

### 3.4 Provider 与计算设备（已定）

Provider 只实现声明过的锁定安装计划；现存 Provider 为 `ManagedBinary`（GGUF worker）与 `OnnxRuntime`（版本化不可变的共享 ORT DLL），可共享只读、hash 精确的公共 artifact，active 环境保持隔离。`PythonVenv` Provider 已随 0.22.10 退役。计算设备区分用户 preference、解析后的安装 profile 与 health 回报的 actual backend；显式设备失败不得静默回退。

### 3.5 OCR 路由与生命周期（已定）

默认 OCR 为 WinRT；`auto` 在已安装本地引擎时优先使用并允许按需冷启动。显式 `paddleocr` 在环境未安装时降级 WinRT，并在成功结果附带 `backend_degrade_hint` 供截图界面提示准确率下降与安装入口；已安装但启动/识别失败仍返回可行动错误，不掩盖基础设施故障。PaddleOCR 首次调用 singleflight，取消、session/epoch 与 idle TTL 共同防止旧结果覆盖与进程泄漏；主动停止优先调用带 token 的 `/shutdown`，超时再由 `ManagedProcess` 回收，`Stopped { code: Some(1) }` 保留诊断信息但不进异常退出错误态。

PP-OCRv6 spike 结论为 `conditional-go`：准确率明显优于当时 WinRT 基线，但 CPU 热识别约 4 秒、峰值工作集约 1136MB，**不得描述为轻量默认后端**。原始数据见 `xtask/spikes/ppocrv6/decision.md`。

### 3.6 FunASR 模型治理与交互归属（已定）

产品侧始终只有稳定的 `funasr` engine id，由 `ManagedBinaryProvider + ManagedProcess + NDJSON stdio worker` 承载（stdout 仅承载协议，诊断走 stderr），一次只常驻当前选择的一个模型；旧 Python/PyTorch/HTTP 链路已删除。SenseVoice、Paraformer-zh、Fun-ASR-Nano 是同引擎的受限模型候选，来源固定 revision/SHA-256，不跟随浮动分支；SenseVoice 常驻实测约 251MiB。

- FunASR 卡片是模型选择的唯一写入口，语音页只读展示并跳转；`selected` 与进程实际加载的 `active` 分开表达。
- 未知旧模型 id 保持「不可用而不静默换模」；退役模型（如 `onnx/paraformer-online`）的存量选择同语义，磁盘残留经设置页存储管理显式清理。
- 运行中切模经用户确认后 stop → commit selection → start，失败恢复可解释状态；用户 stop、切模重启、空闲回收和应用退出不得被上层投影成「进程意外退出」。
- 前端只暴露「是否实时预览」：模型支持真流式走 native partial，否则伪流式；runtime/transport/VAD 类型仅作只读诊断，不设 GGUF/ONNX、True/Pseudo 技术开关。
- GGUF worker 不消费热词与可控 ITN，相关 UI/配置投影/协议字段/能力声明已删除（旧配置安全读取并自然收敛）；SenseVoice 归一化与 GGUF 固有后处理不是可切换 ITN，未误删。EnergyVad 参数保留。

### 3.7 多 implementation 边界与 executor topology（已定）

架构不允许同一 engine id 直接换 runtime（`EngineDefinition` 只有一组 runtime/transport/install plan，registry 按 id 唯一映射 adapter，deployment 只有一个 active pointer），因此 0.22.9 引入受限的内部 implementation 层：

- 产品层仍只有稳定 engine id；implementation 编译期闭合，各自声明 runtime、transport、install plan 与可承载模型。
- 模型 descriptor 绑定 implementation；用户选模型后由编译期映射解析，前端不能提交 runtime、可执行路径或 URL。
- start 时冻结 selected model、resolved implementation、runtime profile 和 deployment identity；运行中切模走 stop → commit → start，失败保持可解释回滚。
- 产品状态只投影一个 selected model 和一个 active model/implementation，一次只允许一个 ASR implementation 活跃。
- 该层只服务编译期内置实现，不演化为任意第三方 runtime 注册或前端可注入通道。

最终 topology：**OCR in-process lazy Session + idle TTL；三模型 GGUF worker**（ParaformerOnline 的独立 ONNX worker 已随退役移除）。in-process OCR 不具备 native crash isolation，不得对外宣称；上层只呈现闭合的 `StreamingSttExecutor`/OCR executor 能力，transport、DLL 路径不穿透 domain 与前端。生产音频热路径不得使用 JSON 浮点数组或 Base64，应采用有界二进制 framing 或等价的流式背压协议。

### 3.8 ONNX 双 runtime 结论与 ASR 侧退役（已定，0.22.10 收口）

- **OCR ONNX 保留**：`oar-ocr + ORT` 原位替换 Python PaddleOCR（0.22.8），稳定 `paddleocr` id 与 `OcrResult` 契约不变。
- **GGUF 三模型保留**：GGUF 不是脚本，而是锁定构建的 C++ worker 二进制及其供应链锁与 NDJSON 协议；Fun-ASR-Nano 无等价 ONNX 实现且有独特中文识别价值，不借双 runtime 改造删除或静默迁移既有模型。
- **ASR 侧 ONNX 退役**：ParaformerOnline 注册门 NO-GO——first_partial p50≈1.27s（门限 400ms）、CER 相对 Nano 基线 Δ≈6.2pp，其差异化价值（低延迟真流式）恰是实测最差项，已从模型目录、implementation 注册、安装/启动链路整体移除；FSMN-VAD 采用门关闭（生产伪流式直持 EnergyVad，从未接线，移除无生产风险）。证据见 `xtask/spikes/onnx-spike/followup/decision.md`。
- **恢复真流式的重启条件（关键）**：从 git 历史恢复被删提交，先完成 CIF 首窗校准把 first_partial p50 压进 400ms，再重跑全量注册门（句尾前非空 partial、final-after-release p95≤800ms、RTF p95<0.8、中文 CER 相对同语料 Nano 恶化≤1pp、100 次 start/stop/cancel 与 10 次 kill/restart 无 orphan/协议死锁/旧 generation 泄漏）；不得降低阈值换取完成。「流式 VAD + 离线 ASR」必须标为伪流式；VAD 与 ASR runtime 正交，收益不得错误归因。

### 3.9 OCR ONNX 安装与执行边界（已定）

- **供应链**：生产 asset lock 保存完整 URL、版本/revision、SHA-256、大小与 license；CPU-only，锁定必要 DLL，禁止隐式下载和 CUDA/TensorRT provider；ORT DLL 与 ONNX 模型按需下载不进安装包，lock metadata 随程序发布。
- **联合提交，指针最后切换**：ORT 与模型分别 staging、校验和 promote；只有两者联合 self-test 通过才提交 deployment pointer，未被引用的中间产物是可清扫 residue。
- **安装自检隔离**：真实 Session + 最小推理 self-test 运行在一次性隔离验证进程中，主进程不从 staging 加载 DLL；该进程不是用户 CLI，也不是第二种 executor topology。
- **desired/loaded/pending_restart**：主进程未初始化 ORT 时首次安装可立即加载 active runtime；已加载不同 DLL identity 时，更新/回滚只改 desired 并投影 `pending_restart`，当前 loaded deployment 不说谎；只切模型 generation 不要求重启。
- **单 Session 有界执行**：一个 OAROCR pipeline/Session 由专用 blocking executor 承载，一次一个请求、最多 4 个 pending，queue 满返回结构化 Busy；ORT 默认 `inter_op=1`、`intra_op≤4`，不建 Session pool，不阻塞 Tokio worker。
- **取消语义诚实**：queued 请求立即移除；active 请求取消后立即返回 Cancelled 并丢弃迟到结果；不可中断的 native call 允许有界收敛，不得宣称 ORT kernel 已被强制终止。
- **hot 定义**：只有 Session Ready 才算 OCR hot；「资产已安装」「DLL 已加载」不算，`auto` 不得据此走本地引擎。
- **结果契约**：优先 oar-ocr 原生 region/word boxes；缺 word boxes 时以 region 生成单个 fallback word，禁止按字符等宽伪造框；坐标校验有限值、正面积和图片边界后进既有 line grouping 与 `OcrResult`。

升级用户的旧 Python deployment 曾作为降级资产独立保留（不参与运行时 fallback），0.22.10 已完成默认迁移并删除。

### 3.10 交互收尾：全局快捷键 / 三态确认 / 引导向导（已定，0.22.11～0.22.13）

- **全局快捷键 = RegisterHotKey 有条件启用**：仅次级可提升的 chord 动作组合键（默认建议 `Ctrl+Alt` 前缀降低冲突面），注册到既有 message-only 窗口 `BlinkInputWindow`，`WM_HOTKEY` 进既有无锁 effect 通道 → `HotkeyService` → 复用 `ChordRegistry::trigger`（含 `disabled_chord_actions` 门禁），符合 spec-architecture「多协议入口，单一原子执行语义」。选它的关键理由是「注册即冲突探测」（`ERROR_HOTKEY_ALREADY_REGISTERED`），hook 路线给不了；LL hook 全局吞 `Alt+字母` 则双重违反「热键默认不吞键」铁则。**主热键（Alt+Space）仍走 hook**：product.md 取舍表与 0.10「为何不用 RegisterHotKey」的否决仅针对主热键（体验与 fallback 顾虑），本决策以「可探测冲突」换「系统注册」，是对该取舍的补充而非推翻。已知局限：`RegisterHotKey` 探测不到 LL hook 类软件（Snipaste/QQ 截图等）的占用，冲突反馈定位「尽力而为」，UI 需明示。
- **冲突探测两层**：OS 级在保存配置与启动注册时试注册，失败报「组合键已被占用」，配置保留但标记未生效；内部级扩展 `ChordRegistry::binding_conflicts()` 覆盖「全局键互斥」「全局键 vs 主热键」，顺带补齐既有「chord 键 vs 主热键」跨检查缺口。配置变更即重注册（先注销旧键），单键失败不阻塞其他键并投影「未生效 + 原因」（`occupied` / `invalid` / `error`，经 `blink://global-hotkey-status` 回显）；全局键并入 `InputConfigSnapshot` 经 `refresh_input_config` 热更新，应用退出注销。
- **共享对话框三态向后兼容**：`showCustomDialog()` 增加可选第三动作，不传第三动作时返回值与既有 boolean 语义完全一致，约 25 处调用零改动。编辑器关闭确认三态化：保存并关闭（Enter，复用 `handleSave()` 的隐藏→后台保存→失败回显闭环）/ 放弃更改 / 继续编辑（Esc）；标题栏 X、Esc、Alt+F4 三个关闭入口收敛到同一确认。
- **设置页关闭 = hide**：右上角关闭按钮接线既有 `#close-btn` 空转监听，走 `hideSettingsWindow()`，与 ESC 同语义；不改窗口创建与预热路径，按钮区域不落在拖拽区。
- **向导只编排不复制引擎逻辑**：4 步（欢迎+核心快捷键 / 常用开关 / 引擎增强 / 完成）步步可跳过/后退；开关走 `set_config` 即时生效；OCR 一键下载 PP-OCRv6 复用引擎页同一条 install 命令与进度事件（backend 默认 `auto`，装完即生效，不做引擎选择 UI）；FunASR（237MB~954MB）只深链 `open_settings_tab` 不进默认下载路径；任意退出路径置 `onboarding_version`；老用户存量配置一次性迁移为未完成（0），升级后补看一次新版向导，常用开关页提示 AI/语音输入需进设置页手动配置。
- **范围否决保留**：chord 模式全局化（去掉 `window.visible` 门禁）不做——无法探测冲突且全局吞 Alt 字母违反铁则；不引入 Tauri global-shortcut 插件（现有 hook 基建 + RegisterHotKey 已覆盖）。

### 3.11 下载与截图体验收尾（已定，0.22.14）

- 欢迎页与设置页引擎卡共用纯函数下载进度模块和 CSS 组件，展示真实字节、百分比与有界样本 ETA；环境安装与 GGUF 模型下载复用同一进度事件，未知总量使用不定进度态。
- OCR 阅读模式把连续字符/词框按行合并为选区块，右键菜单按所在显示器实际尺寸和 UI scale 定位；后端可行动错误直接进入截图 toast。
- 截图 capability 成功结果保留实际 backend 与 fallback 原因；仅“显式 PaddleOCR 未安装而降级 WinRT”附 `backend_degrade_hint`，`auto` 回退不提示，避免正常 fallback 反复打扰。
- 混合 DPI 首次截图时，截图 overlay HWND 在会话内钉扎虚拟桌面物理矩形并拦截 `WM_DPICHANGED` 的错误重排；隐藏或进入单屏图片编辑器前解除钉扎。

### 3.12 AI 净化截图与窗口管理（已定，0.22.14 追加项，已实现待实机验收）

**问题定位**：现有 AI 已能走 `list_windows → screenshot { op: "window", hwnd }`，但 `window` 实际按 DWM 窗口矩形截取桌面区域；目标被遮挡时会截到遮挡物。区域截图仅隐藏主窗与右键菜单，从对话窗口发起时 chat/settings/便签等 Blink 窗口仍可能入镜。现有 `hide_chat_window` 会中止活跃 AI 请求，不能用于同一工具调用中的截图自隐藏。

**截图参数**：`screenshot` 增加 `blink_visibility: "auto" | "exclude" | "include"`，默认 `auto`。

- `auto`：全屏、指定显示器或外部窗口截图时临时排除全部 Blink 窗口；目标本身属于 Blink 时仅保留目标，排除其他 Blink 窗口。
- `exclude`：强制排除全部 Blink 窗口；若目标是 Blink 窗口则返回结构化参数冲突。
- `include`：保持当前现场，Blink 对话、设置、便签等窗口可进入截图。
- 用户未说明时 AI 使用 `auto`；“原样/包括对话框/保留 Blink”使用 `include`；“不要截到 Blink”使用 `exclude`。确认卡需展示解析后的实际行为。

**原子截图事务**：AI 不负责先调用窗口管理来隐藏 Blink。app 层截图编排在一次 `screenshot` 调用内完成：记录 Blink 可见窗口、当前前台窗口与目标窗口状态 → 对需排除的 Blink HWND 使用 DWM cloak（不触发 hide/close 业务语义）→ 必要时临时 restore/activate 外部目标 → `DwmFlush` → 复核目标身份并截图 → 无论成功、失败、超时或取消均恢复 cloak、目标原状态与合理的前台焦点。不得调用会 abort ChatService 的语义化 `hide_chat_window`，也不得把已隐藏但尚未截图的半完成状态暴露给 AI。

**目标准备语义**：外部窗口截图默认保证目标可见并前置；若目标最初最小化，截图后恢复最小化。默认不最大化目标，不扰动其尺寸；最大化只由用户显式请求或独立窗口管理调用触发。长期若切换到可捕获被遮挡窗口内容的专用后端，可在不改变 Capability 契约的前提下替换此编排实现。

**独立窗口管理 Capability**：新增 `manage_window`，首版只提供 `activate`、`minimize`、`maximize`、`restore`。不把这些动作塞进 `screenshot`；截图只消费内部目标准备编排。首版不提供 `close`、`move`、`resize`、持久 `always_on_top`，避免数据丢失、混合 DPI 几何与遗留置顶状态。

**窗口引用与权限边界**：`list_windows` 返回短期有效的 opaque `window_ref`，AI 的 `screenshot.window` 与 `manage_window` 优先使用该引用，不把模型生成的裸 `hwnd` 当作权限凭据。执行时重新核验 HWND、PID、窗口身份、可见性与引用代际/时效，失效返回结构化错误并要求重新列举。通用 `manage_window` 不控制 Blink 自身窗口；Blink 自隐藏只走截图事务。窗口列表/截图继续是 sensitive；改变外部窗口状态属于有副作用能力，按 Capability policy 做授权、确认与审计。

**分层落点**：Win32 枚举、身份验证、cloak、激活与窗口状态原语归 `infra/platform/window`；截图事务与恢复 guard 归 app 编排层；domain 只保留 `manage_window`/`screenshot` 的闭合 schema、policy 与窄语义 port，不获得 AppHandle、任意 label 或通用事件发送权。符合 `spec-architecture.md §A3.5` 的“多入口、单一原子执行语义”。

### 3.13 EnergyVad 自适应升级与语音文本防回退（已定，0.22.15，待实现）

**调研结论**：不以 GGUF FSMN-VAD 直接替换现有 EnergyVad。第一轮 GGUF spike 的当前 worker 接口只能整段离线调用，且每次调用重新加载模型；接到累积音频热路径会放大为重复全段推理，缺少流式 cache/reset。第二轮可增量 ONNX FSMN-VAD 对比中误触发更少，但短句漏检更多，采用门已在 0.22.10 关闭。0.22.15 不新增模型、runtime、worker 参数或安装资产，先升级常驻内存内的 EnergyVad；原始证据继续以 `xtask/spikes/fsmn-vad/decision.md` 和 `xtask/spikes/onnx-spike/followup/decision.md` 为准。

**现状定位**：当前 EnergyVad 只以单一固定 RMS 阈值判静音，没有环境底噪学习和开/关滞回；风扇、键盘、远场低声与麦克风增益变化需要用户手调。伪流式句尾还有一条非原子路径：`SentenceEnd` 先推进 `current_sentence_start`、清空 `latest_preview`，随后才异步请求句子定稿；若定稿为空或失败，上一条非空预览及其音频边界缺少回滚。句子定稿 task 也没有像 preview task 那样携带内部 generation，旧 session 迟到的 pending 结果存在进入新 session 的结构性风险。G1 另有确定的前端接线回归：后端正常结束仍发送 `blink://chord-fill-query`，但 `frontend/js/main-window/lifecycle.js` 中对应 listener 在提交 `e5806c29`（剪贴板独立模式）移除后未为语音终稿保留；录音中的首句只显示在 ghost preview，`VOICE_RECORDING_END` 清理 ghost 后，终稿无法写回 `#query`，用户观察为“空文本吞掉备选文本”。

**EnergyVad 升级边界**：

- 在 domain 内引入有界底噪估计与 speech-on/speech-off 双阈值滞回；只在非语音/低能量帧更新噪声基线，阈值受安全上下界约束，避免把持续人声学成底噪。既有 `silence_threshold` 保留为用户灵敏度基准，`min_silence_ms` 与 `min_sentence_ms` 保持兼容，不静默丢弃旧配置。
- 保留 `process_chunk` 纯内存、同步、无 I/O 的热路径；统计窗口固定容量，稳态不得产生随录音时长增长的分配或扫描。VAD 只负责端点事件，不获得 worker/transport/Tauri 依赖。
- 句尾裁剪与 VAD 共用同一套噪声语义，移除互相矛盾的固定尾静音阈值；增加最小有效语音/短噪声抑制，咳嗽、敲键和瞬时爆音不得单独提交空句。
- 本链路从 session 开始就连续保存全部 PCM，当前没有按 speech-start 丢弃开头，故不为“看起来完整”额外增加 pre-roll；只有未来引入起点裁剪时才重新评估。也不在 `min_silence_ms` 之外叠加不可解释的 hangover，句尾延迟仍由一个可配置预算控制。

**文本提交必须单调**：

- VAD 句尾只创建 pending segment；句子音频边界、confirmed 文本与 preview 清理作为同一事务提交。异步定稿非空才 commit；空串、清洗后为空、错误或超时均不得缩短当前可见文本，也不得不可逆跳过该段音频，留给后续预览/最终 release 重试。
- G1 恢复 `CHORD_FILL_QUERY → search.fillQuery()` 终稿接线，正常结束以 final text 覆盖录音态预览并只触发一次搜索；剪贴板继续使用独立 `CHORD_ENTER_MODE`，二者不再共用业务语义。G3 保留录音开始时的 `voiceBaseText`，G1 同样明确“原有输入 + 本轮语音”的基线策略，空 partial 只能是 no-op。
- domain/adapter/app/frontend 四层都执行 generation/session 校验；前端再以单调 revision（或等价 session token）拒绝迟到事件。`confirmed` 在同一录音代际内只增不减，空事件不得作为清空命令；取消/ESC 才允许按既有产品语义清理。
- 修复采用双层防线：domain 保证不会提前丢音频/预览，frontend 保证空或陈旧事件不能覆盖输入。不得只用 UI 条件判断掩盖底层句子提交丢失。

**观测与回归**：日志只记录 generation、segment/revision、样本数、VAD 转移原因、定稿结果类别（non-empty/empty/error/timeout）与耗时，不记录音频和识别正文。新增确定性状态机测试、伪流式异步定稿测试和主窗事件测试；实机语料至少覆盖安静近讲、远场低声、持续风扇、键盘瞬态、咳嗽、慢速停顿、多句续说、全静音及“说一句后继续按住并长停顿”。

### 3.14 音频解码规范化与 STT Capability（已定，0.22.16，待实现）

**目标与边界**：新增原子能力 `transcribe_audio`，把用户已持有的常见 WAV 音频规范化后交给当前配置的 STT executor，返回转写文本和必要诊断元数据。它不是新的模型引擎、文件播放器、实时录音入口或通用媒体转码器，不复制 VoiceService 的 G1/G2/G3 UI 事件，也不允许调用方选择任意 worker、模型路径或远端 URL。引擎未安装、未配置或不可用时返回结构化可行动错误；Capability 不隐式下载模型。

**唯一规范化链路**：麦克风原生 PCM 与文件 WAV 共用同一套格式转换原语，输入先解析真实容器元数据，再按“采样类型转归一化 f32 → 多声道降混 mono → 有状态重采样 16kHz”收敛为 domain STT 唯一输入。现有 `wav::parse_wav_to_f32` 只在测试构建中按 16-bit/16k/mono 假设读取 data chunk，0.22.16 将其替换为会验证 `fmt`、数据边界、帧对齐和实际时长的正式 decoder；Windows 实时采集中的私有 downmix/resample 原语抽出复用，避免测试、Capability 与麦克风产生三套不同结果。跨 chunk 重采样保持连续相位，不以回调边界重置。

**首版格式闭合**：支持 RIFF/WAVE 中常见的整数 PCM（16/24/32-bit）、IEEE float32 及相应 `WAVE_FORMAT_EXTENSIBLE`，接受常见采样率和单/多声道后统一规范化；8-bit PCM、float64 等只有在真实需求与测试覆盖同时存在时纳入。压缩 WAV、MP3、AAC、FLAC、视频容器和网络 URL 不进首版，遇到未知 codec 返回 `UnsupportedAudioFormat`，不得猜格式或把错误字节交给模型。

**Capability 输入与信任边界**：AI/MCP 等外部出口优先消费经 app 层签发、短期有效的 opaque `audio_ref`；本地 UI/CLI 若允许直接路径，也必须先做既有授权与 policy 检查，再由 app 层解析为受控资源。执行前核验 regular file、引用代际、文件大小、声明/实际时长、声道数、采样率和解码后样本预算，防止路径替换、超大 RIFF chunk、解码炸弹和内存无界增长。domain 只定义闭合 schema、错误与窄端口，不读取路径、不依赖 Tauri/文件系统；infra 负责字节解码，app 负责引用、预算、取消、引擎选择和执行编排。

**执行与结果语义**：`transcribe_audio` 使用执行开始时冻结的 STT 配置、engine/model identity 与 generation；同一请求只调用一个选定 executor。长音频可复用 0.22.15 的 VAD 分段和单调提交规则，但片段只在 app 内编排，最终对外返回一个稳定结果；取消、超时或 engine 切换后迟到结果不得投影。结果至少包含 `text`、`duration_ms`、实际 engine/model 与格式规范化摘要；标点只按模型真实能力返回，不为 Paraformer 伪造 punctuation，日志和诊断不记录音频正文或转写全文。

**语料与隐私**：真实用户录音只允许位于已忽略的本地 `testdata/stt/corpus/`（或用户显式指定的外部目录），不得提交 Git、上传、复制进安装资源或由测试失败输出音频内容。机器可提交的 decoder 单测使用程序生成的无语义波形覆盖各 WAV 格式；本地 corpus 测试由环境变量显式启用、缺失时跳过。文件名只作人类场景标签，期望正文、允许的数字/标点归一化、预期语音段数和 `expected_empty` 由独立 manifest 表达，不从括号或文件名猜测。

---

## 四、复用与范围边界

### 4.1 已有基础设施复用

| 基础 | 0.22 用法 |
|---|---|
| `ConfigStore<T>` 与配置事件 | 引擎偏好、OCR 路由和 STT 模型选择 |
| `OcrBackend` / `OcrResult` | PP-OCRv6/ONNX 接入并保持截图结果契约 |
| WinRT OCR | 默认后端和降级逃生口 |
| 截图 session/epoch | OCR 启动、取消与结果回流竞态墙 |
| 设置页模块/CSS token | 本地运行时卡片、日志、诊断和清理 |
| 事件名常量与结构化错误 | local-engine 状态、日志和可行动错误投影 |
| `ManagedBinaryProvider` / deployment 事务模式 | ONNX Provider 复用其 staging、hash、自检与 active/rollback 设计 |
| `model_storage` 模型资产管理 | ONNX 模型（.onnx + config + 字典）复用此管理 |

### 4.2 不进 0.22

| 项 | 理由 |
|---|---|
| 第三方任意本地引擎注册 | 需要独立签名、授权和任意代码执行信任设计 |
| 通用模型商店 | 下载、版本和校验语义依赖具体工具链 |
| PP-Structure、表格、公式、OCR-VL | 输出协议与资源预算超出截图文字识别 |
| 完整 GPU 调度、多 ASR 同时常驻 | 需要 RAM/VRAM 仲裁和独立产品设计 |
| Linux/macOS 本地引擎 | 当前实现与验收以 Windows 为目标 |
| AI/MCP 安装、启动或清理引擎 | 管理操作尚无外部授权与危险确认模型 |
| MP3/AAC/FLAC、压缩 WAV 与视频音轨转写 | 需要独立 codec 依赖、供应链与解码资源预算；0.22.16 首版只闭合常见无压缩 WAV |

---

## 五、子版本完成摘要

| 版本 | 完成结论 |
|---|---|
| **0.22.0** | PP-OCRv6 thin wrapper、协议、锁、benchmark 和 golden corpus 落盘；资格 `conditional-go`。 |
| **0.22.1** | 通用进程 start/stop/wait、日志洪泛、竞态、异常退出、同步 shutdown 和 Windows 进程树回收闭合。 |
| **0.22.2** | Provider-neutral 安装契约、隔离环境、single-active deployment、事务回滚和引用保护落地。 |
| **0.22.3** | 领域协议、唯一 Manager 和 registry 落地；FunASR 生命周期迁入管理面。 |
| **0.22.4** | PaddleOCR adapter、路由、singleflight、取消/TTL、几何结果和 WinRT fallback 闭合。 |
| **0.22.5** | 引擎页统一状态、日志、模型、空间与清理；事件按 epoch/revision 和 identity 去重。 |
| **0.22.6** | 模型/部署事务、崩溃恢复、OCR 几何裁剪、结构化错误和模块拆分完成。 |
| **0.22.7** | 三模型常驻 GGUF worker 接管 `funasr`，旧 Python/PyTorch/HTTP 链路删除；热词/ITN 清理与唯一模型选择入口两项契约收口（含 PaddleOCR 主动停止误报修复）。 |
| **0.22.8** | OCR ONNX 原位替换：`OnnxRuntime` 贯穿协议/安装/Manager/cleanup，ORT+模型联合 staging、隔离 validator、in-process executor（单 Session、有界 queue、TTL、generation 取消），契约不变。 |
| **0.22.9** | STT 双 runtime：implementation 层、ParaformerOnline ONNX worker（有界二进制音频协议）、FSMN-VAD 可插拔前端；实机 GGUF↔ONNX 双向切模与 lifecycle（100/100 start/stop/cancel、10/10 kill/restart、0 orphan）通过。注册门/采用门证据项按用户决策收口，后由 0.22.10 退役。 |
| **0.22.10** | ASR 侧 ONNX 线路退役；`PythonVenvProvider`、uv 安装链、PaddleOCR Python server 与嵌入资源全部删除（legacy serde 变体与存储页清理路径保留）；UI/UX 打磨两轮（引擎卡/存储页排版、OCR 面板尺寸与引擎标注、`auto` 语义修订、字符级划选、推荐徽章、校验态派生、诊断图标与列表降噪等）。 |
| **0.22.11** | 设置页右上角关闭按钮落地（hide 语义，接线既有 `#close-btn` 空转监听）；编辑器关闭确认三态化，`showCustomDialog` 可选第三动作向后兼容，既有调用零改动。 |
| **0.22.12** | chord 动作可提升为独立全局快捷键：`hotkey/global.rs` RegisterHotKey 注册 + `WM_HOTKEY` 进 effect 通道 → `HotkeyService` → `ChordRegistry::trigger`；OS 试注册 + `binding_conflicts` 内部检查两层冲突探测（补齐 chord 键 vs 主热键缺口）；录制组件泛化支持非 Alt 修饰键；配置热更新重注册、退出注销、注册状态事件回显。 |
| **0.22.13** | 首启向导分步化 4 步（欢迎快捷键 / 常用开关 / 引擎增强 / 完成）：开关 `set_config` 即时生效；OCR 一键下载复用 install 命令与进度事件（`auto` 装完即生效）；FunASR 深链引导；任意退出置 `onboarding_version`；老用户存量配置迁移为未完成（0），升级后补看一次新版向导。 |
| **0.22.14** | 已完成下载进度组件共享、OCR 选区/错误/降级提示收敛与截图 overlay 首建混合 DPI 修正；AI 净化截图（三态 `blink_visibility` + DWM cloak 事务串行化 + `window_ref` 身份二次核验）、`manage_window`（通过 `SurfacePort` trait 调用，不直接依赖 infra）已实现并修复 review P1/P2 问题，通过编译/clippy/单测。 |

### 0.22.15 规划结论（EnergyVad / 语音文本防回退）

- 生产默认继续使用 EnergyVad；FSMN-VAD 不回流 GGUF/ONNX runtime，避免为端点检测引入额外模型、进程和安装面。
- EnergyVad 升级为有界自适应底噪 + 双阈值滞回，并统一尾静音裁剪语义；固定阈值配置平滑兼容。
- 伪流式句尾改为 pending → commit/rollback 事务，空定稿不得推进音频边界或清掉最后非空预览。
- 恢复 G1 语音终稿事件接线，并对空/陈旧 partial 建立前端防线；该缺陷按 P0 数据覆盖风险处理。

### 0.22.16 规划结论（WAV 规范化 / STT Capability）

- 新增 `transcribe_audio` 原子 Capability，复用当前 STT 配置和唯一 executor；不创建第二套模型生命周期，不允许输入任意 worker/模型路径或网络 URL。
- 正式 decoder 解析并验证常见无压缩 WAV，统一输出 16kHz/mono/f32；麦克风与文件复用 downmix 和有状态重采样原语。
- 外部出口使用受控 `audio_ref` 与资源预算；Capability 不隐式安装模型，未知格式与引擎不可用返回结构化错误。
- 自动化 decoder 测试使用可提交的合成波形；真实用户录音仅作本地 env-gated corpus，音频不进 Git，期望值由 manifest 明确记录。

### 最终 release gate（已收口）

- 实机以 0.22.x 制品自 0.21.17 原地升级并走查 Alt+Space 输入、设置页运行时、G1/G2 本地语音、Alt+A OCR/WinRT fallback，profile 沿用；MSI/NSIS 打包清单零 ORT DLL/ONNX 模型/Python，仅随包 3 个 GGUF worker；未加载/ready/识别中/停止后与 OCR 冷/热态的整进程树资源已记录。全新安装与卸载场景按用户决策豁免。
- 0.22.10 清理后由工程门禁复绿兜底（见 §六）。过程 handoff 文档已移除，细节见 git 历史。

---

## 六、验收

最终工程门禁（2026-09-06，0.22.10 收尾）：`cargo fmt --all -- --check`、`cargo clippy --bin blink --all-targets -- -D warnings`（0 warning）、`cargo test --bin blink`（2889 passed / 0 failed）、`node frontend/run-tests.mjs`（56 个测试文件）、`cargo xtask release-check`（版本/资源/许可/GGUF+ONNX 供应链/Python 回流守卫/分层）全部通过。

0.22.11～0.22.13 沿用同一工程门禁全绿（0.22.12 收尾时点 `cargo test` 2905 passed，含新增 wizard 测试）；各子版本逐项验收条件与实机证据见 git 历史（0.22.0～0.22.7 最终审查提交 `99368e0f`，0.22.11～0.22.13 见 `e4d58e96` / `03dcd36a` / `b9942c66` / `c9b94f51`）。

0.22.14（提交 `a018145f`）初版复核：`node frontend/run-tests.mjs` 59 个测试文件全过；`cargo test --bin blink` 2908 passed / 0 failed / 6 ignored。`cargo fmt --all -- --check` 仍报告格式差异，故本次不记录为工程门禁全绿。

0.22.14 review 修复（净化截图并发串行化 + 窗口身份二次核验 + CLI/MCP 兼容性 + 结构化错误映射 + 前端进度校验 + 分层重构）：`cargo fmt --all -- --check` 通过、`cargo clippy` 0 warning、`cargo test --bin blink` 2958 passed / 0 failed / 6 ignored、前端测试（`local-engine-state` 等）全过。待 Windows 实机验收后闭合。

### 0.22.14 追加项验收（AI 净化截图 / 窗口管理）

- [x] `screenshot` schema 支持 `blink_visibility=auto|exclude|include` 且默认 `auto`；非法值与 `exclude + Blink 目标` 返回结构化参数错误。
- [ ] 从 chat 发起全屏、指定显示器和外部窗口截图时，`auto` 不截入 Blink 窗口；`include` 保留现场中的 Blink 窗口。
- [ ] 截取 Blink 自身目标时，`auto` 只保留目标并排除其他 Blink 窗口；确认卡展示最终解析行为。
- [x] 截图事务不调用 `hide_chat_window`、不中止活跃 ChatService；成功、失败、超时和取消路径均恢复所有 cloak、目标原始 minimized/maximized 状态与合理前台焦点。
- [x] 外部窗口被遮挡或最小化时可临时 restore/activate 后正确截图；默认不改变窗口尺寸，截图完成后恢复原状态。
- [x] `list_windows` 产生短期 `window_ref`；过期、代际不匹配、HWND/PID/身份变化均拒绝，AI 重新列举后可继续。
- [x] `manage_window` 仅支持 `activate|minimize|maximize|restore`；`close|move|resize|always_on_top` 不进入首版。（注：`manage_window` 可操作任意窗口，但 Blink 自隐藏只走截图事务）
- [x] 窗口状态副作用走授权、确认和审计；截图/窗口列表敏感策略不降级；AI/本地/CLI/MCP 出口符合各自 policy。
- [x] 覆盖窗口引用、三态策略解析、恢复 guard、每个窗口动作与错误路径的单元测试；前端测试、`cargo fmt --check`、clippy、Rust 测试全绿（2958 passed / 0 failed / 6 ignored）。
- [ ] 完成 chat 发起截图的 Windows 实机验收（手动）。

### 0.22.15 验收（EnergyVad / 语音文本防回退）

- [x] EnergyVad 自适应双阈值落地：固定容量环形能量统计 + P25 低分位 noise floor + 非对称更新（慢升快降）+ on/off 滞回 + attack debounce 抑制脉冲；旧 `silence_threshold` 保留为灵敏度基准，旧 JSON 配置兼容，`min_silence_ms`/`min_sentence_ms` 语义不变。14 个新 VAD 单测覆盖全静音、低声、风扇噪声、阈值抖动、键盘脉冲、咳嗽、多句、不同 chunk size、reset、NaN/Infinity。
- [x] `process_chunk` 无 I/O，稳态无随录音时长增长的无界内存/CPU 开销：固定 150 帧环形 buffer + `try_compact` 回收已 committed PCM（无 pending/deferred 时安全 drain）。连续录音多句后 committed PCM 可回收，`samples` 不永久保存全部历史。
- [x] “非空 preview → 长停顿 → 句子定稿为空/失败/超时 → 继续说话/松键”全路径中，可见文本和 confirmed 均不回退，原句音频仍可在后续预览或最终 release 中重试：pending/commit/rollback 事务化 + identity 校验 + preview 快照保留 + release finalize 尾段 preview 兜底（即使已有 confirmed 也补尾段，做好 prefix 去重）。
- [x] G1 正常结束消费一次 `CHORD_FILL_QUERY` 并触发一次搜索（`search.fillQuery` 只搜一次）；空 partial 不清空 `#query`，迟到 epoch/generation 不写 DOM；G2/G3 既有插入和基线文本语义不回归。前端纯状态模块 `voice-transcript-state.js` + 8 个单测覆盖空/幂等/旧 epoch/confirmed-only 等路径。
- [ ] 全静音松键不写入文本；取消/ESC 不提交终稿；成功、空结果、错误和超时均恰好结束一次录音状态，不残留 ghost preview。（待 Windows 实机验收）
- [ ] `cargo fmt --all -- --check`、`cargo clippy --bin blink --all-targets -- -D warnings`、`cargo test --bin blink`、`node frontend/run-tests.mjs` 全绿，并完成 Windows 实机长按语音验收；记录 sentence-end 与 release-to-final p50/p95，final-after-release p95 继续满足 ≤800ms 门限。（clippy 0 warning、Rust 44 个新单测 + 前端 60 文件全过；Windows 实机验收待完成）

### 0.22.16 验收（WAV 规范化 / STT Capability）

- [ ] 合成 fixture 覆盖 PCM16/24/32、float32、mono/stereo/多声道、8/16/44.1/48/96kHz、奇数扩展 chunk、`WAVE_FORMAT_EXTENSIBLE`、截断/伪造 chunk 与不支持 codec；规范化后的时长、声道、采样率、幅值和帧数在明确误差内。
- [ ] 48kHz 双声道 WAV 经 decoder 后成为 16kHz 单声道 f32，时长不被放大；同一原始样本走测试加载器与 Capability 得到逐样本等价（允许规定的重采样浮点误差）结果。
- [ ] `transcribe_audio` 使用冻结的已配置 engine/model，成功返回非空或语义允许的空文本及真实 identity；未安装、未配置、不支持格式、损坏文件、超预算、取消、超时和切模竞态均返回稳定结构化结果，无迟到投影。
- [ ] AI/MCP 不可用裸路径或 URL 绕过 `audio_ref`/policy；引用过期、代际不匹配、文件身份变化和非 regular file 均拒绝。日志、错误和测试输出不包含音频字节、用户转写正文或可恢复的隐私内容。
- [ ] 本地 11 份用户 WAV corpus 均可规范化，env-gated 测试缺失时安全跳过；全静音与非语音样本不产生正文，短词、远场、背景噪声、长停顿和慢速语音按 manifest 验收。音频文件保持 Git untracked/ignored，发布资源清单不含 corpus。
- [ ] Capability 从 local UI、AI/CLI/MCP 各授权出口走同一原子执行语义；`cargo fmt --all -- --check`、clippy、Rust/前端测试与 release-check 全绿，并记录代表性短/长 WAV 的 decode、queue、inference 总耗时与峰值内存。

---

## 七、关联文件速查

**保留的核心改动**：
- `src/domain/local_engine/` — 引擎身份、描述符、implementation 声明、模型与 adapter 协议。
- `src/infra/local_engine/` — 受管进程、runtime/deployment store、模型存储、Provider（`binary.rs`/`onnx.rs`）、`onnx_ocr/` in-process executor、worker 协议。
- `src/app/local_engine/` — 唯一 Manager、registry、`funasr/` 与 `paddleocr/` adapter、`ocr_coordinator/`、模型安装和事件投影。
- `src/app/commands/local_engine/` — 受限 catalog/lifecycle/model/storage/diagnostics/preferences IPC。
- `src/domain/ocr/` — OCR 契约与输入预算。
- `resources/ocr/paddleocr-onnx/asset-lock.json` — ORT + PP-OCRv6 det/rec/字典供应链真源（只嵌 metadata）。
- `resources/stt/funasr-gguf/worker-lock.json`、`xtask/funasr-worker/` — GGUF worker 与模型供应链真源及构建入口（Nano 继续使用）。
- `frontend/js/settings/tabs/engines/`、`voice.js` — 本地运行时卡片、唯一模型选择入口与 STT 配置。
- `frontend/css/views/settings/settings-local-runtime*.css` — 本地运行时 views 层样式。
- `xtask/src/main.rs` — release-check 门禁（含「Python 栈回流」守卫与 GGUF+ONNX 供应链校验）。

**0.22.10 已删除**：`src/infra/local_engine/providers/python/`、`src/infra/platform/python/`（收敛为 `gpu.rs`）、`resources/ocr/paddleocr/`、`paddleocr/{launch,health,locks}.rs`；`RuntimePlan::PythonVenv` / `ManifestExtension::PythonVenv` serde 变体与存储页 legacy 清理路径保留（旧用户资产安全读取/清理的唯一通道）。

**0.22.11～0.22.13 交互收尾**：
- `src/infra/platform/hotkey/global.rs` — 新建（RegisterHotKey 注册、`GlobalHotkeyStatus`、注销生命周期）。
- `src/infra/platform/hotkey/`（`windows.rs` / `state.rs` / `mod.rs`）— 改造（`WM_HOTKEY` effect 变体与全局键管道）。
- `src/domain/chord/mod.rs` — 改造（per-action 全局键字段、`binding_conflicts` 两层冲突检查）。
- `src/app/service.rs`、`src/app/config.rs` — 改造（全局键分派、`refresh_input_config` 快照）。
- `src/app/commands/`（`config.rs` / `search.rs`）— 改造（全局键配置命令、注册状态查询、录制泛化）。
- `src/domain/config/`（`app_config.rs` / `shards.rs`）、`src/main.rs`、`src/infra/platform/window/windows.rs` — 改造（`onboarding_version` 配置、向导开关分片、welcome 窗口形态）。
- `src/infra/event_names.rs`、`frontend/js/shared/event-names.js` — 改造（`blink://global-hotkey-status` 事件常量）。
- `frontend/js/settings/tabs/chord.js` — 改造（全局快捷键 UI 与注册状态回显）。
- `frontend/js/shared/tauri.js`、`frontend/js/content-editor/main.js` — 改造（`showCustomDialog` 第三动作、编辑器三态确认）。
- `frontend/settings.html`、`frontend/css/views/settings/settings.css` — 改造（设置页关闭按钮、全局键 UI 样式）。
- `frontend/welcome.html`、`frontend/js/welcome.js`、`frontend/js/welcome/wizard.js`、`frontend/css/views/welcome.css` — 改造（4 步向导与 OCR 安装编排）。

**0.22.14 下载与截图体验收尾**：
- `frontend/js/shared/download-progress.js`、`frontend/css/components/download-progress.css` — 新建（欢迎页/引擎页共享的下载进度与 ETA）。
- `frontend/js/settings/tabs/engines/`、`frontend/js/welcome.js` — 改造（环境与模型下载进度消费、陈旧状态清理）。
- `src/app/local_engine/model_installer.rs`、`funasr/gguf_installer.rs` — 改造（GGUF 真实字节进度广播与节流）。
- `src/domain/capability/builtins/ocr_*`、`src/app/local_engine/ocr_coordinator/`、`frontend/js/screenshot/` — 改造（显式未安装降级契约、截图提示、选区与菜单交互）。
- `src/infra/platform/window/windows.rs` — 改造（截图 overlay 会话级混合 DPI 几何钉扎）。

**0.22.14 AI 净化截图 / 窗口管理（已实现）**：
- `src/domain/capability/builtins/screenshot.rs` — 改造（三态 `blink_visibility` 参数、`window_ref` 消费、`resolve_target` 返回 `ResolvedTarget`、`SurfacePort::verify_window_identity` 二次核验、`map_surface_error`/`map_surface_error_detail` 穷尽映射、`runtime_requirement` 降级为 `DESKTOP_SESSION` 恢复 CLI/MCP 兼容性）。
- `src/domain/capability/builtins/manage_window.rs` — 新建（通过 `SurfacePort::manage_window_action` 调用，不直接依赖 `infra::platform::window`；`map_surface_error` 结构化错误映射；`WindowAction::parse` + 状态核验）。
- `src/domain/capability/policy.rs` — 改造（`SurfacePort` 新增 `verify_window_identity` 和 `manage_window_action` async 方法；`WindowActionResult` 结构体；`SurfaceError::WindowStateMismatch` 变体；`DummySurface` stub 实现）。
- `src/domain/capability/mod.rs` — 改造（re-export `WindowActionResult`）。
- `src/app/domain_env.rs` — 改造（`TauriDomainEnv` 实现 `verify_window_identity`（封装 `validate_window_ref_detailed` Win32 调用）和 `manage_window_action`（`spawn_blocking` 封装 Win32 API + 状态核验））。
- `src/app/capture_orchestrator.rs` — 改造/新建（`CAPTURE_TX_LOCK` Mutex 进程级互斥确保 `cloak → capture → restore` 串行化；`CaptureGuard` RAII 恢复 cloak/目标状态/前台焦点）。
- `src/infra/platform/window/control.rs` — 改造（opaque `window_ref` 注册/校验、DWM cloak 查询、`is_maximized`/`is_minimized`/`activate_window`/`minimize_window`/`maximize_window`/`restore_window` Win32 原语）。
- `src/infra/platform/window/mod.rs` — 改造（re-export `is_maximized` 等）。
- `frontend/js/settings/tabs/engines/local-engine-state.js` — 改造（`applyInstallProgress` 增加 `operation_id` 精确匹配校验，防止多模型下载进度串扰）。
- `frontend/js/settings/tabs/engines/local-engine-state.test.mjs` — 改造（新增 `operation_id` 校验测试用例）。
- Capability/feature catalog 标题与设置页投影相关文件 — 改造（`manage_window` 身份、双语短名、出口授权与确认卡摘要）。

**0.22.15 EnergyVad / 语音文本防回退（规划）**：
- `src/domain/stt/vad.rs`、`pseudo_streaming.rs`、`wav.rs` — 改造（自适应 VAD、句子 pending/commit/rollback、统一尾静音语义）。
- `src/domain/config/stt_config.rs`、`frontend/js/settings/tabs/voice.js` — 兼容性改造（保留既有灵敏度与句尾参数；只有确有必要才新增高级参数，不扩大默认 UI）。
- `src/domain/stt/streaming_port.rs`、`src/app/voice.rs` — 改造（空结果与 generation/revision 契约、终稿单次投影）。
- `frontend/js/main-window/lifecycle.js`、`search.js`、主窗口前端测试 — 改造（恢复 G1 终稿接线、空/陈旧 partial 防覆盖、单次搜索）。
- `xtask/spikes/fsmn-vad/decision.md`、`xtask/spikes/onnx-spike/followup/decision.md` — 只读证据，不改写历史实测结论。

**0.22.16 WAV 规范化 / STT Capability（规划）**：
- `src/domain/capability/builtins/transcribe_audio.rs`、STT 窄端口与错误协议 — 新建/改造（闭合 schema、结果、policy 与原子执行语义）。
- `src/infra/platform/audio/`、`src/domain/stt/wav.rs` — 拆分/改造（正式 WAV decoder、共享 downmix、有状态重采样与规范化摘要；domain 不反向依赖 infra）。
- app 层 STT/Capability 编排与 feature catalog — 改造（`audio_ref` 解析、预算、取消、冻结 engine/model identity、executor 调用和出口投影）。
- `testdata/stt/corpus/`、manifest 与合成 WAV fixtures — 本地隐私 corpus + 可提交无语义格式矩阵；真实录音不进 Git、不进 release 资源。

**spike / 实测产物**：
- `xtask/spikes/ppocrv6/decision.md` — PP-OCRv6 资格结论（conditional-go）。
- `xtask/spikes/funasr-runtime/` — GGUF worker 构建与可复现实测产物。
- `xtask/spikes/onnx-spike/decision.md`、`followup/decision.md`（含 `followup/results/` 证据 JSON）— ONNX 路线与 ASR 侧退役决策。
- `xtask/spikes/fsmn-vad/` — FSMN-VAD 调研产物。
