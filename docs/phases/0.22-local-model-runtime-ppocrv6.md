# Blink 0.22 — 本地模型运行时与 PP-OCRv6（从 STT 专用维护到多引擎生命周期）

> **范围**：建立 Blink 托管的多 Provider 本地模型运行时（首版落地 Python venv，并为受管原生二进制预留闭合扩展位），迁移 FunASR 生命周期管理，接入 PP-OCRv6 作为可选 OCR 后端，并在「引擎」设置页提供统一的环境、服务、模型与空间管理；末尾以 STT runtime spike 验证去 PyTorch 路线。
>
> **状态**：🚧 进行中（0.22.0 spike、0.22.1 ManagedProcess、0.22.2 Runtime Provider/generation 与 0.22.5 设置 UI 已完成；0.22.3 代码与自动化验收完成，待 Windows 真实 FunASR 全流程手工验收；PP-OCRv6 仍为 `conditional-go`，后续按资格门收敛生产 runtime；下一步为 0.22.6 硬化与发布验收）。
>
> **动机**：现有 uv、venv、Python server、模型状态和清理逻辑绑定在 STT/FunASR 中；PP-OCRv6 为第二个真实本地模型引擎，继续复制专用命令会形成两套安装、进程和诊断体系。
>
> **前置**：0.10 本地 STT 与 uv 自管理、0.11/0.17 OCR 能力与 WinRT 后端、0.21 Capability 唯一原子执行入口均已完成。
>
> **原则**：不影响 `Alt+Space → Focus → 输入` P0 主链路；domain 不 `use tauri`；引擎环境按 generation 隔离，公共下载资产只在可证明安全时共享；模型由受限 adapter/provider 管理来源与校验，Blink 统一管理运行目录与可观测状态；FunASR 迁移前后行为兼容；未知端口占用进程绝不自动终止；不把 Python、Paddle、ONNX Runtime、llama.cpp 或 vLLM 中任何一种实现误写成所有本地模型必须共用的唯一执行器。

---

## 一、里程碑总览

### 1.1 0.22 版本里程碑

| 版本 | 状态 | 内容 | 优先级 | 依赖 |
|---|---|---|---|---|
| **0.22.0** | ✅ | PP-OCRv6 Windows/CPU 技术 spike、候选资格门与最小服务/请求协议 | P0 | 0.11 / 0.17 |
| **0.22.1** | ✅ | `ManagedProcess`：安全的通用子进程生命周期与日志管道 | P0 | 0.22.0 |
| **0.22.2** | ✅ | Runtime Provider 与 generation：通用安装/回滚契约，首版实现 `PythonVenv` | P0 | 0.22.0 |
| **0.22.3** | 🚧 | 本地引擎领域协议、app 服务与编译期注册表；FunASR 零行为迁移（代码与自动化验收完成，待 Windows 手工验收） | P0 | 0.22.1 / 0.22.2 |
| **0.22.4** | 📋 | PP-OCRv6 正式接入 `OcrBackend`，按需启停与 WinRT 回退 | P0 | 0.22.3 |
| **0.22.5** | ✅ | 「引擎 → 本地模型运行时」设置 UI、通用事件与存储管理 | P1 | 0.22.3 / 0.22.4 |
| **0.22.6** | 📋 | 故障恢复、资源预算、升级/回滚、诊断与发布验收 | P1 | 0.22.5 |
| **0.22.7** | 📋 | FunASR llama.cpp/GGUF轻量链路 spike；通过后注册为新可选 STT引擎 | P2 | 0.22.3 / 0.22.6 |

**依赖关系**：0.22.0 先用第二个真实引擎确定约束并给出 `conditional-go`。0.22.1 与 0.22.2 可并行；0.22.3 汇合二者并以现有 Python/PyTorch FunASR 做零行为迁移，先验证管理层而不同时更换推理引擎；0.22.4 先以配置/诊断命令闭合 OCR 端到端，再由 0.22.5 接设置 UI。0.22.6 硬化可发布底座；0.22.7 使用已落地的 Provider/adapter 边界优先验证官方 llama.cpp/GGUF独立二进制及其轻量 HTTP wrapper，链路与资源门通过后在同一子版本注册为新的可选 STT引擎，不替换或删除现有 PyTorch引擎；主路径不可用时才展开 C++ ONNX备选调研。

---

## 二、背景与动机

### 2.1 为什么 0.22 做这件事

当前本地 STT 已具备一套可工作的 Python 模型生命周期，但职责散落且带有强烈的 FunASR 语义：

- `src/infra/platform/python/mod.rs` 同时硬编码 uv、Python 3.12、torch、funasr 与单一 `venv`。
- `src/domain/stt/funasr.rs` 同时承载脚本释放、启动参数、HTTP/model health 与环境状态投影。
- `src/app/commands/stt/maintenance.rs` 同时持有全局 child、安装环境、轮询模型、转发 Tauri 事件、清理目录、诊断和退出回收。
- `frontend/settings.html` / `frontend/js/settings/tabs/voice.js` 把 Python 环境和服务操作绑定在语音页。
- 服务端口默认固定为 8000；孤儿清理存在“按端口定位并终止进程”的误伤风险，不能复制到多引擎。

截至 0.22 规划时，PaddleOCR 官方资料把 PP-OCRv6 作为通用 OCR pipeline 的默认代际，提供 tiny/small/medium 三档；small/medium 使用单一模型覆盖中文、英文、日文等 50 种语言，tiny 不含日文。具体 PaddleOCR/PaddlePaddle 版本、官方模型 id、模型 revision 与 Windows wheel 兼容矩阵均以 0.22.0 spike 当日验证结果为准，不把会变化的上游事实提前固化为 Blink 契约。它与 FunASR 的框架、依赖、模型缓存、健康协议和运行策略均不同，正好用于验证真正的复用边界。

0.22 的目标不是“把 FunASR 名字替换成 Engine”，而是让 Blink 获得一个受限、安全、可诊断的内置本地模型运行时，同时升级现有 OCR 准确率与多语言能力。

### 2.2 明确“不是问题”的

- **系统 Python 与 Blink 托管 Python 不需要合并**：代码解释器使用用户选择的系统 Python；本地模型运行时只使用 Blink按版本/hash托管的 Python distribution。多个 provider可以引用同一只读托管解释器 artifact，但不读取用户解释器配置，也不共享 site-packages。
- **模型文件不需要进入 Blink 安装包**：FunASR/PaddleOCR 仍按自身机制下载模型；Blink 只指定引擎级缓存根目录、展示占用并观察模型状态。
- **WinRT OCR 不删除**：它零额外下载、启动快，是可靠 fallback；PP-OCRv6 是可选高质量后端，不把 OCR 变成“未安装 Python 就不可用”。
- **设置页内部生命周期命令不必伪装成 Capability**：状态查询、安装进度和日志属于管理 UI 协议。仅当未来确有 CLI/MCP/AI 调用需求时，再把允许外部调用的闭合操作建模为受限 Capability。
- **本地模型不自动全部常驻**：FunASR 延续用户可选自动启动；OCR 默认按需启动并在空闲后停止，不用后台常驻换取低频路径的首击速度。
- **前端仍无 bundler/npm/构建步骤**：0.22 只按现有 settings 模块与 CSS 分层扩展。

---

## 三、核心设计与决策

### 3.1 分层归属 —— domain 定协议、infra 做执行、app 管生命周期（已定）

**定案**：不把带文件、网络、进程与 Tauri 事件的完整 Manager 塞进 domain。

```text
frontend / settings
  └─ 本地模型运行时（底座摘要 + 引擎卡片）
       │ invoke / blink://local-engine-*
       ▼
app/local_engine
  ├─ LocalEngineService（安装/启动/停止/状态/清理编排）
  ├─ EngineRegistry（仅内置 allowlist）
  └─ Tauri command + event projection
       │
       ├───────────────┐
       ▼               ▼
domain/local_engine    infra/local_engine + runtime providers
  ├─ EngineId            ├─ ManagedProcess
  ├─ EngineDescriptor    ├─ RuntimeProvider
  ├─ EngineStatus        ├─ PortProbe / HTTP client
  ├─ LifecyclePolicy     ├─ PythonVenvProvider / UvEnvironment
  └─ LocalEngineAdapter  ├─ ManagedBinaryProvider（协议冻结，按需落地）
                         └─ file/process/cache primitives
```

职责边界：

- `domain/local_engine`：稳定 id、声明、状态类型、错误分类、生命周期策略和引擎特有的启动/健康适配接口；不发送 Tauri 事件。
- `infra/local_engine`：启动/停止子进程、排空管道、PID 身份验证、端口探测、HTTP 请求和目录原语；不读取 app 配置，不反向依赖 domain/app。
- runtime provider：把“如何取得并验证可执行环境”封装在受限 provider 中；`PythonVenvProvider` 负责 uv、Python distribution、venv 和包同步，`ManagedBinaryProvider` 负责锁定 archive/可执行文件/DLL、哈希、CPU feature/driver 前置与 self-test，并可声明只读工具依赖（如 Blink托管 Python stdlib artifact）。工具依赖不因此创建 venv或开放 pip。0.22.2 必须冻结两者可共同表达的 manifest/状态/清理契约，但首版只要求完整实现 `PythonVenvProvider`。
- `app/local_engine`：读取各引擎配置、串行化状态变更、调用 adapter + infra、持有运行实例、广播事件，并在退出时回收所有受管进程。
- 业务消费者仍留在所属领域：STT 通过本地 STT client 请求 FunASR；OCR 通过 `OcrBackend` 请求 PaddleOCR。

### 3.2 环境拓扑 —— generation 隔离，Provider 专属公共资产（修订已定）

**定案**：每个引擎拥有独立、不可变的 generation；Python引擎可共享 uv二进制、uv cache与 uv管理的 Python distribution，但不共享 site-packages。只需标准库脚本的 binary引擎也可以引用同一只读 Python distribution，无需创建 venv；原生二进制引擎可共享经过版本与 SHA-256验证的 runtime artifact（例如同一 ONNX Runtime build），不得共享可变安装目录。保留命名共享环境作为未来显式逃生口，不提供简单 `shared: bool`。

```text
%APPDATA%\blink\python\
├─ tools\uv\
├─ cache\uv\
├─ pythons\
└─ envs\
   ├─ funasr\
   │  ├─ generations\{install-id}\
   │  ├─ staging\{operation-id}\
   │  └─ current.json
   └─ paddleocr\
      ├─ generations\{install-id}\
      ├─ staging\{operation-id}\
      └─ current.json

%APPDATA%\blink\models\
├─ funasr\
└─ paddleocr\

%APPDATA%\blink\runtimes\
├─ shared\{provider}\{artifact-id}\        # 只读、内容寻址、引用计数
└─ engines\{engine-id}\
   ├─ generations\{install-id}\
   ├─ staging\{operation-id}\
   └─ current.json
```

0.22.3 的 FunASR 与 0.22.4 首版 OCR 允许先使用隔离 Python环境；这只是首个 Provider 实现，不是领域契约。`Shared(name)` 不进入本版环境协议；共享 native runtime 只能由编译期 descriptor 引用精确 artifact id，不能由前端指定。

**理由**：torch/FunASR 与 PaddlePaddle/PaddleOCR 的 GPU wheel、NumPy、protobuf、OpenCV 等传递依赖升级节奏不同，共享 site-packages 会令安装、更新、清理和回滚相互污染；而 ONNX Runtime 等 native runtime 只有在版本、provider、ABI、DLL 集合与哈希完全一致时才有安全复用价值。统一的是生命周期和 artifact 管理，不强求所有模型使用同一数学执行器。

### 3.3 状态模型 —— 三维观测，不做线性“三级状态机”（已定）

**定案**：UI 可以展示“环境 → 服务 → 模型”三级，但后端保存彼此正交的状态快照：

```rust
EngineStatus {
    service_epoch,  // 本次 Blink/LocalEngineService 实例的随机 epoch
    desired,        // Stopped | Running
    operation,      // Idle | Installing | Updating | Repairing | Migrating | RollingBack | Cleaning
    operation_id,   // 长操作实例；Idle 时为空
    environment,    // Missing | Ready | Broken { reason }
    process,        // Stopped | Starting | Running { pid } | Stopping | Exited
    service,        // Unknown | Unreachable | Healthy | Degraded
    model,          // Unknown | NotLoaded | Downloading | Loading | Ready | Failed
    revision,       // 仅在同一 service_epoch 内单调递增
    last_error,
}
```

约束：

- 状态版本以 `(service_epoch, revision)` 比较；`revision` 在同一 epoch 内每次状态提交单调递增，前端遇到新 epoch 必须重置旧快照，避免 Blink 重启后较大的旧 revision 压住新状态。
- `desired` 与 observed state 分离；“用户要求停止但进程尚在退出”可以准确表达。
- `env ready` 不推出 `server/model ready`；child 存活也不推出端口上的服务属于该 child。
- 每个 engine id 同时只允许一个变更操作；查询可并发，安装/更新/修复/迁移/回滚/启动/停止/清理必须串行化。长操作携带 `operation_id`、阶段与 `cancellable`，取消只作用于匹配的 operation。
- 错误使用结构化阶段与稳定错误码，日志中保留详细上下文，前端显示可行动文案。

### 3.4 引擎描述符与 adapter —— 数据声明有限，行为显式（已定）

**定案**：描述符声明静态事实，adapter 实现引擎特有行为；不把健康检查和启动逻辑编码成通用字符串 DSL。

描述符至少包含：

- 稳定 `engine_id`、显示元数据与所属能力（STT/OCR）。
- `runtime_kind`、隔离环境和 CPU/GPU 安装 profile；首版允许 `PythonVenv`，协议预留 `ManagedBinary`，不接受前端自定义 kind。
- 锁定安装计划：Python provider 声明 Python/package lock/index/self-test；binary provider 声明 archive、文件清单、SHA-256、可执行入口、所需 CPU feature/driver 与 self-test。
- 环境 schema 版本，以及模型 id/revision 与 server-model 兼容 schema。
- 默认生命周期（manual/auto/on-demand）、启动/模型超时与空闲 TTL。
- 引擎拥有的脚本、配置、日志与模型缓存子目录。
- 资源预算提示与 cleanup policy。

adapter 至少负责：

- 从已校验配置构造 `LaunchRequest`。
- 生成受限环境变量（FunASR 的 ModelScope、PaddleOCR 的模型缓存等）。
- service health 与 model health 的协议映射。
- 安装后 self-test。
- 引擎输出到领域 DTO 的映射。

注册表为编译期内置 allowlist。前端只能传 `engine_id` 与有限动作，不能传 executable、任意 argv、脚本路径或环境变量；0.22 不把它扩展成第三方任意进程托管器。

### 3.5 计算设备 —— 用户偏好、安装 profile、实际后端三层分离（已定）

**定案**：计算设备是每个引擎独立配置，不设全局 CPU/GPU 开关。用户配置保存受限的 `compute_preference = auto | cpu | gpu_auto | cuda | vulkan | directml`；descriptor 只暴露该引擎已验证的选项，并把偏好解析为具体、锁定的 runtime profile；服务 health 必须回报实际后端，不能只展示配置意图。

三层语义：

- **requested preference**：用户意图。`auto` 允许按引擎声明的优先级回退；`gpu_auto` 只在 GPU backend 间选择；显式 `cpu/cuda/vulkan/directml` 失败时返回可行动错误，不静默换后端。
- **resolved profile**：具体 artifact 与兼容合同，例如 `cpu-x64`、`cpu-avx2`、`cuda12-sm86`、`vulkan-x64`、`directml-x64`；profile 决定下载内容、DLL、execution provider、CPU feature/driver 前置、启动参数与 self-test。
- **actual backend**：进程启动后由 token health 回报 `actual_backend/device_name`；状态同时保留 requested、resolved、actual 与 fallback reason，UI 不得把“检测到 NVIDIA/AMD”当作实际已启用加速。

每个 adapter 声明自己的候选顺序和兼容矩阵。例如 llama.cpp 可声明 `cuda（精确架构匹配）→ vulkan（实机 self-test）→ avx2 → cpu`，ONNX引擎可声明 `cuda/directml → cpu`；这不是跨引擎全局顺序。硬件/API探测、artifact兼容检查和最小 self-test 全部通过后才可解析为可用 profile。

profile切换原则上视为新 generation：在 staging安装并验证目标 profile，停止旧实例后原子切换；启动验证失败回滚上一 generation。即使某个 archive同时携带 CPU/Vulkan backend、只需更改启动参数，也必须经过同样的解析、self-test、状态回报与回滚语义。

### 3.6 安装与更新 —— staging、自检、原子指针切换、可回滚（已定）

安装流程：

```text
解析内置 lock/profile
  → provider 准备锁定 runtime artifact
  → 创建 engine/staging/{operation-id}
  → PythonVenv: 确保 uv/Python、创建 venv、同步锁定依赖
    或 ManagedBinary: 下载/解包锁定 archive、验证文件/DLL/CPU feature
  → provider version/self-test
  → 写 generation manifest（含依赖与模型契约）
  → 停止旧实例
  → staging 提升为不可变 generations/{install-id}
  → 原子替换 current.json 小文件指针
  → 本次启动验证失败则原子切回上一 generation
```

要求：

- 依赖真源必须锁版本；不得在生产安装路径使用无上界 `latest`。
- CPU/GPU profile 分开，profile 切换视为环境重建，不在原 venv 原地覆盖框架 wheel。
- 不用目录覆盖伪装“原子切换”；Windows 文件占用、杀软扫描或任一步崩溃后，启动恢复逻辑必须能从 `current.json` 与完整 generation manifest 选出最后一个有效 generation。
- 安装失败不破坏当前 generation；取消只清理匹配 `operation_id` 的 staging。仍被进程占用的旧 generation 标记 deferred cleanup，不强删。
- manifest 通用部分记录 engine id、schema、runtime kind、profile、artifact id/hash、安装时间、模型 id/revision/checksum 来源及兼容 schema；Python扩展记录解释器与包版本，binary扩展记录 archive/可执行文件/DLL及其哈希。状态检查使用 manifest + provider self-test 双重验证。每引擎至少保留当前与上一有效 generation，额外旧代按 cleanup policy 回收。
- 清理只作用于该引擎声明拥有的 generation/model cache；公共 uv/Python/cache 或共享 native artifact 使用独立清理与引用检查，不得顺带删除其他引擎仍在使用的资产。

### 3.7 进程与端口安全 —— PID 身份优先，未知占用不杀（已定）

`ManagedProcess` 提供：

- stdout/stderr 同时异步排空，避免管道背压死锁。
- 有界日志环形缓冲与结构化生命周期事件。
- child handle、PID、可执行路径、引擎 token 与启动时间身份记录。
- 启动超时、退出监听、优雅停止 deadline、超时后只终止已验证的进程树。
- Blink 正常退出时统一回收；异常退出后的重连/清理必须先验证身份。
- Windows 生产实现优先使用 Job Object（kill-on-job-close）约束受管进程树；每次实例同时写原子 lease，记录 engine/instance id、PID、进程创建时间、可执行路径、endpoint 与 token fingerprint。重启后只有 lease、进程创建时间/路径和带 token health 全部匹配才可接管或终止；验证失败只报告遗留项，不自动 kill。

端口规则：

- 服务仅绑定 `127.0.0.1`。
- 配置保存 preferred port；启动时检测冲突，必要时从受控范围选择新端口并更新本次 endpoint。
- “探测空闲”与子进程 bind 之间仍可能竞争；遇到 address-in-use 时限次重新分配并重启。
- 每次启动生成随机 token，健康与业务请求均携带 token；健康响应回显 engine id / instance id，用于确认端口服务身份。
- 只有 PID + 可执行路径/instance token 与受管记录一致时才能终止；未知进程占用端口只报冲突或换端口。

0.22.3 必须删除 FunASR 现有“仅凭端口定位 PID 后自动 kill”的路径。

### 3.8 生命周期策略 —— FunASR 可自启，OCR 默认按需 + 空闲回收（已定）

| 引擎 | 默认策略 | 首次调用 | 空闲策略 |
|---|---|---|---|
| FunASR | Manual；沿用用户现有 `auto_start_server` | 未运行时保持当前可行动错误提示，不在录音手势热路径隐式长等待 | 自动启动者随 Blink 生命周期；手动启动者手动停止 |
| PaddleOCR | OnDemand | OCR 请求在同一次 `recognize` future 内触发并等待单飞启动；loading/progress 走旁路事件，不复制或“重放”图片请求 | 默认 5 分钟无请求停止，可配置为“保持运行”或“使用后立即停止” |

约束：

- OCR 启动不得阻塞 Alt+Space、截图 overlay 展示或前端事件线程。
- 相同引擎并发首次调用合并为一个启动 future，禁止重复拉起。
- `OcrBackend::recognize` 扩展受限请求上下文，至少携带 deadline/cancel token/origin；截图 Interaction 额外持有自身 session/epoch，`ocr_image` Capability 只使用调用 deadline/cancel，不伪造截图 session。
- OCR 回流继续遵循截图 session/epoch 校验；旧选区启动的识别结果不得覆盖新选区。等待启动期间只持有单份请求图片，session 取消或 deadline 到期后不得再发业务请求。
- 空闲计时只在 in-flight 请求归零后开始；停止与新请求竞争时，新请求提升 `desired=Running` 并取消空闲停止。

### 3.9 OCR 后端路由 —— PP-OCRv6 可选，WinRT 永久回退（已定）

新增 OCR 配置：

- `backend = auto | windows | paddleocr`，升级与全新安装均默认 `windows`；安装 PP-OCRv6 不隐式改变现有 OCR 路径，由用户明确切换。`auto` 是显式选择，不是默认值。
- `paddle_model` 的稳定配置值以 0.22.0 验证后的官方模型 id 为真源；UI 可显示 tiny/small/medium 友好档位。桌面候选由 benchmark 定案，不预设 `small` 必然合格。
- `compute_preference` 遵循 §3.5；0.22 首发必须保证 `auto/cpu`，CUDA/Vulkan/DirectML 只有在对应 Windows artifact、驱动矩阵与 self-test 验证通过后才由 descriptor 开放。
- `lifecycle = on_demand | keep_running | stop_after_use`。
- 文档方向/矫正/行方向等增强模块默认关闭，截图 OCR 只启检测 + 识别；后续按真实收益开放。

路由语义：

- `windows`：始终使用当前 WinRT backend。
- `paddleocr`：明确选择 PaddleOCR；未安装/启动失败时返回可行动错误，不静默改变用户选择。
- `auto`：仅当 PaddleOCR 环境已安装且 service/model 已热态 ready 时优先使用 PaddleOCR，否则立即使用 WinRT，不为一次普通 OCR 引入隐式冷启动等待；PaddleOCR 在单次识别中崩溃可降级 WinRT，并记录/展示降级原因。
- 每次识别开始时快照 `Arc<dyn OcrBackend>`，切换配置不改变已经在途的请求。
- PaddleOCR 输出必须完整映射现有 `OcrResult` / line / word rect 契约，前端阅读模式、翻译、pin 与 overlay 不感知后端差异。

### 3.10 模型与缓存边界（已定）

Blink 不实现通用模型下载器。adapter 设置引擎专属缓存根目录并从服务健康协议观察 `NotLoaded/Downloading/Loading/Ready/Failed`；但 descriptor/manifest 必须锁定并记录可复现的模型身份，不能只锁 Python 包而让模型随安装时间漂移。

Blink 负责：

- 下载前展示预计体积（若描述符可知）与网络提示。
- 传入 descriptor 锁定的 model id/revision，校验服务报告的实际 revision 与兼容 schema；上游不提供稳定 checksum 时记录下载来源与上游 revision，并在 spike 结论中明确可复现性限制。
- 展示引擎 venv、模型缓存、日志和公共缓存占用。
- 打开引擎目录、停止后清理该引擎缓存。
- 模型 ready 前不发送业务请求。

引擎负责：

- 下载源、断点续传、文件校验、模型版本解析与加载；不得擅自把“最新模型”替换为 descriptor 锁定值之外的 revision。
- 将可观测阶段通过 health 协议或服务日志暴露；无法取得字节级进度时只展示阶段，不伪造百分比。

### 3.11 设置页信息架构（已定）

在现有「引擎」页的“代码解释器”下方新增“本地模型运行时”，不新开顶级 tab：

```text
本地模型运行时
├─ 运行时底座（默认折叠）
│  uv / 托管 Python / 公共缓存 / 根目录
├─ 语音识别 · FunASR
│  环境 / 服务 / 模型三段状态，安装、修复、启动、停止、日志、清理
└─ 文字识别 · PP-OCRv6
   后端选择、模型档位、设备、生命周期、安装、启动、停止、测试、日志、清理
```

“代码解释器 Python”文案明确为“系统/用户解释器”；“本地模型运行时”明确为“Blink 托管环境”，避免用户误解 Blink 会修改其 Python。

引擎卡片使用同一 renderer 和状态协议，但允许 adapter 提供有限的额外配置区；不为每个引擎复制一套安装按钮与事件监听。状态事件包含 `engine_id + service_epoch + revision + snapshot`，日志事件包含 `engine_id + instance_id + seq`。

---

## 四、复用与范围边界

### 4.1 已有基础设施复用盘点

| 现有基础 | 0.22 用法 |
|---|---|
| `infra/platform/python` | 保留 uv 下载、Python 3.12 与路径基础，拆除 torch/funasr 硬编码 |
| `domain/stt/funasr` | 保留 STT HTTP 协议、模型参数和文本后处理语义；生命周期迁出 |
| `app/commands/stt/maintenance` | 作为迁移清单；通用部分下沉，STT 诊断保留薄适配 |
| `domain/capability/builtins/ocr_engine::OcrBackend` | PP-OCRv6 接入点；保持现有 `OcrResult` 契约 |
| WinRT `PlatformOcrBackend` | `windows` 后端与 `auto` fallback |
| `ConfigStore<T>` / `blink://config-changed` | 保存 OCR backend、model、device 与生命周期策略 |
| settings tab 模块化结构 | 新增本地运行时模块；复用卡片、badge、折叠与存储统计视觉语言 |
| 事件名常量模块 | 新增通用 local-engine status/log 事件，删除新增 FunASR 专属事件的需求 |
| 现有截图 OCR session/epoch | PaddleOCR 异步启动与结果回流继续使用同一竞态墙 |

### 4.2 不进 0.22 的项

| 项 | 理由 |
|---|---|
| 第三方插件注册任意本地 Python 引擎 | 会把受限内置 registry 扩大为任意代码执行/进程托管面，需独立信任与签名设计 |
| 通用模型商店/模型下载器 | 下载、校验和版本语义高度依赖工具箱；0.22 只统一缓存根与状态观察 |
| PP-StructureV3、表格/公式/文档解析 | 与截图文字识别的输出协议、资源预算和产品入口不同，另立能力评估 |
| PaddleOCR-VL / 多模态大模型 | 资源体积和结果语义超出轻量 OCR 范围，继续留在 roadmap |
| 把 ONNX Runtime、llama.cpp 或 vLLM 定为唯一执行器 | 三者分别面向通用 ONNX 计算图、已适配的 GGUF/ggml 架构和高吞吐生成式模型；0.22 统一管理面，不伪造执行能力交集 |
| 本地 LLM/Embedding 产品接入与 GPU 调度 | Provider/adapter 协议为其留扩展位，但多模型 RAM/VRAM 仲裁、抢占、session/KV cache 与产品入口应另立 phase |
| Linux/macOS 本地引擎支持 | 当前产品和进程实现以 Windows 为目标，但协议避免写死 Win32 类型 |
| 自动决定/安装 CUDA 驱动 | Blink 只探测并选择已验证 profile，不修改系统驱动 |
| 多引擎共享 site-packages | 0.22 明确隔离；本版不预埋 `Shared(name)` 协议，只有未来有真实兼容组合时再扩展 |
| 删除 WinRT OCR | 保留零安装 fallback 与排障逃生口 |
| AI/MCP 安装、启动或清理引擎 | 管理操作暂不开放外部出口；以后需单独做授权与危险确认 |

---

## 五、分子版本设计

### 5.1 0.22.0 — PP-OCRv6 spike、候选资格门与协议冻结

> **完成状态**：✅ spike 已实跑并形成 `conditional-go`；18/18 benchmark 运行成功，tiny corpus CER 为 0.1492（WinRT 0.5218），word rect 253/253 有效。热识别约 4s 未过 2s 门，峰值工作集约 1136MB 接近 1.2GB 门，缓存损坏场景未完成，详见 `decision.md`。
>
> **实测版本**：Python 3.12 / PaddlePaddle 3.1.0 / PaddleOCR 3.7.0 / PaddleX 3.7.2；精确安装输入与结果以 spike lock/result 文件为准。
>
> **官方模型 id**：PP-OCRv6 tiny/small/medium（PaddleOCR 2.9.x 默认 pipeline）；精确 revision 和 checksum 以首次下载时 PaddleOCR 实际拉取的 URL 为准。

目标是先回答会改变架构的事实问题，不提交正式产品入口，也不改现有 WinRT OCR 路由。0.22.0 是后续版本的 go/no-go 门：只有形成可复现证据并明确记录 `go`，0.22.1～0.22.6 才以 PP-OCRv6 作为第二引擎继续；`no-go` 时保留 spike 产物并重新评估更轻引擎或只做 FunASR 生命周期收敛。

产物与实验要求：

- 在 Windows x64 CPU 上锁定可安装的 uv-managed Python/PaddlePaddle/PaddleOCR 组合，记录精确版本、index、wheel 来源与 SHA-256；不依赖系统 Python。
- 用仓库内可复现脚本比较 thin Blink HTTP wrapper、PaddleX basic serving、单次 worker 三种拓扑；所有拓扑使用同一输入集、模型、预热规则和采样脚本。
- 建立不含隐私数据、可再分发的 Blink OCR golden corpus，覆盖中文/英文/日文混排、竖排、小字号、浅色/深色 UI、缩放与混合 DPI；保存期望文本、line/word/rect 标注或明确的几何判定规则。
- 测量 tiny/small/medium 的 venv/模型/总磁盘、缓存已存在时的 service/model 冷启动、首次/热识别、CPU、峰值/稳定内存和停止后回收；每项至少 10 次，报告 P50/P95，记录机器 CPU/内存/Windows 版本与电源模式。
- 验证官方 `return_word_box` 等能力能否稳定产出当前 `OcrResult` 所需 line/word/物理像素 rect；不得用不可验证的平均分替代现有阅读模式的双向高亮契约。
- 验证模型缓存重定向、首次下载阶段、断网离线复用、缓存损坏、revision 不符和上游下载失败；记录官方模型 id、revision、来源与可取得的 checksum。上游无法稳定锁 revision/checksum 时，必须把它列为残余供应链风险。
- 先冻结最小服务协议草案：带 token 的 `/health`、`/recognize`、`/shutdown`，health 回显 protocol version、engine id、instance id、service/model state、model id/revision；recognize 使用一次请求 future，支持 request id、deadline/cancel 语义，不设计图片“重放”队列。
- 输出 `decision.md`（或等价报告），逐项给出原始数据位置、候选拓扑/模型、`go | conditional-go | no-go` 与理由；不得只写“测试通过”。

PP-OCRv6 桌面候选资格门（规划阈值；任一项不满足必须 `conditional-go/no-go`，不得静默放宽）：

| 维度 | 资格门 |
|---|---|
| P0 隔离 | spike 产物不接入启动 wiring；未运行 benchmark 时不新增后台 Python 进程，Alt+Space/focus/首结果指标无可测回退 |
| 冷/热延迟 | 选定桌面模型在缓存已存在时，service+model ready P95 ≤ 10s；1440p 截图热识别 P95 ≤ 2s |
| 资源 | OCR child 稳定工作集 ≤ 750MB、峰值 ≤ 1.2GB；停止后 Python 进程归零；选定 CPU venv+模型总磁盘 ≤ 2.5GB。Blink 主进程仍遵守 `<300MB` 目标，子进程预算单独报告，不用拆进程规避统计 |
| 准确率 | 相比当前 WinRT，在加权 golden corpus 上字符错误率相对下降 ≥ 10%，或在 WinRT 明显薄弱的日文/竖排子集下降 ≥ 20% 且整体字符错误率退化不超过 3% |
| 几何契约 | ≥ 99% 的有效识别词具有非空、有限、落在图像范围内的 rect；抽样阅读模式双向高亮无系统性坐标/DPI 偏移 |
| 可复现/离线 | 全新安装可由锁定输入复现；完整缓存后断网可启动识别；损坏或 revision 不符能确定性失败并给出修复路径，不静默换“最新”模型 |

默认选型倾向 thin wrapper：Blink 是单用户低并发桌面应用，不默认引入完整 PaddleX serving；这只是待验证假设。若官方 serving 在可靠性或性能上明显更优可选官方方案；若所有方案未过资格门，结论必须是 conditional-go/no-go，而不是为迁就既定路线修改数据。

### 5.2 0.22.1 — ManagedProcess

**状态：✅ 已完成。**

- 已在 `infra/local_engine` 落地通用 `ManagedProcess`，统一 start/stop/wait、状态、日志与退出清理；基础设施层不理解 FunASR/OCR/Provider，也不依赖 Tauri。
- 生命周期采用 `InstanceToken（generation + instance_id）` 与 operation-in-lock 模型；并发 start/stop 共享持久化完成结果，迟到任务无法覆盖新 generation，stop 返回时不遗留 `Stopping`。
- 日志使用固定分块读取、bounded `LineAccumulator`、ring buffer 与 bounded broadcast；超长无换行输出也不会造成无界内存增长。
- Windows 使用 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` 回收进程树；终止前按可执行路径与 OS creation time 做 fail-closed 身份校验。
- 端口冲突仅探测并报告，不再依据端口终止未知进程；FunASR 已迁移到 `ManagedProcess`，其参数、脚本路径、噪声过滤与模型健康检查仍保留在业务层。
- 已建立真实子进程行为测试与确定性 spawn/commit gate，覆盖并发启动、并发停止、Starting 期间停止、日志洪水、异常退出、generation 隔离及同步 shutdown。
- 最终验收：`cargo check --bin blink`、`cargo fmt --check`、`git diff --check` 均通过；`cargo test --bin blink` 为 **2068 passed、0 failed、6 ignored**。

### 5.3 0.22.2 — Runtime Provider 与 generation（首版 PythonVenv）

**状态：✅ 已完成。**

- 冻结 provider-neutral 的安装计划、generation manifest、artifact identity、self-test、状态和清理契约；`RuntimeKind` 首版允许 `PythonVenv`，为受管 `ManagedBinary` 保留闭合变体，但不实现任意 URL/executable 托管。
- 路径迁移为 provider 公共资产 + `engines/{engine_id}/generations`；兼容识别旧 `python/venv`。
- 完整实现 `PythonVenvProvider`：uv、Python distribution、隔离 venv、package lock/index/import self-test；`PythonEnvStatus` 改为 provider 状态 + `packages: Vec<PackageStatus>`，torch/funasr 投影留在 FunASR adapter。
- 把 uv获取的锁定 Python distribution建模为可被 provider引用的只读共享 artifact；支持“stdlib only”工具依赖，启动时使用隔离参数且不创建 venv、不执行 pip、不读取用户代码解释器。
- 建立按 operation 隔离的 staging install、provider self-test、`current.json` 原子指针切换和失败回滚。
- 实现 §3.5 的 per-engine `compute_preference → resolved profile → actual backend` 契约；CPU/GPU/provider profile 使用独立 lock/index/artifact 规则，显式后端失败不静默回退。
- 清理 API 明确 engine generation、engine model cache、provider shared artifact/download cache 三种 scope；共享项必须做引用检查。
- 旧 FunASR venv 采用一次性受控迁移：可验证兼容则登记/移动，不兼容则提示重建；不得静默删除用户现有模型缓存。
- Windows 小文件指针使用 `MoveFileExW(REPLACE_EXISTING | WRITE_THROUGH)` fail-closed 原子替换；切换后重新执行 promoted generation 的 provider self-test，失败则切回 previous 并把新代登记为 deferred cleanup。服务启动/token health activation 由 0.22.3 `LocalEngineService` 消费同一契约。
- `RuntimeProvider` 为异步契约，Python 安装链路不再在 Tokio runtime 内嵌 `block_on`；包 hash 通过带 `--hash=sha256:` 的 requirements 输入交给 uv 强校验，解释器 artifact 记录实际文件 SHA-256。
- 旧 `PythonEnvStatus` 已移除 torch/FunASR/CUDA 专属字段，infra 只报告 provider 基础状态与通用 packages；FunASR 兼容命令在 STT 层投影专属诊断字段。
- 验收结果：`cargo fmt --check`、`cargo check --bin blink`、`git diff --check` 均通过；`cargo test --bin blink` 为 **2120 passed、0 failed、6 ignored**。

### 5.4 0.22.3 — 协议、服务与 FunASR 迁移

**状态：🚧 代码与自动化验收完成，待 Windows 手工验收。**

- 已落地 framework-neutral 的 domain 状态、描述符和 adapter 协议，以及进程级唯一的 `LocalEngineService` 与编译期 registry；通用状态以 `engine_id + service_epoch + revision` 发布。
- 已注册 `funasr` adapter，统一迁移环境安装、启动、两阶段健康与 Model Ready 验证、日志实例隔离、停止、空间、诊断和清理；服务身份通过环境变量注入，health 与转录请求统一使用 `X-Engine-Token`，并核对 engine、instance、token fingerprint 与 endpoint。
- 现有 STT 专属 commands 与旧 FunASR 状态/日志事件保留为兼容薄投影，不创建第二套 polling 或状态真源；配置 key、模型选择、热词、ITN、VAD、HTTP 请求与自动启动语义保持兼容。
- `SERVER_RUNNING` 不再充当生命周期真源；未知端口占用不会触发按端口杀进程，应用退出通过独立同步 process registry 尽力回收全部 managed instances。
- start 的失败路径统一 rollback；日志 pump 通过 cancellation、实时实例身份和 broadcast 生命周期隔离旧实例。环境 Ready 基于受管 `current.json`、manifest 与 self-test，不以目录存在代替。
- 自动化验收：`cargo check --bin blink`、`cargo fmt --all --check`、`git diff --check` 通过；`cargo test --bin blink` 为 **2268 passed、0 failed、6 ignored**。仍需在 Windows 验证真实安装到识别、旧兼容事件、未知端口保护及退出后 Job Object 进程树归零。
- 非阻断收尾项：两阶段健康轮询已使用 descriptor timeout；后续可将模型阶段 deadline 精确改为从 Phase 1 首次通过时起算，使 `model_load_timeout` 的阶段语义更严格。

### 5.5 0.22.4 — PP-OCRv6 正式接入

- 注册 `paddleocr` adapter、锁定依赖和嵌入 thin server 脚本/配置。
- 新增 `PaddleOcrBackend` 与 `OcrBackendRouter`，扩展受限请求上下文并保持 `OcrResult` word rect 契约。
- 配置 `auto/windows/paddleocr`、模型档位、设备和生命周期。
- 按需启动采用 singleflight；同一次请求 future 等待 ready 后继续，不复制/重放图片；取消/过期 session 不提交结果。
- 完成 idle TTL 回收、in-flight 保护和停止/新请求竞态处理。
- 默认 `windows`；显式 `auto` 仅在 PaddleOCR 已热态 ready 时选择它，否则立即使用 WinRT；显式 `paddleocr` 未安装/启动失败时返回带安装入口的结构化错误。
- 保留 OCR 诊断：当前 backend、模型、service/model state、fallback 原因、单次耗时与语言信息。

### 5.6 0.22.5 — 设置 UI 与通用事件

- 在 engines tab 增加本地模型运行时 section，移动语音页的环境/服务管理 UI；语音页仅保留语音业务配置及跳转入口。
- 新增 `get_local_engine_catalog/status/logs/storage` 与 install/start/stop/repair/cleanup 管理协议；命令按 `engine_id` allowlist 路由。
- status 监听以 `engine_id + service_epoch + revision` 去重，日志以 `engine_id + instance_id + seq` 去重；页面切换和销毁时解除监听。
- 长操作展示 operation id、阶段、可取消性和明确错误，不伪造下载百分比。
- 清理弹窗列出准确 target generation/cache、预计释放空间和影响；公共缓存与引擎环境分开确认。
- 中英文 i18n、Lucide 图标、主题 token、无 inline display 新债。

### 5.7 0.22.6 — 硬化与发布验收

- 覆盖 Blink 崩溃、server 崩溃、端口被抢、模型损坏、断网安装、磁盘不足、安装取消、旧环境升级和日志洪泛。
- 验证主链路性能不回退；本地引擎状态探测、自动启动和空间扫描都在后台运行。
- 复验 0.22.0 已建立的资源预算与默认策略：只启动用户正在使用的引擎，OCR 空闲回收；不得到发布硬化阶段才首次决定预算。
- 安装包不包含模型；发布构建必须包含脚本、lock、manifest schema 与必要许可信息。
- 完成 diff 自审、单测、Rust 编译、前端静态契约检查及 Windows 手动矩阵。

### 5.8 0.22.7 — FunASR llama.cpp/GGUF轻量链路 spike 与条件接入

本节采用“先 spike、门通过后条件接入”两段式：首要且默认验证的链路是官方 `llama-funasr-*` Windows独立二进制 + GGUF模型 + 上游 `funasr_gguf_server.py` 轻量 HTTP wrapper，复用 Blink当前 OpenAI-compatible transcription client。现有 Python/PyTorch FunASR只作为回退与资源对照，不做准确率基线；本节不设置 CER/WER门，不因文本差异阻断链路验证，但必须用固定音频确认返回非空、UTF-8合法且协议可解析。

阶段 A 只验证链路与资源。若安装、启动、请求、取消、停止、清理、离线复用和资源报告全部闭合，阶段 B 在同一子版本注册新的 `funasr-gguf`（稳定 id 以实现时 descriptor为准）可选 STT引擎；不静默迁移用户配置，不替换或删除现有 `funasr` Python/PyTorch引擎。若主路径因 Windows兼容、延迟/进程抖动、资源或协议问题不可用，再把 C++ ONNX常驻 2-pass server作为备选 spike，不要求本子版本同时完成两条路线。

候选边界：

- **首要链路 — llama.cpp/GGUF + 轻量 HTTP wrapper**：官方 Windows自包含 runtime、SenseVoice Q8（首个模型）与 FSMN-VAD按锁定 release/hash安装；wrapper只用 Python标准库，暴露 `POST /v1/audio/transcriptions` 并可转发受限的 `--backend cuda|vulkan`。首版由 `ManagedBinaryProvider` 引用 uv基础设施已锁定的 Blink托管 Python distribution，以 `python -I -S` 隔离模式运行 wrapper；不创建 venv、不安装 pip包、不读取用户代码解释器。后续可由 Rust实现等价薄 wrapper，两者的 C++推理路径相同。
- **备选链路 — C++ ONNX Runtime**：仅在首要链路不合格时展开。ONNX Runtime本身是通用执行层，可由 OCR与 STT分别创建独立 session；只有精确版本、execution provider、ABI与 DLL集合一致时才共享同一只读 runtime artifact。模型、前后处理、进程与内存仍按引擎隔离。
- **模型覆盖不预设为三者等价**：FunASR C++ ONNX 常驻服务当前重点验证 Paraformer online/offline 2-pass 组合；SenseVoice、Paraformer、Fun-ASR-Nano 三种模型是 llama.cpp/GGUF release 明确列出的三个 runtime 入口。每个模型是否存在功能等价、可发布的 ONNX 路径须逐项验证，不能因 Python `AutoModel` 可加载就视为 C++ server 已支持。
- **每请求子进程是真实约束**：上游 HTTP wrapper常驻但模型不常驻，每个 transcription请求都会 spawn推理进程。Blink当前 final-only链路可能可接受，但必须测量首次、连续短句、取消和两个并发请求；不得把 HTTP health可达写成 model warm。伪流式若导致频繁整段重识别，`funasr-gguf` 首版应明确只提供 final，不能用重复 spawn伪造 partial。
- **vLLM**：是面向 Fun-ASR-Nano等生成式模型的高吞吐服务后端，不执行传统 ONNX 图，也不是 OCR/STT 共用的 native runtime。其 Python/CUDA/服务体积与桌面约束单独计量，默认不进入轻量 Windows候选；只作为有明确 GPU吞吐需求时的对照项。

阶段 A 实验要求：

- 固定 Windows x64目标机、runtime/model revision、输入音频与采样口径，先跑 SenseVoice Q8 + FSMN-VAD；至少比较直接 CLI/Rust spawn和上游标准库 HTTP wrapper，两者都按每请求新进程统计。F16、Paraformer、Nano、C++ ONNX和 vLLM均不是本阶段必测项。
- 测量 runtime/模型/托管 Python distribution增量与总磁盘、wrapper ready、单次端到端 final、连续短句与两个并发请求的 P50/P95、实时率、CPU、稳定/峰值 RAM/VRAM、停止回收和进程抖动；若该 Python artifact已被其他引擎引用，仍分别报告物理总占用与本引擎增量。该拓扑不定义独立的 model-ready或 partial指标。
- 用固定短音频做协议 smoke：HTTP 200、JSON shape正确、文本非空且 UTF-8合法；不计算 CER/WER，不比较 PyTorch文本，不把准确率作为 go/no-go。
- 验证 final语义、FSMN-VAD开关、超时/取消、wrapper重启、两个并发请求、迟到结果隔离、临时文件回收、子进程归零和完整缓存后的断网运行；热词、ITN、时间戳和 partial只记录能力现状，不作为首版接入门。
- 产出 Windows自包含包清单、许可/SBOM与精确 hash，证明推理环境无需 Docker、系统/用户 Python、PyTorch、torchaudio、FunASR Python包和管理员权限；单独报告共享托管 Python distribution的物理体积与本引擎增量。
- 输出 `decision.md`，结论为 `adopt-llamacpp-http | fallback-cpp-onnx-spike | no-go`，记录全部原始资源数据和已知功能限制。

阶段 B 条件接入要求：

- 使用 `ManagedBinaryProvider` 安装锁定的 runtime archive和 GGUF模型，轻量 wrapper作为引擎自有脚本，并引用锁定的共享托管 Python stdlib artifact；禁止为它创建 venv或安装 torch、torchaudio、funasr及任何 pip包。
- 注册独立 engine id和配置卡片，复用现有 STT OpenAI-compatible client；首版声明 final-only，不冒充 realtime/2-pass。
- 默认不自动切换现有用户；用户显式选择后才使用新引擎，失败时返回可行动错误并允许手动切回 Python FunASR。

---

## 六、验收

### 6.1 0.22.0

- [x] 仓库内有可复现的 Windows CPU 安装锁、SHA-256 与 spike/benchmark 脚本，不依赖系统 Python，不改生产 OCR wiring。
  - `xtask/spikes/ppocrv6/install.ps1` — 环境安装（uv/venv/PaddlePaddle/PaddleOCR）
  - `xtask/spikes/ppocrv6/requirements.txt` — 精确锁版本
  - `xtask/spikes/ppocrv6/lock.json` — 包版本/index/wheel URL/SHA-256 记录
  - `xtask/spikes/ppocrv6/run_benchmark.ps1` — benchmark 脚本（P50/P95, ≥10次）
- [x] golden corpus 可合法留在仓库并覆盖中文/英文/日文混排、竖排、小字号、主题与 DPI；评价脚本可重复得到 CER 与 rect 有效率。
  - `testdata/ocr/ppocrv6/manifest.json` — 22 项 corpus 标注（CC0-1.0）
  - `xtask/spikes/ppocrv6/generate_corpus.py` — 程序生成 corpus 图片
  - `xtask/spikes/ppocrv6/evaluate.ps1` — CER / rect 有效性评价
- [x] 三种服务拓扑在同一协议、输入、模型与采样规则下完成对比；3 拓扑 × 3 模型共 18/18 次运行成功，原始结果已落盘。
  - `server_thin.py`（拓扑 A）/ `server_paddlex.py`（拓扑 B）/ `worker_once.py`（拓扑 C）
- [x] `/health` 草案能区分 service reachable 与 model downloading/loading/ready/error，并回显 protocol/engine/instance/model id+revision；所有业务与 health 请求都要求 token。
  - `xtask/spikes/ppocrv6/protocol.md` — 最小服务协议草案（/health, /recognize, /shutdown + token）
- [x] `OcrResult` 所需 line/word/物理像素 rect 可稳定获得；实测 word rect 253/253 有效，几何资格门通过。
- [ ] 全新安装与完整缓存后离线复用已验证；缓存损坏检测未完成，保持为 `conditional-go` 残余项，不得视为通过。
- [x] 已产出带原始数据索引的 `decision.md`，结论为 `conditional-go`；正式接入前必须处理热延迟、峰值内存解释与缓存损坏验证，不能把该结论升级为无条件 `go`。

### 6.2 0.22.1

- [x] stdout 与 stderr 各连续输出超过管道容量时进程不死锁，日志内存有明确上限。
- [x] start/start、stop/stop、start/stop 并发均幂等且状态最终一致。
- [x] 未知进程占用 preferred port 时不会被 Blink 终止。
- [x] 受管进程崩溃、超时与应用退出均回收 child/进程树并产生结构化状态。
- [x] `rg "kill_process_by_port" src/app src/domain` 不再命中本地引擎生命周期调用路径（仅保留说明性注释）。

### 6.3 0.22.2

- [x] FunASR 与 PaddleOCR 的 current generation venv 物理隔离。
- [x] 两者复用 uv/Python/download cache，不复用 site-packages。
- [x] domain/app 的 runtime 契约不要求 Python字段；`PythonVenv` 与 `ManagedBinary` 是编译期闭合变体，前端无法提交 runtime kind、URL、executable、argv 或环境变量。
- [x] 同一 generation manifest 的通用部分可表达 Python package artifact 与 native binary/DLL artifact；provider 专属字段不会泄漏到通用状态机。
- [x] `ManagedBinary` 可引用锁定的共享 Python stdlib artifact；引用不创建 venv、不允许 pip，清理时有引用计数保护，且无法解析到用户代码解释器路径。
- [x] 至少用 fake descriptors覆盖 `auto/cpu/gpu_auto/cuda/vulkan/directml` 解析：只产生 descriptor允许且本机兼容/self-test通过的 profile；显式后端失败不回退，`auto` 回退记录原因。
- [x] 状态与 manifest同时记录 requested preference、resolved profile和 actual backend；伪造 health 返回不同 backend时能够检测不一致并进入 degraded/error，而非显示成功。
- [x] 故意在 staging、generation 提升、current 指针切换与首次启动验证阶段制造失败/崩溃，旧 generation 仍可启动且恢复确定。
- [x] CPU/GPU profile 切换不在 current generation 原地覆盖；被占用旧代进入 deferred cleanup 而非强删。
- [x] 单引擎 cleanup 不删除另一引擎环境/模型，公共缓存需独立确认。
- [x] 包状态来自描述符/manifest，`PythonEnvStatus` 不再包含 `funasr_installed` 等引擎专属字段。

### 6.4 0.22.3

- [x] `domain/local_engine` 中 `rg "tauri::|windows::"` 为 0。
- [x] `infra/local_engine` 不依赖 `app` 或 `domain`。
- [ ] FunASR 安装、启动、模型 ready、识别、停止、自动启动、日志、诊断与清理行为通过回归。
- [ ] FunASR 热词、ITN、VAD、模型与旧配置兼容。
- [x] 状态以 `engine_id + service_epoch + revision` 发布，新 epoch 重置旧快照，不再由独立 atomic bool 充当真源。
- [x] engine id、argv、脚本和环境变量无法由前端任意注入。

自动化与静态验收已完成；上方两个未勾选项需要 Windows 真实 FunASR 安装、启动、Model Ready、识别、停止、重启、日志、诊断、空间与清理全流程验证后再关闭。手工验收还必须确认旧 `funasr-server-status` / `funasr-server-log` 兼容事件可接收、未知端口占用进程不被终止、旧 token 被拒绝，以及 Blink 退出后 Job Object 下的进程树归零。

### 6.5 0.22.4

- [ ] `windows`、`paddleocr`、`auto` 三种路由语义与错误/回退行为符合 §3.9。
- [ ] PP-OCRv6 中文、英文、日文混排识别可用，word rect 能驱动现有阅读模式双向高亮。
- [ ] 同时触发多个 OCR 首次请求只拉起一个 server。
- [ ] 每个请求只保留一份图片且不进入重放队列；deadline/cancel 后不再发送业务请求或提交结果。
- [ ] 旧截图 session 的延迟结果不会覆盖新选区。
- [ ] OCR in-flight 时不被 idle timer 停止；空闲 TTL 到期后内存可回收。
- [ ] 升级/全新安装默认均为 `windows`；未安装或未热启 PP-OCRv6 时 WinRT OCR 不增加隐式冷启动等待。

### 6.6 0.22.5

- [x] 「引擎」页清晰区分用户代码解释器与 Blink 托管运行时。
- [x] 每张引擎卡片只展示 descriptor声明的计算设备；本机不兼容项禁用并说明原因，运行态同时展示实际 backend/device和 fallback reason。
- [x] FunASR 管理从语音页迁出，语音页保留业务设置和稳定跳转入口。
- [x] 两张引擎卡片共用 renderer、状态协议和日志组件，无复制的专属 lifecycle 实现。
- [x] 旧 epoch/revision/status 与旧 instance log 不覆盖当前 UI。
- [x] 清理确认准确显示 generation/cache scope，取消操作只影响匹配 operation id。
- [x] dark/light 与至少一个非默认主题下状态、错误、按钮对比度合格。
- [x] 无新增 emoji 图标、inline `style.display` 或 bundler/npm 依赖。

实现提交 `d8b266a`；本地运行时设置模块的 4 个 `node --test` 文件共 74 条断言通过。

### 6.7 0.22.6

- [ ] `cargo test --bin blink` 全绿，新增核心状态转换与 adapter 测试覆盖。
- [ ] `cargo check` 与 release 资源校验通过。
- [ ] Alt+Space 唤起、focus 和首个搜索结果指标无可测回退。
- [ ] 未启动本地引擎时不新增常驻 Python 进程；默认 OCR 使用后能按 TTL 释放。
- [ ] Blink 正常退出无受管孤儿进程；异常退出遗留实例不会通过端口误杀无关进程。
- [ ] Windows Job Object/lease 恢复矩阵通过：PID 重用、路径不符、token 不符时均不接管、不终止。
- [ ] 无网络、模型损坏、磁盘不足、端口冲突均给出可行动错误且保留 WinRT 逃生口。
- [ ] release 包包含锁文件/脚本/许可，不包含预下载模型或 staging 环境。

### 6.8 0.22.7

- [ ] 锁定并校验官方 Windows `runtime-llamacpp` archive、SenseVoice Q8 GGUF和 FSMN-VAD GGUF；完整缓存后断网可复现，推理 generation不含 torch、torchaudio或 funasr Python包。
- [ ] 上游轻量 HTTP wrapper可由 Blink托管启动，现有 OpenAI-compatible client无需业务协议分叉即可完成固定音频转录；响应只要求 HTTP/JSON/UTF-8合法且文本非空，不计算 CER/WER、不设准确率门。
- [ ] 直接 CLI/Rust spawn与上游标准库 HTTP wrapper均报告 runtime/模型/共享托管 Python物理体积与本引擎增量/总磁盘、P50/P95延迟、CPU、稳定/峰值 RAM/VRAM、连续短句和两个并发请求；不得把 wrapper存活误报为模型热态。
- [ ] wrapper由锁定的 Blink托管 Python以隔离模式启动，不创建 venv、不执行 pip、不读取用户解释器或用户 site-packages；解释器 artifact被其他引擎引用时单引擎清理不会删除它。
- [ ] 超时/ESC取消、wrapper崩溃重启、迟到结果、临时文件和子进程回收通过；请求结束或停止后无 `llama-funasr-*` 残留进程。
- [ ] `decision.md` 给出 `adopt-llamacpp-http | fallback-cpp-onnx-spike | no-go`。若选择 adopt，必须在同一子版本注册新的 final-only可选 STT引擎，并保持现有 Python FunASR默认配置与回退入口不变。
- [ ] 新引擎的 descriptor只开放实测通过的 compute profiles，health回报 actual backend；显式 CUDA/Vulkan失败不静默使用 CPU。

---

## 七、关联文件速查（规划）

**0.22.0**：

- `xtask/spikes/ppocrv6/` — **新增（已就位）**：开发期 PP-OCRv6 可复现安装、三拓扑 wrapper、benchmark/evaluate/cache_tests 脚本、锁、协议草案与 decision。
  - `README.md` — 环境、命令、目录、网络需求、复现步骤
  - `requirements.txt` — 精确锁定的 Python 依赖
  - `lock.json` — 包版本/index/wheel URL/SHA-256 记录
  - `server_thin.py` — thin Blink HTTP wrapper（拓扑 A）
  - `server_paddlex.py` — PaddleX basic serving 适配（拓扑 B）
  - `worker_once.py` — 单次 worker 实验（拓扑 C）
  - `install.ps1` — 环境安装脚本（uv/venv/PaddlePaddle/PaddleOCR + SHA-256）
  - `run_benchmark.ps1` — benchmark 运行脚本（P50/P95, ≥10次, 三拓扑×三档模型）
  - `evaluate.ps1` — CER / rect 有效性评价脚本
  - `cache_tests.ps1` — 缓存验证实验脚本
  - `cleanup.ps1` — 清理脚本
  - `generate_corpus.py` — golden corpus 生成脚本
  - `protocol.md` — 最小服务协议草案（/health, /recognize, /shutdown + token）
  - `decision.md` — 资格门逐项判定与最终结论
- `testdata/ocr/ppocrv6/` — **新增（已就位）**：可再分发 golden corpus（CC0-1.0）、期望文本/rect 规则与数据来源说明。
  - `manifest.json` — 22 项 corpus 标注
  - `README.md` — 许可、来源、覆盖范围、几何判定规则
- `docs/phases/0.22-local-model-runtime-ppocrv6.md` — 更新：记录开工状态、实测版本和官方模型 id、原始数据索引、资格门结论、协议草案与实测预算。

**实测残余风险**（benchmark 运行后更新）：
- PaddlePaddle 官方 wheel index 可能变更 URL 或 wheel 文件（SHA-256 不稳定）
- PaddleOCR 模型仓库的 model id/revision 可能随版本更新而变化（上游不提供稳定 checksum）
- PaddleOCR 2.9.x 默认 pipeline 可能随 patch 版本切换模型 revision
- 以上风险在 `decision.md` §9 的「供应链残余风险」节逐项列出

**0.22.1（已完成）**：

- `src/infra/local_engine/mod.rs` — 通用运行时模块入口与公共 API。
- `src/infra/local_engine/state.rs` — 状态、实例身份与操作提交模型。
- `src/infra/local_engine/process.rs` — `ManagedProcess` 生命周期、并发操作与进程树回收。
- `src/infra/local_engine/log_pipe.rs` — 有界行累积、历史 ring buffer 与实时 broadcast。
- `src/infra/local_engine/tests.rs` — 生命周期、日志与确定性竞态测试。
- `src/infra/platform/process.rs` — Job Object、进程身份验证与端口探测。
- `src/infra/mod.rs` — 注册 `local_engine` 基础设施模块。
- `src/domain/stt/funasr.rs` — 迁移到 `ManagedProcess`，保留 FunASR 特有逻辑。
- `src/app/commands/stt/maintenance.rs` — 托管实例、token 绑定的日志转发与 readiness。
- `Cargo.toml` — 启用 Windows Job Objects API feature。

**0.22.2（已完成）与 0.22.3（代码完成，待 Windows 手工验收）**：

- `src/domain/local_engine/` — **新增（已完成）**：领域类型、三维状态、描述符、连接快照与 adapter 契约。
- `src/app/local_engine/` — **新增（已完成）**：`LocalEngineService`、FunASR adapter、registry、事件出口与兼容投影。
- `src/app/commands/stt/maintenance.rs` — **改造（已完成）**：旧 FunASR IPC 保持名称不变并薄转发进程级 service。
- `src/infra/local_engine/port.rs` — **新增（已完成）**：本地 endpoint 分配、service token、fingerprint 与身份验证原语。
- `src/infra/local_engine/runtime.rs` — **新增（已完成）**：provider-neutral artifact/generation manifest、状态、backend一致性、原子指针、引用与清理契约。
- `src/infra/local_engine/providers/python.rs` — **新增（已完成）**：`PythonVenvProvider` 的 uv/Python/隔离venv/package事务安装与self-test。
- `src/infra/local_engine/providers/binary.rs` — **协议位（已完成）**：`ManagedBinaryProvider` 的闭合 descriptor、manifest/self-test与只读工具 artifact依赖契约；网络安装实现按后续真实引擎接入。
- `src/infra/platform/python/mod.rs` — **改造（已完成）**：兼容路径降为通用 Python provider 基础状态/原语，FunASR字段由 STT 层投影。
- `src/main.rs` — **改造（已完成）**：注册进程级唯一 service、自动启动策略与退出同步回收。
- `src/infra/event_names.rs` / `frontend/js/shared/event-names.js` — **改造（已完成）**：通用 local-engine status/log 事件常量。
- `resources/stt/funasr/blink_stt_server.py` — **改造（已完成）**：环境变量身份注入、token fail-closed、身份和模型状态 health 回显。

**0.22.4**：

- `src/domain/ocr/` 或现有 OCR domain 模块 — 改造（规划）：`OcrBackendRouter` 与配置快照。
- `src/domain/capability/builtins/ocr_engine.rs` — 改造（规划）：受限请求上下文、复用 router，保持结果契约。
- `src/app/local_engine/paddleocr.rs` — **新建（规划）**：PaddleOCR adapter 与 DTO 映射 wiring。
- `resources/` 下引擎产物目录 — **新增（规划）**：只放随二进制发布的 server 脚本、lock/schema 等产物级资源。
- `src/domain/config/` — 改造（规划）：OCR backend/model/device/lifecycle 配置。
- `frontend/js/screenshot/` — 改造（规划）：按需启动进度、session 取消与 fallback 原因展示；不改变现有 OCR 结果交互契约。
- `Cargo.toml` / `src/main.rs` — 改造（规划）：资源嵌入/注册与测试依赖收口。

**0.22.5（已完成）**：

- `frontend/settings.html` — 改造：引擎页新增本地模型运行时，语音页移除生命周期实现。
- `frontend/js/settings/tabs/engines/local-runtime.js` — **新增**：catalog/status/action/event 编排与统一 controller。
- `frontend/js/settings/tabs/engines/local-engine-card.js` — **新增**：通用引擎卡片 renderer。
- `frontend/js/settings/tabs/engines/local-engine-cleanup-modal.js` — **新增**：引擎与共享资源清理确认。
- `frontend/js/settings/tabs/engines/local-engine-state.js` / `local-engine-process.js` / `local-engine-hooks.js` — **新增**：状态归并、进程状态展示与设置页生命周期 hooks。
- `frontend/js/settings/tabs/voice.js` — 改造：仅保留 STT 业务配置与稳定跳转。
- `frontend/css/views/settings/settings-local-runtime.css` — **新增**：运行时/引擎卡片样式，遵守七层与 token。
- `frontend/js/i18n/zh.js` / `en.js` — 改造：中英文运行时、状态、错误与清理文案。
- `src/app/commands/local_engine.rs` / `src/app/local_engine/dto.rs` — **新增**：受限管理命令、catalog/status/log/storage DTO 与 allowlist 路由。

**0.22.6**：复验以上生产文件与 release 资源，不新增独立功能面。

**0.22.7**：

- `xtask/spikes/funasr-runtime/` — **新增（规划）**：官方 Windows llama.cpp/GGUF archive与模型锁、轻量 HTTP wrapper、链路 smoke、资源采样、并发/取消/回收及清理脚本；仅在主路径失败时追加 C++ ONNX备选实验。
- `xtask/spikes/funasr-runtime/decision.md` — **新增（规划）**：`adopt-llamacpp-http | fallback-cpp-onnx-spike | no-go` 结论、资源数据、限制与条件接入记录。
- `testdata/stt/funasr-runtime/` — **新增或复用既有合法语料（规划）**：用于非空转录和协议 smoke的可再分发固定音频，不建设准确率 corpus。
- `src/app/local_engine/funasr_gguf.rs` — **条件新增（规划）**：阶段 A通过后注册 `ManagedBinaryProvider` adapter、wrapper启动参数、health与 STT DTO映射。
